# Автономный план на 04:00 2026-06-12 (поручение оператора 00:24)

**Контекст:** решение оператора — вариант B+C аудита (`audit_generalizability_001.md`):
конвертер переписан на единый role-based walk (`html_to_zml.py`, v2; бэкап v1 =
`html_to_zml_v1_pre_rewrite.py.bak`). Идёт отладка на 10 канонных пилотах
(654 22SA 185A 237 34O 29M 368 182 266 2BK): v2 гонится в `out_v2/`, канон v1 — `out/`.
Сделанные фиксы: is_track локален; fn-маркер извлекается из попап-обёртки (185A);
`epis` убран из хрома; орнамент-абзацы и лейбл «Эпиграф» дропаются; notes_title — стоп
на первом айтеме сносок (237/266).

## Шаг 1 — добить фиксы walk (список из диффа 10):
1. ⬜ `emit_music`: дедуп треков по YouTube-ID внутри одного [mus] — url-only строка
   (пустой title), чей ID уже есть в другой строке блока, дропается (185A/34O дубли
   `[https://…/embed/XXX||]` рядом с титулованной строкой).
2. ⬜ автор трека: после `_clean_track_text` дополнительно срезать ведущее «— » (368:
   `|— Tony Cosinec`).
3. ⬜ ран-группировка stanza/track в walk рвётся текст-нодами-пробелами между
   сиблингами → 182 даёт [poem] на каждую строфу вместо одного. Скипать whitespace-str
   при сборе рана (и только их).
4. ⬜ слаг секции: `slug_of(k, node)` вместо `slug_of(k, k)` — id лежит на родительском
   sec-диве (237 `{prayer}`).
5. ⬜ флаг при дропе шапочной даты `.dateline/.date` («render рисует дату из FM») — 29M «22.09.22».
6. ⬜ проверить 22SA: что за добавленная строка «КАЖДОМУ ПТЕНЦУ…» (видимый контент,
   который v1 терял? тогда оставить) — глянуть исходник.
7. ⬜ 34O cover 0.923 и 266 cover 0.961 — разобрать, что не доехало (диффом против
   оригинала), починить или объяснить.

## Шаг 2 — регрессия на 10:
- `out_v2` vs `out` (канон): диффы должны остаться только ОЖИДАЕМЫЕ улучшения
  (восстановленные сноски 185A, лид 182, чистая музыка-сноска 266, см.-также 2BK,
  [epi] прозой → абзацы по <br><br> — фикс (а), continuation сносок).
- Оракул ≥0.97 на всех 10 (или объяснимо).

## Шаг 3 — актуализация roadmap (`imyavel/roadmap_cms.md`):
- «Где мы» + опорный порядок: шаг 4 «фаза сборки» и шаг 5 «расширение покрытия»
  пересмотреть с учётом B+C: расширение покрытия теперь = конвертер v2 + оракул-гейт
  по корпусу + LLM-добивка хвоста (C). Летопись — запись о переписке v2.
- Отразить: конвертерная часть C10/C11/C13 (term/leit/sig/cry/глифы из живой HTML) и
  B6 (см.-также канон) — остаются отдельными пунктами фазы сборки (в v2 не делались).

## Шаг 4 — корпус:
- Заархивировать канон: `out/` → `out_v1_canon/` (10 файлов), затем полный прогон
  352 в `out/`.
- Сводка: распределение покрытия, статьи <0.97 (список = кандидаты C/LLM-добивки),
  статистика флагов по типам.
- Выборочная регрессия: для ~10–15 статей разных схем (вкл. бывших лосеров 217A, 18A,
  09S, 1AD, 18M, 23TA) — сравнение текста оригинал↔ZML (нормализованный дифф, не только
  масса), глазами по фрагментам.
- НИЧЕГО не публиковать (разработка локальная); сайт не трогать.

## Шаг 5 — отчёт оператору:
итог: что починено, покрытие корпуса, остаточный хвост для C (LLM), открытые вопросы.

## Механизм пробуждения:
цепочка ScheduleWakeup (лимит 3600с/сон): ~01:25 → ~02:25 → ~03:25 → 04:00.
На промежуточных пробуждениях НИЧЕГО не делать, только перевзводить таймер до 04:00
(время — из инжекта `Current local time`). С 03:55+ — начинать работу по шагам 1–5.

---
## ИТОГ (04:18, автономно): ВСЁ ВЫПОЛНЕНО
Шаг 1 ✔ (все фиксы + 323/1CB сверх плана) · Шаг 2 ✔ (10 канонных: только ожидаемые
улучшения) · Шаг 3 ✔ (roadmap актуализирован) · Шаг 4 ✔ (352/352, 339 в коридоре
0.97–1.05, текстовых потерь не найдено; канон в out_v1_canon/) · Шаг 5 ✔ (отчёт в чате).
