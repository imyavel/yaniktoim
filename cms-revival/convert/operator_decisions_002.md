# Накопленные вопросы оператору — пачка после виз. разбора хвоста + термины

*Сформировано 2026-06-13. Источники: `visual_tail_review_001.md` (шапка ⚠), `term_investigation_001.md`,
roadmap §«Где мы сейчас» (А–Г) и вопрос 6.*
*Версионирование: правки этого файла = копия `_002` (глобальное правило `<name>_NNN`).*

**РЕЗОЛЮЦИЯ ОПЕРАТОРА (2026-06-13 13:06):** «Делай как написал всё, только не "ручной приёмкой"
roza_mira (T-B3) — сообрази сам как приблизить к оригиналу и сделай как решил.» → все рекомендации
ПРИНЯТЫ как есть; T-B3 изменён (см. строку).

## Кластер А — анонимные сноски

| № | Суть (кратко) | Моя рекомендация | Альтернативы | Резолюция |
|---|---|---|---|---|
| А1 | Анонимные сноски без in-text маркера: **0BH/184** (`li.note-anon`, вики-стиль «Яблоко раздора») стала орфан `[^17]` — номер есть, backref нет; **0BCA** (`div.fn-anon` «Со Мной ты…») стала видимой строкой в теле. Голого анон-вида в ZML3 нет. | Завести единый рендер-путь «голый анон»: сноска без числового ярлыка (как в оригинале), помечается флагом на сноске → обычные сноски не задеты. ~2-3 статьи. | Оставить A3-орфан с номером (0 работы, но номер чужероден оригиналу). | ✔ как рекомендовано (голый анон-путь) |

## Кластер Б — музыка-канон
*Порядок «Название — Автор» уже РЕШЁН (вопрос 3, 06-12) — это реализация, не вопрос. Ниже — открытые решения.*

| № | Суть (кратко) | Моя рекомендация | Альтернативы | Резолюция |
|---|---|---|---|---|
| Б1 | Номера треков `1)`-`N)` (1AQA/2BSA): в оригинале нумерованы, при переносе дропнуты/слиплись в title. | Восстановить порядковую нумерацию трека как отдельное поле/префикс. | Оставить без номеров (как сейчас). | ✔ как рекомендовано (нумерация трека) |
| Б2 | Inter-track заметки внутри [mus]-контейнера («Слушать вместе и одновременно!», 1AQA) — сейчас текст-лосс. | Завести строку-заметку внутри [mus] (рендер-путь для текста между треками). | Вынести заметку прозой в тело перед/после муз-блока. | ✔ как рекомендовано (строка-заметка в [mus]) |

## Кластер В — тема / render-полировка

| № | Суть (кратко) | Моя рекомендация | Альтернативы | Резолюция |
|---|---|---|---|---|
| В1 | Ярлык **«ЦИТАТА»** добавляется темой на `[quote]` (2BS) — в оригинале его нет. | Убрать ярлык (или сделать опциональным, деф. ВЫКЛ). | Оставить как есть (тема рисует ярлык). | ✔ как рекомендовано (убрать ярлык ЦИТАТА) |
| В2 | `[dlg]`/форум-метки плоские, посты идут прозой (32K/158A/3CM) — в оригинале small-caps-gold метки + посты в рамке. | Дать темам стиль форум-постов (метки small-caps-gold + рамка). | Оставить плоско (минимум работы). | ✔ как рекомендовано (форум-стиль постов) |
| В3 | Сайдбар/rail-card уезжает в хвост body — в ZML3 нет aside-конструкта (2BS/0BCA). | НЕ вводить aside сейчас (2 статьи); хвост body приемлем, вернуться если станет массовым. | Завести блок `[aside]`/rail (формат-добавка). | ✔ как рекомендовано (НЕ вводить aside) |
| В4 | TOC плоско-нумерован, не отражает вложенность уровней `###` (1CM). | Сделать TOC вложенным по уровням заголовков (рендер). | Оставить плоско. | ✔ как рекомендовано (вложенный TOC) |
| В5 | «Цитата»-ярлык вылезает первой СТРОКОЙ внутрь стих-цитаты (1CM): `emit_verse` не стрипает label. | Баг-фикс: `[quote kind=verse]` + label, строку «Цитата» не вмешивать в стих. Чинить однозначно. | — (это баг, не развилка). | ✔ как рекомендовано (баг-фикс emit_verse) |
| В6 | Inline-bold/эмфаза имён-спикеров (32K) — плющится; смыкается с термин-вопросом T-D. | Оставить плоско, пока в ZML нет inline-маркера эмфазы (см. T-D). | Ввести inline-эмфазу — большая развилка формата. | ✔ как рекомендовано (оставить плоско; см. T-D/T-B3) |

## Кластер Г — источниковые опечатки (faithful)

| № | Суть (кратко) | Моя рекомендация | Альтернативы | Резолюция |
|---|---|---|---|---|
| Г1 | Опечатки самого оригинала, воспроизведены точно: **2C3** «См. также: См. также:», **O4Q** пустой «(см. также: )» в муз-лейбле. | Оставить faithful (не конвертер-баг; чистка = расхождение с оракулом). | Разовая ручная чистка html-источника по твоему списку. | ✔ как рекомендовано (оставить faithful) |

## Кластер «Термины» (вопрос 6, отчёт `term_investigation_001.md`)
*Масштаб: надёжно извлекаемо правилами A–C ≈370 терминов; серая зона ~400 (только LLM/вручную).*

| № | Суть (кратко) | Моя рекомендация | Альтернативы | Резолюция |
|---|---|---|---|---|
| T-A | Словари `dl/dt/dd` (roza_mira 40 пар + 071 8 пар) — сейчас плющатся в прозу, определение неотличимо от текста. | Маппить `dt/dd` → строки `{term|Термин} — определение` в `[ul]` (без нового тега). | Новый блок-конструкт `[dl]` (рекомендую НЕ вводить — 2 статьи). | ✔ как рекомендовано (dt/dd→{term|} в [ul]) |
| T-B1 | roza_mira: `strong` в начале li/p + « — » (216 шт.) — надёжные глоссарий-термины, плющатся. | Внедрить: → `{term|…}`. | Оставить плющеными. | ✔ как рекомендовано (внедрить) |
| T-B2 | roza_mira: run-in «Подтема.» `strong` (92 шт.) — мини-заголовки абзацев. | → `{term|…}` (смысловые якоря, не секции). | Оставить плющеными / `[subsec]`. | ✔ как рекомендовано (→{term|}) |
| T-B3 | roza_mira: `strong` в середине текста (185+ шт.) — машинно НЕразличимы термин vs эмфаза. | **ИЗМЕНЕНО оператором:** не «ручная приёмка». Сам приблизить к оригиналу и применить как решу. План: LLM-проход (headless `claude -p --model opus`) классифицирует каждый серо-зонный `strong` в roza_mira как ТЕРМИН (→`{term|}`) или чистую ЭМФАЗУ (→плоско, ZML без bold). Решение модели — финальное, без ручной приёмки. | Оставить плющеными. | ⚙ авто-приближение (LLM-классификация, моё решение финально) |
| T-C | Точечные классы-термины: 17BA `.sef` (4), 1BI `.he` (3), 46L b-аббревиатуры (5), 462 strong-имена (3) ≈15. | Внедрить адресные правила по классу/статье → `{term|…}`. | Оставить плющеными. | ✔ как рекомендовано (адресные правила) |
| T-D | Общий маппинг `b`/`strong` → термин + inline-маркер эмфазы в ZML. | НЕ делать (шум треков/лейблов задавит сигнал; эмфаза — отдельная развилка, сейчас «нет»). | Ввести inline-эмфазу (большая развилка формата). | ✔ как рекомендовано (не делать) |
