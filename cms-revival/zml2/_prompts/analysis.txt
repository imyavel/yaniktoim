Ты — технический аналитик и типограф-инженер. Нужно подготовить ПОДРОБНЫЙ сравнительный отчёт в формате Markdown для решения о портировании дизайна.

КОНТЕКСТ. Проводился дизайн-эксперимент: один и тот же литературный текст (каббалистическая поэма-трилогия с проза.ру) оформлен 10 раз — 5 ранних «узких» вариантов и 5 новых «широких», в пяти эстетиках (airy, editorial, classic, modern, manuscript). Заказчик будет выбирать один или несколько из этих стилей и заменять им ТЕКУЩИЙ дизайн/рендер в проекте yaniktoim (формат zml). Отчёт должен помочь заранее спланировать портирование.

ИСТОЧНИКИ — ИЗУЧИ ИХ ВНИМАТЕЛЬНО (читай инструментом Read):

A) Эксперимент (папка C:\Users\admin\design-experiment\):
   - Узкие v1: variant_airy, variant_editorial, variant_classic, variant_modern, variant_manuscript — в каждой index.html (сама вёрстка) и run.log (резюме автора по блокам).
   - Широкие v2: v2_airy, v2_editorial, v2_classic, v2_modern, v2_manuscript — аналогично (index.html + run.log).
   - article.txt — исходный текст.
   Прочитай ВСЕ 10 run.log (это структурные резюме) и хотя бы бегло сами index.html (CSS-подход, ширина, шрифты, как оформлены блоки).

B) Текущий формат yaniktoim (папка C:\Users\admin\yaniktoim\):
   - _zml_spec\SPEC.md — спецификация формата zml (ОСНОВНОЙ документ: какие блоки/теги существуют, их семантика).
   - _zml_spec\demo.zml и _zml_spec\demo.html и _zml_spec\style.css — демонстрация.
   - zml\237.zml, zml\416A.zml, zml\654.zml — реальные примеры разметки.
   - templates\article.html — шаблон рендера статьи (текущий дизайн).
   - style.css (в корне репозитория) — текущие стили продакшена.
   - src\transform_prompt.md и src\3_transform.py — как сырой текст превращается в zml/json (модель данных).
   - json\078.json (или любой *.json) — структурированный вид статьи.
   Цель: понять ТЕКУЩУЮ блочную модель zml и то, как она сейчас выглядит/рендерится.

ЗАДАЧА — напиши подробный отчёт в Markdown по пути:
C:\Users\admin\design-experiment\REPORT_blocks_vs_zml.md

Структура отчёта (соблюдай, дополняй по необходимости):

1. **Краткое резюме** (executive summary): главные выводы и рекомендации в 5–8 пунктах.

2. **Инвентарь логических блоков**. Сведи в таблицу логические блоки, найденные в тексте (главный заголовок, авторский глиф <\^/>, подзаголовок-жанр, артикул/дата, эпиграфы 1/2/3 разной природы, вступительная молитва, заголовки глав «Стих N», стихотворные строфы, капс-пассажи/крещендо, сноски-маркеры *N, раздел сносок, финальный слоган, колофон/копирайт, декоративные разделители). Для каждого блока — присутствует ли он и КАК оформлен в каждом из 5 стилей (можно усреднять v1/v2, отмечая различия).

3. **Сравнение стилей между собой**. По ключевым осям: ширина/сетка (1/3 vs 2/3), палитра и контраст, шрифтовые пары, трактовка эпиграфов, модель стиха (строка/строфа), сноски (надстрочные ссылки vs список внизу), капс-пассажи, глифы/эмотиконы, декор. Таблица + короткий разбор сильных/слабых сторон каждого стиля для нашего контента.

4. **Текущая модель zml (yaniktoim)**. Опиши, какие блоки/теги уже есть в zml (по SPEC.md и примерам), как они рендерятся (article.html + style.css), и где границы текущего формата.

5. **Сопоставление: блоки эксперимента ↔ блоки zml**. Таблица соответствий: для каждого экспериментального блока — есть ли эквивалент в zml, частичный или отсутствует. Явно отметь: (а) что zml уже покрывает; (б) что придётся ДОБАВИТЬ в формат/рендер; (в) что в эксперименте лишнее/декоративное и в модель данных не нужно.

6. **Примерная карта портирования**. Для сценария «взять стиль X и заменить им текущий рендер»: что меняется в templates\article.html, что в style.css, нужны ли новые типы блоков/атрибуты в zml и в transform-пайплайне (src\3_transform.py / transform_prompt.md). Отдельно отметь, что можно сделать чисто на уровне CSS/шаблона (без изменения данных), а что требует расширения модели.

7. **Ключевые развилки (decision points)**. Пронумерованный список архитектурных решений, которые заказчику нужно принять заранее, с вариантами и рекомендацией по каждому. Примеры осей: сноски (надстрочные якоря vs концевой список vs боковые маргиналии); модель стиха (хранить построчно или абзацем); сохранение авторских глифов как данных vs как оформления; ширина колонки и адаптив; шрифтовая стратегия (один стиль на весь сайт vs пер-статейные темы); смешение стилей (можно ли брать «несколько» — и как это уживётся в одном рендере).

ТРЕБОВАНИЯ К ОТЧЁТУ:
- По-русски, подробно, инженерно, с таблицами Markdown.
- Опирайся на ФАКТЫ из прочитанных файлов (цитируй конкретику: имена тегов zml, классы CSS, значения ширины и т.п.), а не на общие слова.
- Можешь звать под-агентов / CLI / веб-поиск, если поможет, но не обязательно.
- Думай столько, сколько нужно для качественного анализа.

КОГДА отчёт записан — твой ПОСЛЕДНИЙ ответ (stdout) сделай кратким (8–12 строк): главные выводы, число найденных развилок и путь к .md.
