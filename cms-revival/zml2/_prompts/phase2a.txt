Ты — аналитик формата разметки. Задача: провести АУДИТ НАСЛЕДОВАНИЯ существующего формата zml (zml1) — определить, какие его элементы ОБЯЗАНЫ перейти в новый формат ZML 2, опираясь на РЕАЛЬНЫЙ КОРПУС статей, а не на догадки.

КОНТЕКСТ. Проект: переход на ZML 2 (единый семантический источник → несколько тем оформления). Дизайн-темы делались всего на 2 статьях и не могли вскрыть всё, что zml1 накопил на реальном корпусе (~350 статей): внутренние ссылки, ссылки на Зохар, музыкальные блоки, порядок секций, изображения и т.п. Эти конструкции обкатаны корпусом — часть из них обязательна к наследованию. Твоя работа — это отдельный, КОРПУС-управляемый вход (НЕ смотри на дизайн-темы; работай только со стороны zml1).

ИСТОЧНИКИ (прочитай инструментом Read; корпус — просканируй):
- Спецификация: C:\Users\admin\yaniktoim\_zml_spec\SPEC.md (полный перечень блоков/inline-конструкций zml1 и их семантика).
- Правила трансформации / модель данных: C:\Users\admin\yaniktoim\src\transform_prompt.md и C:\Users\admin\yaniktoim\src\3_transform.py.
- Примеры разметки: C:\Users\admin\yaniktoim\zml\*.zml
- РЕАЛЬНЫЙ КОРПУС: C:\Users\admin\yaniktoim\json\*.json — структурированные статьи. Определи структуру json сам (Read нескольких файлов + при необходимости Grep/Bash для подсчётов) и выясни ФАКТИЧЕСКУЮ частоту использования каждого типа блока/конструкции по всему корпусу.

Сначала разберись в связке: raw → (3_transform.py) → json и где здесь zml-разметка (в SPEC описан zml-синтаксис; в json — структурированный результат). Затем посчитай, что реально встречается в корпусе.

НАПИШИ документ: C:\Users\admin\design-experiment\zml1_inheritance_001.md

Структура:
1. **Карта формата zml1.** Перечисли ВСЕ блок-типы и inline-конструкции zml1 (из SPEC.md + json): напр. заголовки/секции `##`, `[poem]`, `[epi]`/эпиграф, `[num]`, `[sub]`, `[mus]`, сноски `[^N]` и пара ref↔fn, внутренние ссылки `[[nnn]]`, ссылки на Зохар `{chapter|N}`, `[url|анкор]`, маркер `^СЛОВО`, CAPS-pass, изображения, порядок секций и пр. По каждому — краткая семантика.
2. **Фактическая частота в корпусе.** Таблица: тип → сколько статей/вхождений его используют (по скану json). Отдели «ядро» (часто), «редкое» и «не встречается / только в SPEC».
3. **Вердикт наследования.** Для КАЖДОГО типа — метка:
   - **must-inherit** — реально используется корпусом, несёт семантику, обязан быть в ZML 2;
   - **под вопросом** — редкое/спорное, решим при проектировании;
   - **deprecated / сворачивается** — можно убрать или выразить иначе (укажи чем заменить).
   Каждый вердикт — с обоснованием (частота + семантическая роль).
4. **Особое внимание — недизайнерская семантика.** Явно перечисли конструкции, которых НЕ было в дизайн-макетах, но которые есть в корпусе и важны: внутренние/Зохар-ссылки, музыка, изображения, порядок секций, спец-инлайны. Это кандидаты, которые иначе потерялись бы.
5. **Заметки для слияния с дизайн-семантикой (Фаза 2b).** Где zml1-конструкция пересекается с тем, что нашли дизайнеры (напр. эпиграф, сноски, поэма) — отметь для последующего объединения, но НЕ объединяй здесь.

Опирайся на ФАКТЫ (имена из SPEC, реальные подсчёты по json). Если структура json не даёт прямого счёта по типам — опиши, что смог измерить, и помечай оценки как оценки. Думай столько, сколько нужно.

В конце (stdout) — краткое резюме (8–14 строк): размер корпуса (сколько json), число типов, сколько must-inherit / под вопросом / deprecated, главные «недизайнерские» конструкции, путь к файлу.
