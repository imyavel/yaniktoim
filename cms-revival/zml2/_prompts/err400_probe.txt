Ты — инженер-диагност по Anthropic API и Claude Code. Задача: установить НАИБОЛЕЕ ВЕРОЯТНУЮ причину повторяющейся ошибки 400 при headless-прогонах и проверить/опровергнуть выдвинутую гипотезу. Не принимай гипотезу на веру — опирайся на доказательства из транскриптов и документации.

=== ФАКТЫ ===
- Инструмент: Claude Code CLI 2.1.119, запуск `claude -p` (headless, print-mode), модель `claude-opus-4-8`, extended thinking включён (adaptive, явно уровень не задавался), `--permission-mode bypassPermissions`, промпт подавался через stdin, рабочая директория `C:\design-lab`.
- Контент задач: русский литературный текст (кириллица) + необычные авторские глифы `<\^/>`, `;;;;`, `;::)))`, `^`, `::`. Агенты читали несколько файлов (Read), писали крупный HTML/MD (Write), затем должны были напечатать итоговое резюме.
- Ошибка (дословно, повторялась ≥3 раз):
  `API Error: 400 {"type":"error","error":{"type":"invalid_request_error","message":"messages.1.content.56: \`thinking\` or \`redacted_thinking\` blocks in the latest assistant message cannot be modified. These blocks must remain as they were in the original response."},"request_id":"..."}`
  В другом случае индекс был `messages.1.content.16`. ВАЖНАЯ ДЕТАЛЬ: всегда `messages.1` (первый ассистентский ход), но номер контент-блока разный — то есть в ОДНОМ первом ассистентском сообщении копится длинная цепочка interleaved thinking+tool_use.
- Наблюдения по последствиям: иногда падение происходило ДО записи файла (v2_editorial — файл отсутствовал, перезапуск помог), иногда ПОСЛЕ записи (синтез — файл цел, потерян только stdout-итог). Недетерминированно.

=== ВЫДВИНУТАЯ ГИПОТЕЗА (проверить: подтвердить / опровергнуть / уточнить) ===
«Наиболее вероятный триггер — тяжёлый не-ASCII контент (кириллица + глифы): при пересборке истории клиентом thinking-блок подвергается юникод-нормализации/переэнкодингу, байты внутри подписанного блока меняются → подпись не сходится → 400.»

=== ЧТО ИССЛЕДОВАТЬ (инструменты Read, Glob, Grep, Bash, WebSearch, WebFetch доступны) ===
1. Транскрипты сессий: каталог `C:\Users\admin\.claude\projects\C--design-lab\` — файлы `*.jsonl`. Изучи:
   - структуру первого ассистентского сообщения: сколько в нём блоков, как чередуются `thinking`/`tool_use`;
   - есть ли поля `signature`, есть ли `redacted_thinking`, пусто ли тело `thinking`;
   - есть ли записи об ошибках/повторных попытках; есть ли признаки авто-компакции/обрезки контекста.
2. Логи ошибок: `C:\Users\admin\design-experiment\_logs_*.log` и `...\v2_editorial\run.log` — собери все вхождения 400 и индексы content.N.
3. Документация Anthropic по extended thinking в многошаговых диалогах: требование возвращать thinking-блоки дословно с подписями; роль `signature`; «interleaved thinking» beta-заголовок; ограничения при tool use. Найди первоисточник (docs.anthropic.com / docs.claude.com).
4. Известные баги Claude Code по этой ошибке: поищи в GitHub issues anthropics/claude-code формулировку «thinking or redacted_thinking blocks ... cannot be modified» / 400 при -p + thinking + tool use. Зафиксируй, что является клиентской (CLI) проблемой, а что — ожидаемым поведением API.

=== ВЫХОД ===
Запиши отчёт: `C:\Users\admin\design-experiment\error400_analysis_001.md`
- Вердикт: НАИБОЛЕЕ ВЕРОЯТНАЯ причина (с уверенностью и обоснованием на доказательствах).
- Оценка выдвинутой гипотезы про не-ASCII: подтверждается / опровергается / частично — с аргументами (напр.: если подпись считается по нормализованному/каноничному представлению или по encrypted payload, то кириллица сама по себе не должна ломать подпись — проверь это по докам/транскриптам).
- Ранжированный список альтернативных причин (напр.: клиентская реассемблия/сериализация истории в CLI; авто-компакция, задевающая блок; рассинхрон порядка interleaved thinking; гонки параллельных фоновых сессий в одном проект-каталоге; баг конкретной версии 2.1.119).
- Что доказуемо из транскриптов, а что осталось гипотезой.
- Митигации, ранжированные по эффективности.

В конце (stdout) — кратко (8–14 строк): вердикт, статус гипотезы про не-ASCII, топ-2 альтернативы, главная митигация, путь к файлу. (Если этот финальный шаг упадёт с той же 400 — ничего страшного, файл уже записан.)
