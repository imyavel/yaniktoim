# Что надо исправить и сделать к проекту yaniktoim

Проект состоит из разнородных файлов docs/art/Art.html
И общего оглавления и списков разделов docs/index.html,
генерируемых скриптами batch_runner.

---

## ПРОГРЕСС (обновлено 2026-06-01)

- **Пункт 1 (сноски-grid):** ✅ корпус чист. Статический скан всех 350 статей:
  единственный узкий 2-колоночный grid сносок (239) уже с явным `grid-column`,
  255 больше не использует grid. Профилактику в промпте решили НЕ добавлять.
- **Пункт 2 (маркер ↔ URL / «текст сноски виден в зоне»):** ✅ исправлено.
  Истинная причина — **невалидные вложенные `<a>` в `<a>`** (popup со ссылкой
  внутри якоря-маркера, либо заголовок-самоссылка вокруг сноски); браузер
  выбрасывал popup/ссылку инлайном. Детерминированный фиксер
  `tools/fix_nested_anchors.py` расплёл **40 статей** (P/A1, P/A2, W). Итог:
  0 вложенных якорей в корпусе. Это же закрыло большинство «голых URL в потоке».
- **Пункт 3 (Алиса пропускает разделы):** ⏸️ ДАЛЬНЕЙШУЮ ПОДГОНКУ ПОД АЛИСУ
  ПРЕКРАТИЛИ. Диагноз: дистиллятор Яндекса берёт крупнейший текстовый блок как
  старт и отбрасывает то, что выше. На 237 пробовали: `<main>`, единый
  `<article>` + `section`→`div`, эпиграфы в `<p>`, перенос оглавления выше.
  Прогресс был (старт сместился «СТИХ ВТОРОЙ» → «Предваряющая молитва»), но
  полностью с H1/эпиграфов читать не заставили. Решили оставить как есть.
  (Эти структурные правки 237 закоммичены — они корректны и полезны сами по себе.)
- **Пункт 4 (ссылки):**
  - **4a корпус (proza→наш сайт):** ✅ сделано. `tools/relink_corpus.py` перевёл
    **313 ссылок в 113 статьях** на внутренние; вариантные названия согласованы
    (`tools/relink_review.md`). 0 остаточных корпусных proza-ссылок.
  - **4b Зоар → zohar-sulam:** ⬜ не начато.
  - **4c музыкальные блоки:** ⬜ не начато.
  - **4d url с `*kabbalah*`:** ⬜ не начато.
  - **19 не-корпусных proza** (авторские `agent017`/`viktor1717`, дневники) —
    ждут решения оператора.
- Остальные пункты (отступ заголовка, popup-сноски, SEO/goatcounter, поиск по
  сайту) — ⬜ не начаты.

---

## 1. Сноски-grid схлопываются в колонку по одному слову  (2026-05-30)

**Где:** статьи со сносками-цитатами, аппарат внизу. Пример: art/255.html
(«WOULD YOU DO IT WITH ME?»). Текст тела сноски ломался по 1 слову в столбик.

**Причина:** LLM верстает `.fn-item` как `display:grid; grid-template-columns:1.6em 1fr`,
но кладёт ВНУТРЬ ТРИ ребёнка — `.fn-back` (стрелка ↑), `.fn-num` (номер) и
`.fn-body` (текст) — на ДВЕ колонки, без явного `grid-column` у номера/тела.
Auto-flow: `.fn-back`→кол.1, `.fn-num`→кол.2 (широкая!), `.fn-body`→новый ряд
кол.1 (30px) → текст переносится по слову.

**Фикс (применён к 255):** закрепить раскладку явно —
```css
.fn-item .fn-num{grid-column:1; ...}
.fn-item .fn-body{grid-column:2;}
```
(у `.fn-back` `grid-column:1` уже стоял).

**Системность:** баг повторится на других статьях, где LLM кладёт и стрелку-
возврат, и номер, и тело в один grid без явного размещения. Промпт CSS сносок
не диктует (LLM делает сама) → дефект выборочный.

**Возможное лечение на будущее:** в convert_prompt добавить мягкое требование —
«аппарат сносок: номер/маркер и тело сноски не должны схлопываться; если
используешь grid/flex для строки сноски — явно закрепи колонку тела и дай ему
занять всю оставшуюся ширину». Либо постпроверка: парсить готовый HTML и
ловить ячейки тела сноски уже шириной (min-content vs ожидаемая).

## 2. Маркер сноски прилипает к сырому URL в тексте  (2026-05-30)

**Где:** art/384.html, абзац «…среди мира слов мертвых героев*1proza.ru/2023/03/09/980 в компании…».

**Симптомы (два дефекта в одном месте):**
1. **Маркер сноски `*1` слит со ссылкой** без отбивки/верхнего индекса —
   `героев*1proza.ru/...` читается как мусор. Маркер должен быть оформлен
   как надстрочная кликабельная ссылка-сноска, отделённая от соседнего текста.
2. **Голый URL как видимый анкор** — ссылка обёрнута в `<a href>`, но
   видимый текст = сам сырой адрес `proza.ru/2023/03/09/980`. По промпту
   («голые URL оборачивать в нормальные гиперссылки, без сырых адресов
   строкой») так быть не должно — нужен человекочитаемый анкор или хотя бы
   не торчащий сырой путь посреди прозы.

**Причина (гипотеза):** во входном тексте маркер сноски `*1` и URL стоят
вплотную (`героев*1 http://proza.ru/...`), LLM распознала `*1` как сноску,
но URL рядом превратила в ссылку с анкором=адрес и не вставила разделитель
между маркером и ссылкой.

**Системность:** повторится там, где авторский маркер-сноска (`*N`) соседствует
с голым URL. Не чинил пока (наблюдение). Лечение — уточнить в промпте
оформление `*N`-маркеров (надстрочные, кликабельные, с отбивкой) и анкоров
голых URL (не оставлять сырой путь видимым текстом).

## 3. Голосовой ассистент «Алиса» пропускает разделы при чтении  (2026-05-30)

**Симптом:** при чтении страницы вслух Яндекс-Алиса часто пропускает куски —
заголовки/названия разделов или целые фрагменты текста. На исходной proza.ru
Алиса читала ВСЁ подряд: от названия статьи и до конца, без пропусков.
Целевое поведение — то же: непрерывное чтение от H1 и далее весь корпус текста.

**Причина (гипотеза):** голосовые ассистенты вытаскивают «основной текст»
эвристикой по семантике разметки. Если LLM кладёт заголовки/части в
нестандартные/декоративные контейнеры (например `<div>` со стилем вместо
`<h2>`/`<p>`, или текст внутри `aside`/`figure`/details, или role-атрибуты,
помечающие блок как несущественный), парсер ридера их отбрасывает. Также
вредят: скрытые/служебные блоки (yanik-meta — это `<script>`, ОК), вынос
текста в `::before/::after`, табличная/grid-вёрстка кусков, секции с
`aria-hidden`/`hidden`/`role=complementary`.

**Системность:** зависит от того, как именно LLM разметила конкретную статью
→ дефект выборочный, но важный (доступность/UX чтения вслух).

**Лечение (на этап доводки промпта):** потребовать СЕМАНТИЧНУЮ разметку
основного потока — заголовки в `<h1..h3>`, абзацы в `<p>`, разделы статьи
линейно в основном `<article>`/потоке документа, в порядке чтения; не прятать
содержательный текст в декоративные/служебные/aria-hidden контейнеры, не
выносить значимый текст в CSS-псевдоэлементы. Проверка: прогнать страницу
через «режим чтения»/reader-mode браузера — что он показывает, то и прочитает
Алиса; всё содержимое от H1 до конца должно попадать в reader-view линейно.


## 4. Ссылки на наш корпус, на Зоар и на музыку

Ссылки в коде на наш корпус (Элиягу Бар Малей / Зеир Арихович Анпин / ЭБМ / ЗАА и т.п.) 
которые у нас адаптированы на сайте, но ссылаются на proza.ru в тексте - должны
быть переделаны на ссылки на этот сайт.
Неоднозначности все согласовать с оператором до переделки

Ссылки на Книгу Зоар / Зоар, которые есть в корпусе, должны быть адаптированы в
ссылки на https://imyavel.github.io/zohar-sulam/ (локальная копия в Heb\Translated\Site)
Названия могут не совпадать; искать мягким regex-ом, при неоднозначностях выяснить с
автором до переделки

Музыкальные блоки "Музыка под настроение" / "Песнь Ступеней" / "Рекомендуемая музыка"
в тексте: иногда ссылаются на m.youtube и похоже в этом случае не работают
(нужно проверить). Вообще попробовать как во фрейме музыкальном, "резолвить"
все вставки уже существующие на автомате, чтобы проверить, есть в них отклик 
в плеере, или его нет. У всех явно "проблемных" вставок ссылок, их проработать
(отдельный вариант - когда на сайте проигрывание разрешено, а во фреймах -
запрещено, это частный случай, в каждом из которых нужно убеждаться на фактах,
а не строить догадки). Все что возможно починить, чтобы проигрывалось на сайте -
починить. Если музыкальный "блок" есть - а ссылки нет - то информировать
оператора для вставки актуальной ссылки на youtube во фрейм.

Ссылки url в которых есть в адресе *kabbalah*, включая завернутые в сокращенные
ссылки, которые нужно автоматизированно развернуть и проверить, дать на проверку
оператору (не более 5 шт за раз)

## Отступ заголовка от первого абзаца

Проверить программно, везде ли есть в html отступ заголовка от первого абзаца текста 
(хотя бы 1 пустая строка). Список того что нет - выдать в url на проверку
оператору (не более 5 штук за раз)

## Удобство popup сносок

Проверить, где сноски popup реализованы так, чтобы при движении мышки в сторону
сноски и внутри нее, окошко не исчезало - чтобы если там есть ссылка, ею
можно было воспользоваться не нажимая на саму сноску и не переходя вниз

## Поисковая инденсация, оптимизация и goatcounter

Прикрутить к статьям и каталогу goatcounter, составить sitemap и произвести
поисковую оптимизацию (возможно, summary по краткому содержанию статей)

## Поиск по сайту

Сделать поиск по сайту, по аналогии с уже сделанным в репо zohar-sulam