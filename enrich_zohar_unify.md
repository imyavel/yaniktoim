# enrich_zohar_unify.md — унификация Зоар-ссылок корпуса yaniktoim (ФАЗА 2)

> **Статус: СПЕЦИФИКАЦИЯ, ожидает выполнения.** Заведена 2026-06-24. Продолжение
> `enrich_zohar.md` (Фаза 1 — инфраструктура — выполнена). Точка входа проекта — `PROJECT.md`.
> Этот файл самодостаточен для старта с ХОЛОДНОГО контекста. **Сейчас НЕ исполнять** —
> только зафиксировано задание (по прямому указанию оператора 2026-06-24).

---

## 0. Состояние на момент заведения (что уже сделано/в каком виде дерево)

- **`zohar-sulam` — ЗАПУШЕН В ЖИВ (commit `52e87fa`, 2026-06-24).** Берешит разделён на
  `bereshit-1`(49)/`bereshit-2`(53) «Берешит N (Вначале)»; на всех 15 866 § якоря `id="pNN"`;
  индексы-справочник; sitemap(1840)+pagefind. **Из дальнейшего разбора Зоар-репо ИСКЛЮЧЁН.**
- **`yaniktoim` — РАБОЧЕЕ ДЕРЕВО НЕ ЗАКОММИЧЕНО** (продолжаем тут). `git status` покажет:
  - `cms-revival/editor/render.js` (+docs/) — `resolveZohar` уже умеет `{глава|N|§}`/`{глава||§}`,
    якорь `#p§`, валидация; слаг допускает цифру (`[a-z0-9-]`).
  - `cms-revival/editor/data/zohar_index.json` (+docs/) — 53 главы, 1780 статей, карта `paras`
    (§→статья). human глав = «Берешит 1/2 (Вначале)».
  - `docs/zml/SPEC.md`+canon, `guide.html`+`guide.pdf`/`rukovodstvo.pdf`.
  - **29 ссылок в 14 статьях** доведены Фазой A до `{глава||§}` (по сноскам «п. §»), НО форма
    временная: **нужно добавить N → `{глава|N|§}`** (см. §1/§2), и рукописный дубль рядом ещё НЕ
    убран. Файлы: `09G 0BC 0BCA 0BH 12F 163 163A 17U 18AA 18T 217A 239 28MA 2C7`.
    Отчёт-черновик: `enrich_zohar_changes.md`(+`.html`).
  - Новые тулзы: `cms-revival/editor/{build_zohar_index,enrich_corpus_footnotes,md2report}.py`.
  - ⚠️ **На живом yaniktoim 0BH (старый, не запушен) ссылается на `bereshit/001|095.html` → 404.**
    Чинится первым же push'ем yaniktoim (в рабочем дереве 0BH уже на `bereshit-1/2`).
- Конвертер md→html для отчётов: `python cms-revival/editor/md2report.py <файл.md>`.
- Локальный preview: серверы `zml-preview`(8099, docs корпуса) и `zohar`(8776, zohar-sulam)
  в `~/.claude/launch.json` (preview MCP).

---

## 1. Цель (что должно стать)

**Унифицировать ВСЕ ссылки на Зоар в корпусе** (`docs/art/*.zml`) к ЕДИНОМУ виду — тег
**`{глава|N|§}`** (три поля: глава, **номер статьи СОХРАНЯЕМ**, §), а весь рукописный
текст «Книга Зоар / том N / название главы / название статьи / п. NN» **убрать** (его несёт
сам анкор тега). Охватить и места, где сейчас **ссылки нет вообще** (голый текст «См. также:
Зоар, …», `cite="…Зоар…"`, упоминания в теле).
> N (номер статьи) определяется по `zohar_index.paras[глава][§]`; в исходнике оставляем явно —
> анкор от формы не зависит («…|N|§» и «…||§» рендерятся одинаково), но оператор хочет N в zml.

Анкор тега **всегда** показывает §NN (НЕ «только если не первый § статьи» — это указание
отменено). Целевой вид надписи: **«Книга Зоар, <Название главы>, <Название статьи>, § NN»**.

---

## 2. Спецификация унификации (корзина A+B+D, БЕЗ замены текста цитат)

Пройти каждую `docs/art/*.zml`, для каждого упоминания Зоара:

1. **Привести к тегу `{глава|N|§}`** (с номером статьи). Если ссылка уже есть (`{глава|N}` —
   добавить §; `{глава||§}` из Фазы A — **добавить N**, N = `paras[глава][§]`). § взять из сноски
   «п. §» или определить по контексту. Если ссылки нет (голый текст/`cite=`) — **определить
   главу+§** из авторской цитаты («Заповедь девятая, п. 233» → `akdama` §233, N=29) →
   `{akdama|29|233}`.
2. **Убрать рукописный дубль**, который теперь несёт анкор: «Книга Зоар», «том N», «Предисловия»,
   название главы, название статьи, «п. §/§NN», «РАШБИ/Бааль Сулам/Перуш Сулам». 
3. **Оставить** связки и уникальное: «См.», «См. также:», «Цитата:», сами кавычки-цитаты
   «…»/«…», уточнения «со слов «…»», «там же», «(по Элияу)»-подобные пометки смысла.
4. **`cite=` эпиграфов/quote** (3 шт: `217A`,`2AT`,`34O` — формат `[zohar-URL|akdama/012]`):
   заменить внешнюю ссылку-путь на тег `{глава|N|§}`; рукописную атрибуцию убрать (несёт анкор).
5. Пустой/битый резолв (§ не нашёлся, глава неясна) — НЕ угадывать, вынести в отчёт оператору.

**ВНИМАНИЕ — несущие ссылки.** ~11 тегов сейчас «голые» (вся надпись держится на анкоре,
авторского текста рядом нет): буллеты `11UA` (`- {akdama|1}`), `165`/`34O` («См. также: {akdama|8}»),
`0B6`/`46L`/`591` (просто `{akdama|N}`). Их анкор НЕ сокращаем (вариант B отвергнут — сломались бы).
После унификации все становятся однотипными тегами — это ОК.

**Несовпадение названий.** У части статей авторское название ≠ нашему в индексе
(«Кто создал их» → у нас «МИ бара ЭЛЕ»; «Кто она» → «Кто эта»; «Ночь Невесты» → «В ночь невесты»).
После унификации читатель увидит **наш** канонический заголовок (из `zohar_index`). Принято.

---

## 3. render.js — формат анкора (РЕШЕНО 2026-06-24, см. §7 Q-A; render.js поправлен+проверен)

Сейчас `resolveZohar` рендерит: **«Книга Зоар. <kniga_human>. <artTitle>, §NN»** (точки-разделители,
`kniga_human` = КНИГА: «Введение (Акдама)»/«Берешит (Бытие)», `§NN` без пробела).

Оператор задал вид: **«Книга Зоар, <Название главы>, <Название статьи>, § NN»**. Расхождения —
решить и поправить `render.js` (+ `cms-revival` canon, синк в docs, перерендер `build_views`):
- разделитель: **запятые** вместо точек?
- «Название **главы**» = глава-параша (`chapter.human`, напр. «Акдама (Предисловие)»/«Ноах (Ной)»)
  ВМЕСТО книги? Сейчас показываем книгу. (`zohar_index.chapters[ch].human` — есть.)
- «**§ NN**» с пробелом после §?
- §NN показывать **всегда** (уже так; «условный §» — отменён).
> Зависит и `enrich_zohar.md §3.2` (хвост якоря «, §53»). Обновить SPEC+guide после решения.

---

## 4. Сверка переводов цитат (корзина C) → HTML-отчёт НА СОГЛАСОВАНИЕ

После унификации (§2) — пройти ВСЕ ссылки, которые **атрибутируют процитированный текст**
(подпись/эпиграф/сноска с «оригинальным» текстом Зоара рядом):
- открыть наш перевод по ссылке `zohar-sulam/<глава>/<NNN>.html#p<§>`, взять текст параграфа;
- сравнить с процитированным в zml;
- **совпадает** → ничего;
- **отличается** (автор привёл по Лайтману/иному переводу или пересказом) → **НЕ менять сам**,
  собрать в **HTML-отчёт на утверждение**: старый текст ↔ наш перевод (diff) + кликабельная
  ссылка на статью (localhost preview), по каждому случаю. Оператор утверждает → тогда заменить
  в zml на наш перевод (дословно), сохранив авторскую рамку (кавычки/`[epi]`/`[quote]`/атрибуцию-тег).
- Если цитата — **Писание** (Тора/Псалмы/…), а не Зоар → не трогать.
- **Правило целостности (CLAUDE.md):** работать только с реально полученным полным текстом
  параграфа; не удалось — остановиться и спросить.
- Отчёт — `md2report.py` → HTML, ссылки открываются в браузере.

---

## 5. Инвентарь (разведка 2026-06-24, для холодного старта)

Зоар-упоминания в `docs/art/*.zml` (формат → количество):
| формат | кол-во | где |
|---|---|---|
| тег `{глава\|N}` (статья) | 49 | сноски/тело; ~37 с дублем-текстом, ~11 «голых» |
| тег `{глава\|\|§}` (уже сделано Фазой A) | 29 | 14 файлов (список в §0) |
| `[zohar-URL\|анкор-путь]` в `cite=` | 3 | эпиграфы `217A`,`2AT`,`34O` |
| голый `cite="…Зоар…"` без ссылки | 19 | эпиграфы/quote (06I,127B,12MA,15R,18MA,1CIA,…) |
| текстовые упоминания Зоар/Сулам/Ашлаг без ссылки | ~110 файлов | см. ворклист B в `enrich_zohar.md` |

Литеральных `{|}` (весь Зоар) и `{глава|}` (вся глава) в корпусе **нет**.

Воспроизвести инвентарь:
```
# теги: классификация dup/bare
python -X utf8 -c "import re,glob;from pathlib import Path;TAG=re.compile(r'\{([a-z][a-z0-9-]*)?\|(\d*)(?:\|(\d*))?\}');[...]"
# (полные сканеры — в истории сессии 2026-06-24; ключевые маркеры дубля:
#  Зоар|Сулам|РАШБИ|Ашлаг|Предислови|Бааль|параграф|п\.\s*\d|§\s*\d|том\s*\d)
```
Ворклисты id статей — в `enrich_zohar.md` «Приложение» (A: 41 со ссылками; B: 110 с упоминаниями).

---

## 6. Метод и порядок

1. Согласовать §3 (формат анкора) и §7 (открытые вопросы) с оператором.
2. Поправить `render.js` под согласованный формат → перерендер, тест resolveZohar, фикстуры.
3. Унификация §2: пофайлово, **headless `claude -p` (Opus 4.8, без явного effort)**, доступ к
   `zohar-sulam` + `zohar_index.json`. Пилот 3–5 статей → показать оператору → масса.
4. Сверка §4: собрать HTML-отчёт расхождений → согласование → применить утверждённое.
5. Приёмка (как `enrich_zohar.md §8`): preview + отчёт. `push.bat` (3 репо) — после зелёной приёмки.
6. zohar-sulam уже в живом — отдельный push НЕ нужен (если его не трогаем).

---

## 7. Открытые вопросы оператору — РЕШЕНЫ 2026-06-24

- **Q-A. Формат анкора → РЕШЕНО.** Вид: **«Книга Зоар, глава ‹chapter.human›, ‹artTitle›, § NN»**
  (запятые; явное слово **«глава»**; **глава-параша** вместо книги; «**§ NN**» с пробелом).
  `render.js` поправлен и проверен (все формы тега ✓). Пример: «Книга Зоар, глава Ноах (Ной), Ноах и ковчег, § 53».
  - **Исключение `akdama`** (решение оператора 2026-06-24): метка главы — просто **«Предисловие»**
    (без «глава Акдама (Предисловие)»). Пример: «Книга Зоар, Предисловие, Погонщик ослов, § 89».
    Прочие главы — как обычно «глава ‹human›».
  - **§ показывается ТОЛЬКО при явном параграфе** (финальное решение оператора 2026-06-24, отменяет
    промежуточное «§ всегда»). `{глава|N|§}`/`{глава||§}` → «…, § NN»; `{глава|N}` (ссылка на статью
    целиком) → **без** «§ NN»; `{глава||}`/`{глава|}` → «Книга Зоар, глава ‹глава›» без §.
  - **`#p`-якорь — только при ЯВНОМ §**. `{глава|N}` ведёт на верх страницы статьи без якоря.
- **Q-B. → РЕШЕНО: убрать** пометки «по РАШБИ / по Бааль Сулам / по Перуш Сулам» везде. НО:
  каждое снятие — **внизу отчёта-на-согласование списком «было → стало»** (до правок в yaniktoim).
- **Q-C. → РЕШЕНО.** Где глава+§ однозначны — ставить тег `{глава|N|§}`. Где не определяется
  однозначно — **не угадывать**, выносить в тот же отчёт на решение оператора.
- **Q-D. → РЕШЕНО (правило §2.3).** Связки «См. также:» и т.п. — остаются **авторским текстом**;
  выписанные автором названия глав/книг/статей — **сворачиваются в тег**. (`11U`: «См. также:»
  остаётся, «Роза/Заповедь девятая/Заповедь десятая» → `{akdama|N|§}`.)

## 8. Инфраструктурный чек-лист (выкат 2026-06-24)
- [x] `render.js` — формат анкора; § и `#p`-якорь ТОЛЬКО при явном §; `{глава|N}` без §; akdama→«Предисловие» (проверено).
- [x] **SPEC** — «якоря-текст» обновлён под новый формат в обеих копиях (`cms-revival/zml3/SPEC.md`, `docs/zml/SPEC.md`).
- [x] **guide.html + rukovodstvo.pdf** — примеры анкора обновлены под новый формат (+пометка «§ только при явном §; akdama→Предисловие»); PDF пересобран (44 стр.) → `docs/guide/rukovodstvo.pdf`.
- [x] **применена унификация** — 126 правок (118 структурных + 8 safe-TEXT) в `.zml`; 15 прозовых-TEXT и сверка цитат ОТЛОЖЕНЫ.
- [x] **перерендер корпуса** `build_views.mjs` — 143 art/html, 0 битых анкоров; синкнут `docs/editor/render.js`.
- [ ] **push** yaniktoim (reindex + commit + push) — выполняется.
- Отложено отдельными проходами: прозовый TEXT-класс (15, `enrich_unify` §4-worklist), сверка/замена цитат (корзина C, `cite_compare.py`/`cite_rows.json`).
