# Добавление статьи «Роза Мира Даниила Андреева — основные идеи»

Спец-процедура (разовая). В отличие от `add_art.md` (перекодирование одной статьи
с proza.ru) здесь источник — книга Д. Андреева «Роза Мира» (12 томов). Контент
готовится **пошаговым вызовом субагентов по каждому тому** (двупроходно по главам),
затем я собираю **одну** самодостаточную HTML-страницу и публикую её в раздел
`other` («Без категории»). Файл — живой чеклист: галки проставляются по мере
выполнения; раздел «Восстановление» внизу позволяет продолжить с чистого контекста.

> Дата документа: старт 2026-06-06. Модель-исполнитель: Claude Opus 4.8 (это же
> пишет и финальную страницу). Все правки версионных файлов — копией `_NNN+1`
> (CLAUDE.md). Перед правкой `manifest.json` — `cp manifest.json manifest.bak.json`.

> **ВНИМАНИЕ (изоляция агентов):** на время проекта файл `C:\Users\admin\CLAUDE.md`
> ПЕРЕИМЕНОВАН в `C:\Users\admin\CLAUDE.md.bak`. Проверено: `--system-prompt` и
> `--bare` НЕ годятся (первый не убирает CLAUDE.md в claude-code 2.1.165; второй
> ломает OAuth). Единственный рабочий способ убрать правила CLAUDE.md из headless-
> агентов — отсутствие файла на диске. **По завершении (после E4) вернуть:
> `mv C:\Users\admin\CLAUDE.md.bak C:\Users\admin\CLAUDE.md`.** Если процедура
> оборвётся — оператор вернёт файл вручную.

---

## Зафиксированные решения (из обсуждения с оператором)

- **Глубина**: промежуточная, ~1.8–2.2 тыс. слов на том (~4–5 PDF-стр.). Итог
  статьи ~24 тыс. слов / ~50–60 стр.
- **Формат**: ОДИН HTML `docs/art/666A.html` (самодостаточная страница), не дробить.
- **Стиль**: естественный, без стилистических ухищрений. Сверху 1–2 абзаца:
  что за источник, какая была задача, какая это модель, какова структура текста.
- **Структура каждого тома**: Главные идеи / Модели и концепции / Ключевые термины
  / Выводы и акценты автора.
- **Глоссарий**: отдельный раздел в конце статьи (термины Андреева) + он же даётся
  субагентам как общий словарь для единообразия.
- **Обложка**: НЕТ (`img: null`).
- **Effort**: `high` на всех субагентов. (NB: CLI поддерживает и `xhigh`/`max` —
  выше `high`; оставлено `high` по решению оператора. Главный рычаг качества —
  двупроход по главам, а не уровень эффорта.)
- **Метод**: двупроход по главам для ВСЕХ 12 томов (проход 1 — заметки по главам,
  проход 2 — сборка фрагмента тома).
- **Публикация**: одним пушем в конце, когда готовы все 12 томов.
- **Юр.рамка**: Андреев ум. 1959, в РФ под охраной до 2029. Это изложение идей
  (производное); на странице — атрибуция, формулировка «изложение/конспект идей»,
  ссылка на источник; CC — только на наш текст.

## Идентификаторы (зарезолвлено)

- `art = roza_mira`  (по решению оператора — собственное имя вместо кода 666A)
- `number = 352`  (текущий total 351 + 1)
- `section = other`  («Без категории»), `section_order = 27`  (для листинга в индексе)
- `stem = 2026_06_06_roza_mira`
- файл статьи: `docs/art/roza_mira.html`; картинки нет
- **STANDALONE:** страница НЕ участвует в prev/next (пейджера на ней нет; сосед 217A
  остаётся замыкающим). Если когда-нибудь перегенеришь nav legacy-пайплайном —
  исключи этот art из prev/next.
- url-источник: `https://lib.rmvoz.ru/bigzal/rozamira`

## Готовые артефакты (вход)

- `C:\Users\admin\roza_mira\text\roza_mira.txt` — полный текст (526 стр., ~753k tok)
- `C:\Users\admin\roza_mira\text\vols\vol_01.txt … vol_12.txt` — тома (нарезаны)
- `C:\Users\admin\roza_mira\text\vols\glossary.txt` — «Краткий словарь» Андреева
- Токены по томам: 01 65k, 02 70k, 03 59k, 04 42k, 05 47k, 06 49k, 07 42k,
  08 40k, 09 47k, 10 82k, 11 100k, 12 101k.

## Рабочая папка процедуры

`C:\Users\admin\yaniktoim\.batch\roza\`
- `vol_NN_notes.md`  — выход прохода 1 (заметки по главам)
- `vol_NN.html`      — выход прохода 2 (готовый фрагмент тома)
- `glossary.html`    — собранный раздел-глоссарий
- `prompt_pass1.md`, `prompt_pass2.md` — тексты промптов (фиксируются на шаге 1)
- `_logs/` — stdout/json каждого headless-вызова

---

## Контракт вывода субагента (проход 2 → фрагмент тома)

Чистый HTML-фрагмент (без `<html>/<head>/<body>`), ровно такой каркас:

```html
<section class="vol" id="vol-NN">
  <h2>Книга NN. НАЗВАНИЕ</h2>
  <h3>Главные идеи</h3> …
  <h3>Модели и концепции</h3> …
  <h3>Ключевые термины</h3> …
  <h3>Выводы и акценты автора</h3> …
</section>
```
- Внутри — обычные `<p>`, `<ul><li>`, `<strong>`. Никаких инлайн-стилей и скриптов.
- Термины Андреева — как в глоссарии (единое написание).
- Объём текста секции: ~1.8–2.2 тыс. слов.

**Валидация фрагмента (контролёр = я):** есть все 4 `<h3>`; длина в диапазоне
(≈1500–2600 слов); нет `<html>`/`<script>`; нет обрыва; используются ключевые
термины. Не прошло → перезапуск тома (при «жидком» — проход 1 более дробно).

## Headless-вызов (Opus 4.8, effort high)

```bash
cd C:\Users\admin\yaniktoim
claude.exe -p \
  --model claude-opus-4-8 --effort high \
  --output-format json \
  --allowedTools Read,Write \
  --permission-mode bypassPermissions \
  --add-dir .batch/roza --add-dir C:\Users\admin\roza_mira\text\vols \
  --max-turns 30 \
  --append-system-prompt "<изоляция: ты адаптируешь том «Розы Мира», см. промпт>" \
  < .batch/roza/prompt_passX_volNN.md
```
с `CLAUDE_CODE_MAX_OUTPUT_TOKENS=64000`. Агент сам пишет файл-результат в
`.batch/roza/`. Источник промпта-шаблона — `prompt_pass1.md` / `prompt_pass2.md`
(+ подстановка пути к `vol_NN.txt`, `glossary.txt`, имени файла-результата).

---

# ПОСЛЕДОВАТЕЛЬНОСТЬ (чеклист)

## Этап A. Подготовка
- [x] A1. Текст скачан, нарезан на `vols/vol_01..12.txt` + `glossary.txt`
- [x] A2. art/number/section_order зарезолвлены (666A / 352 / other:27)
- [x] A3. `.batch/roza/roza_run.py` (драйвер + вшитые промпты pass1/pass2; глоссарий
        `vols/glossary.txt` подаётся субагентам). CLAUDE.md переименован → агенты чистые.
- [x] A4. `glossary.html` собран (40 терминов, gloss_gen.py).

## Этап B. Тома (двупроход, по одному; галки в таблице ниже)
Для каждого тома NN = 01..12:
- проход 1: вызвать субагента → `vol_NN_notes.md` (заметки по главам)
- проход 2: вызвать субагента (вход = notes + glossary) → `vol_NN.html`
- валидировать `vol_NN.html`; не ок → перезапуск

| Том | Название | pass1 | pass2 | валид. |
|----|----------|:--:|:--:|:--:|
| 01 | Роза Мира и её место в истории            | [x] | [x] | [x] |
| 02 | О методах познания                        | [x] | [x] | [x] |
| 03 | Структура Шаданакара. Миры восходящего ряда| [x] | [x] | [x] |
| 04 | Структура Шаданакара. Инфрафизика         | [x] | [x] | [x] |
| 05 | Структура Шаданакара. Стихиали            | [x] | [x] | [x] |
| 06 | Высшие миры Шаданакара                     | [x] | [x] | [x] |
| 07 | К метаистории Древней Руси                | [x] | [x] | [x] |
| 08 | К метаистории царства Московского         | [x] | [x] | [x] |
| 09 | К метаистории Петербургской империи       | [x] | [x] | [x] |
| 10 | К метаистории русской культуры            | [x] | [x] | [x] |
| 11 | К метаистории последнего столетия         | [x] | [x] | [x] |
| 12 | Возможности                               | [x] | [x] | [x] |

## Этап C. Сборка страницы
- [x] C1. Оболочка `docs/art/666A.html` собрана (assemble.py): вступление, оглавление,
        12 фрагментов, глоссарий, колофон-атрибуция, CSS из 217A. 185 КБ, ~20.5k слов.
- [x] C2. Навигация: межтомные ↑/←/→, крошки (Главная / Без категории). Пейджер
        prev/next НЕ ставим (standalone-страница).
- [x] C3. favicon-линки в `<head>`.
- [x] C4. Проверка пройдена: DOCTYPE…/html, 12 якорей `#vol-01..12` + `#glossary`,
        13×h2 / 48×h3, 1 json-script.

## Этап D. Интеграция в корпус
- [x] D1. `manifest.json` (бэкап `manifest.bak.json`): запись 666A добавлена (352).
- [x] D2. Швов prev/next НЕТ (standalone): `217A.html` оставлен замыкающим
        (ранее добавленная «следующая» откачена).
- [x] D3. `docs/other/index.html`: `<li>` 666A добавлен; «Статей: 28»; 3×meta «28 статей».
- [x] D4. `docs/index.html`: total 352 (4 вхожд.); «Без категории» 28 статей.
- [x] D5. Песни: музыки нет — пропущено.

## Этап E. Финализация и публикация
- [x] E1. `gen_descriptions --only art/666A.html` — описание (139 симв.) сгенерировано.
- [x] E2. `apply_seo.py` — SEO-теги вставлены (обновлена 1 страница).
- [x] E3. Pagefind переиндексирован + `docs/sitemap.xml` (372 стр., 666A есть).
        NB: `reindex.bat` заканчивается `pause` — под `cmd /c` зависает; шаги
        выполнены напрямую (`pagefind --site docs` + `tools/build_sitemap.py`).
- [ ] E4. **ОЖИДАЕТ «GO» ОПЕРАТОРА:** `push.bat add art roza_mira — Роза Мира Даниила
        Андреева — основные идеи`. После пуша вернуть CLAUDE.md:
        `mv C:\Users\admin\CLAUDE.md.bak C:\Users\admin\CLAUDE.md`.

### Поля записи manifest (D1)
```json
{
  "stem": "2026_06_06_roza_mira",
  "url": "https://lib.rmvoz.ru/bigzal/rozamira",
  "section": "other",
  "title": "Роза Мира Даниила Андреева — основные идеи",
  "html": "roza_mira.html",
  "img": null,
  "url_date": "2026-06-06",
  "url_nnn": null,
  "text_date": "1958",
  "date_chosen": "2026-06-06",
  "date_source": "manual",
  "number": "352",
  "section_order": 27,
  "art": "roza_mira"
}
```

---

## Восстановление с чистого контекста

1. Прочитать этот файл — галки показывают, что сделано.
2. Артефакты на диске:
   - тексты томов: `C:\Users\admin\roza_mira\text\vols\`
   - промежуточные результаты: `C:\Users\admin\yaniktoim\.batch\roza\vol_NN_notes.md`
     и `vol_NN.html` (наличие валидного `vol_NN.html` = том готов).
3. Проверить готовность томов:
   `ls .batch/roza/vol_*.html` — каких NN нет/невалидны, те перезапустить (Этап B).
4. Если есть `docs/art/666A.html` — сборка уже шла; сверить с C4 и продолжить с D.
5. Если оборвалось на интеграции: `git status` в `yaniktoim` покажет, что уже
   изменено; `manifest.bak.json` — откат записи. Идемпотентные шаги (apply_seo,
   reindex) безопасно повторять.
6. Публикация (`push.bat`) — последний шаг; до неё ничего на сайт не уходит.

## Смета (ориентир)
- Вызовов: 12×2 = 24 (+перезапуски). Вход суммарно ≈ текст всех томов один раз
  (~750k tok в проходах 1) + компактные входы проходов 2.
- Выход: заметки ~3–5k/том + фрагмент ~3k/том ⇒ ~80–110k tok.
- Время: последовательно, по одному тому; ориентир десятки минут.
</content>
