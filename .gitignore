__pycache__/
*.pyc
*.pyo
.DS_Store
Thumbs.db
node_modules/
tools/_parity/
_backups/
raw/
_research/
src/*.sqlite
# audit logs etc — keep ONLY the zohar index the build needs
_logs/*
!_logs/zohar_index.json
# old JSON intermediates (superseded by zml/ source-of-truth)
json/
manifest_pre_art.json
# batch_runner runtime artefacts — регенерируются, в репозитории не нужны
batch_runner/*.log
batch_runner/*.lock
batch_runner/*.stop
batch_runner/state.json
batch_runner/state_html.json
# локальные заметки по багам пайплайна
problems.md

# redate backups/intermediates — в репозитории не нужны
manifest_pre_redate.json
manifest_redated.json

# локальный пуш-скрипт оператора (в репо не нужен)
push.bat

SEO_HANDOFF.md

# .batch/roza run-выхлоп — регенерируется драйверами (assemble/gloss_gen/roza_*.py); в репо не нужен
.batch/roza/_logs/
.batch/roza/batch.log
.batch/roza/glossary.html
.batch/roza/preview.html
.batch/roza/vol_*.html
.batch/roza/vol_*_notes.md

# descriptions_full.* — отчёт-экспорт (html/json/pdf), «посторонний»; канон = descriptions.json
/descriptions_full.*

# cms-revival (внесён в репо 2026-06-21): тяжёлое/регенерируемое/мёртвое НЕ версионируем.
# node_modules/, __pycache__/, *.pyc уже покрыты глобально выше.
# recon/ — ~145M скриншотов разведки Ф1 (мёртвый артефакт). convert/out* — дубль docs/art (канон там).
cms-revival/worker/.wrangler/
cms-revival/recon/
cms-revival/convert/out/
cms-revival/convert/out_v1_canon/
cms-revival/convert/out_v2/
cms-revival/convert/audit_out/
cms-revival/guide_build/guide_raw.pdf
cms-revival/guide_build/guide.pdf
