# enrich_zohar — отчёт об изменениях (для приёмки)

> Сгенерирован 2026-06-24 по итогам автономного прохода по `enrich_zohar.md`
> (инфраструктура §2–§3, §6 + детерминированная часть корпуса §4–5/корзина A).
> Открой в preview, кликай по статьям и проверяй глазами. Сначала — **меняющие
> видимый текст**, потом — где **только добавлена/расширена `{}`-ссылка**.
>
> **Ссылки ведут на локальные preview-серверы** (открываются в браузере из VS Code):
> корпус — `http://localhost:8099` (yaniktoim/docs), Зоар — `http://localhost:8776`
> (zohar-sulam). Оба сейчас подняты; если перезапускал VS Code — скажи, подниму снова.

---

## A. Изменения, меняющие видимый ТЕКСТ (проверять первыми)

### A1. Соседний репозиторий `zohar-sulam` — разделение Берешита (§2.0)
Глава `bereshit` (102 статьи) разделена на две **обычные** главы:
- **`bereshit-1`** «Берешит 1 (Вначале)» — статьи 001–049 (номера те же), § 1–482.
- **`bereshit-2`** «Берешит 2 (Вначале)» — статьи 001–053 (бывшие 050–102, −49), § 1–448.

В перенесённых статьях законно меняются `<title>`/`<h1>`/хлебные крошки/canonical/
og/JSON-LD/нав/строка-меты (имя главы и номер). **Тело перевода — байт-в-байт прежнее**
(проверено). Старый путь `bereshit/*` исчез → 404 (принято планом, Q1).

### A2. `zohar-sulam` — новый стиль индексов (§2.4): из «трекера прогресса» в справочник
- Корневой `index.html`: «Содержит: **53 главы** · 1780 статей»; строки книг — «N глав(а/ы)»
  со склонением (1 глава · 13 глав · 12 глав · 10 глав · 7 глав).
- `kniga-bereshit/index.html`: «Содержит: **13 глав** · 474 статьи»; строки глав вида
  «`noach · 44 статьи`», две главы — Берешит 1 (Вначале) / Берешит 2 (Вначале).
- Индексы глав: шапка «§ 1 – NN», строки статей — «§ A – B» вместо «✅».

### A3. `zohar-sulam` — починка 3 «битых» статей (побочный фикс, текст меняется)
У трёх статей заголовок исторически утёк в тело (`# Статья N. …`), а `<title>/<h1>/og`
были пусты — из-за этого §-якорь не ставился и `{глава|N}` вёл на статью без заголовка.
Восстановлено детерминированно из самой утёкшей строки:
- [vayera/005](http://localhost:8776/vayera/005.html) — «Статья 5. Кто взойдёт на гору Господню» (§ 76–92; §76 снова размечен);
- [vayechi/021](http://localhost:8776/vayechi/021.html) — «Статья 21. Когда приходит время его уйти из мира»;
- [vayechi/082](http://localhost:8776/vayechi/082.html) — «Статья 82. Серебро не считалось во дни Шломо».

### A4. `zohar-sulam` — якоря параграфов (§2.1, невидимо в теле)
Каждому `<p class="para-start">NN)` добавлен `id="pNN"` (15 866 §). Видимая отрисовка
тела **не меняется** (только атрибут `id`); CSS `p.para-start{scroll-margin-top}`.

---

## B. Изменения, где добавлена/расширена `{}`-ссылка (текст не меняется)

Корзина A (детерминированная часть): существующие `{глава|N}` со сноской-цитатой
«…, **п. §**, …» доведены до каноничной формы `{глава||§}` (цитата по параграфу;
§ проверен по индексу — принадлежит именно статье N). **29 ссылок в 14 статьях.**

| Статья | Что изменилось (было → стало) |
|---|---|
| [09G — Моя Звезда](http://localhost:8099/art/09G.html) | `{akdama\|29}` → `{akdama\|\|236}` |
| [0BC — Маасер](http://localhost:8099/art/0BC.html) | `{akdama\|29}`→`{akdama\|\|233}` · `{akdama\|30}`→`{akdama\|\|237}` · `{akdama\|4}`→`{akdama\|\|13}` |
| [0BCA — Кто создал Их](http://localhost:8099/art/0BCA.html) | `{akdama\|4}`→`{akdama\|\|13}` · `{akdama\|12}`→`{akdama\|\|75}` |
| [0BH — Кто Она](http://localhost:8099/art/0BH.html) | `{akdama\|30}`→`{akdama\|\|237}` · `{akdama\|6}`→`{akdama\|\|23}` · `{bereshit\|1}`→`{bereshit-1\|\|1}` · `{bereshit\|95}`→`{bereshit-2\|\|366}` · `{akdama\|1}`→`{akdama\|\|1}` · `{akdama\|22}`→`{akdama\|\|198}` · `{akdama\|3}`→`{akdama\|\|7}` |
| [12F — Чем помогает Творец…](http://localhost:8099/art/12F.html) | `{akdama\|12}` → `{akdama\|\|91}` |
| [163 — Онтология единства Души 1](http://localhost:8099/art/163.html) | `{akdama\|29}`→`{akdama\|\|233}` · `{akdama\|3}`→`{akdama\|\|7}` |
| [163A — Онтология единства Души 2](http://localhost:8099/art/163A.html) | `{akdama\|29}`→`{akdama\|\|233}` · `{akdama\|3}`→`{akdama\|\|7}` |
| [17U — Часть 5. Точка Невозврата](http://localhost:8099/art/17U.html) | `{akdama\|29}` → `{akdama\|\|236}` |
| [18AA — Один день из жизни дельфинов](http://localhost:8099/art/18AA.html) | `{akdama\|12}` → `{akdama\|\|85}` |
| [18T — Ревность ангелов Творца…](http://localhost:8099/art/18T.html) | `{akdama\|11}` → `{akdama\|\|66}` |
| [217A — Коротко, но очень важно](http://localhost:8099/art/217A.html) | `{akdama\|17}` → `{akdama\|\|170}` |
| [239 — Мечтай!!](http://localhost:8099/art/239.html) | `{akdama\|14}`→`{akdama\|\|126}` · `{akdama\|12}`→`{akdama\|\|91}` |
| [28MA — Здесь стоит Трон Элиягу](http://localhost:8099/art/28MA.html) | `{akdama\|29}`→`{akdama\|\|236}` · `{akdama\|12}`→`{akdama\|\|91}` · `{akdama\|7}`→`{akdama\|\|40}` · `{akdama\|4}`→`{akdama\|\|11}` |
| [2C7 — ДНА - Мечта](http://localhost:8099/art/2C7.html) | `{akdama\|3}` → `{akdama\|\|7}` |

Якорь-текст ссылок теперь оканчивается на «, §N» и ведёт прямо на параграф
(напр. `…/akdama/006.html#p23` «Книга Зоар. Введение (Акдама). Буквы рава Хамнуны Старца, §23»).

---

## C. Движок / спека / руководство (§3, §6)

- `cms-revival/editor/render.js` — `resolveZohar`: третье поле `{глава|N|§}` / `{глава||§}`
  (статья по §), якорь `#p§`, валидация §↔статья. **Слаг главы допускает цифру**
  (`bereshit-1/2`) — отклонение от регэкспа плана §3.1, иначе новые главы не резолвились.
- Фикстуры teststand (5 тем) — добавлен §-пример, перерендерены.
- `cms-revival/zml3/SPEC.md` (+ копия `docs/zml/SPEC.md`) — §3 порядок парсинга и таблица.
- `cms-revival/guide_build/guide.html` + пересобран `guide.pdf` → `docs/guide/rukovodstvo.pdf`.
- `zohar_index.json` пересобран из репо zohar-sulam: 53 главы, 1780 статей, карта `paras`
  (§→статья) для `{глава||§}`.

---

## D. Что НЕ сделано — ждёт LLM-триажа (следующая фаза)

Открытая часть §4–5 — требует чтения контекста по каждому файлу (не детерминируется):
- **Корзина A, остаток:** ~49 ссылок `{akdama|N}` в 32 статьях БЕЗ явного «п. §» в сноске —
  многие, вероятно, законно остаются на уровне статьи (автор цитирует статью целиком).
- **Корзина B:** ~110 файлов упоминают Зоар/Сулам/Ашлаг текстом без ссылки — проставить `{глава||§}`.
- **Корзина C:** реальные цитаты Зоара без ссылки → ссылка + **замена чужого перевода на наш**
  (необратимо; правило целостности внешних текстов — работать только с полным исходником).
- **Корзина D:** где стоит общая `{глава|}`/`{|}`, а контекст конкретен — поднять до §.

Рекомендация: запускать пакетно через headless `claude -p`, пилот 3–5 статей,
корзина C — под присмотром (меняет авторский текст).
