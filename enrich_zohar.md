# enrich_zohar.md — обогащение Зоар-ссылок до уровня параграфа

> **Статус: ИНФРАСТРУКТУРА ВЫПОЛНЕНА (2026-06-24, автономный проход), ОЖИДАЕТ PUSH.**
> Точка входа проекта — `PROJECT.md`. Корпус Зоара — соседний репо `imyavel/zohar-sulam`.
>
> **Сделано и проверено (preview MCP + Node-тест resolveZohar):**
> - §2.0 split bereshit → `bereshit-1` (49) / `bereshit-2` (53); тело перевода байт-в-байт.
> - §2.1 якоря `id="pNN"` на всех 15 866 § (идемпотентно) + CSS `scroll-margin-top`.
> - §2.4 новый стиль индексов (root «53 главы», kniga-bereshit «13 глав · 474 статьи»,
>   §-диапазоны статей, склонения) — проверено в preview.
> - §2.2 `zohar_index.json` пересобран из репо: 53 главы, 1780 статей, карта `paras` (§→статья).
> - §3 `render.js`: `{глава|N|§}`/`{глава||§}`, валидация, якорь `#p§`. **Отклонение от плана:**
>   слаг главы в регэкспе расширен до `[a-z0-9-]` — иначе `bereshit-1/2` (с цифрой) не резолвились.
>   Фикстуры teststand перерендерены.
> - §6 SPEC (×2) + guide.html + `guide.pdf`/`rukovodstvo.pdf`.
> - **Побочный фикс:** 3 статьи zohar-sulam с утёкшим в тело заголовком (vayera/005,
>   vayechi/021, vayechi/082) — восстановлены title/h1/og + para-start §76 (иначе ломали
>   счёт статей и `{глава|N}`).
> - Deploy-предусловия zohar-sulam: `sitemap.xml` (1840 URL) + `pagefind` переиндексированы.
> - **§5 корпус, детерминированная часть (корзина A):** 29 ссылок в 14 статьях со сноской
>   «…, п. §, …» доведены до `{глава||§}` (включая миграцию 0BH). Отчёт — `enrich_zohar_changes.md`.
> - Тулинг: `zohar-sulam/tools/*.py` (split, anchors, restyle, sitemap),
>   `cms-revival/editor/{build_zohar_index,enrich_corpus_footnotes}.py`.
>
> **Осталось → вынесено в Фазу 2: `enrich_zohar_unify.md`** (унификация всех Зоар-упоминаний
> к тегу `{глава|N|§}` + сверка цитат с нашим переводом). `zohar-sulam` уже запушен в живое
> (`52e87fa`); yaniktoim-инфра в рабочем дереве, не запушена.
>
> Ниже — исходный план (как заводился), для контекста и остатка работы.

## 0. Суть

Расширить ZML-синтаксис Зоар-ссылки с `{глава|N}` (статья N) до **`{глава|N|параграф}`**
и **`{глава||параграф}`** (статью можно опустить). Зоар во всём мире цитируют по
**глава + параграф (§)**, а не по номеру нашего файла-статьи, поэтому `{глава||параграф}` —
естественная форма цитаты, а якорь ведёт точно на нужный параграф (§), а не на верх статьи.

Текущее (после расширения сохраняется): `{глава|N}` → статья, `{глава|}` → вся глава,
`{|}` → весь Зоар. Реализация: `cms-revival/editor/render.js` → `resolveZohar()`
([render.js:436](cms-revival/editor/render.js)), регэксп на [render.js:440](cms-revival/editor/render.js).

---

## 1. Установленные факты (разведка 2026-06-24)

- **zohar-sulam**: 52 главы-парашот (сгруппированы в книги `kniga-*` через
  `chapters[ch].kniga_slug`), **1780 статей**, **15865 параграфов**. Статья покрывает
  диапазон § (мета `Сулам §A–B`); каждый параграф — `<p class="para-start">NN) …</p>`.
- **Якорей параграфов сейчас НЕТ.** У `<p class="para-start">` нет `id` → сослаться на
  конкретный § нельзя. Это блокер №1 (см. §2.1). Генератор — `<p class="para-start">…`
  в `_archive_pipelines/Heb/Scripts/build_site.py:366`.
- **Неперекрытие параграфов — проверено по факту (парсинг всех 1780 HTML):**
  - **51 из 52 глав — чистые**: нумерация § строго монотонна по статьям, каждый § уникален
    в пределах главы → `{глава||параграф}` резолвится однозначно.
  - **Исключение ровно одно — `bereshit`**: слаг склеивает Берешит-1 и Берешит-2, нумерация
    рестартится с 1 на статье `050.html` (диапазон 1..482 встречается дважды, **448 дублей**).
    → `{bereshit||параграф}` **неоднозначен** → **РЕШЕНО: разделить на 2 главы** (см. §2.0).
  - Часть глав имеет **пропуски** в нумерации (несплошная, напр. `haazinu` 81 § но max=260;
    `vayera` max=513; `toldot` max=208; `chayei-sara` max=270) — на резолв НЕ влияет
    (§ всё равно уникален), но `{глава|N|параграф}`-валидатор должен это допускать.
- **zohar_index.json** (`cms-revival/editor/data/`, копия в `docs/editor/data/`):
  `chapters{human,kniga_slug,kniga_human,article_count}`, `articles{N:title}`, `_url_base`.
  **Данных о параграфах нет** — их надо добавить (см. §2.2).
- **render-цепочка**: `resolveZohar` идёт ДО `{term|…}/{leit|…}` ([render.js:382](cms-revival/editor/render.js)),
  поэтому третье поле не конфликтует с термин/лейтмотив-крючками (у тех после `|` нецифры).
- **Атрибут `cite=`** у `[epi]`/`[quote]` резолвит инлайн (SPEC §1.3a, [SPEC.md:374](cms-revival/zml3/SPEC.md)) —
  значит новый синтаксис обязан работать и внутри `cite=`. Отдельной правки не требует
  (тот же `resolveInline`), но проверить тестом.
- **Корпус yaniktoim** (`docs/art/*.zml`, 352 файла): **41 файл** уже со ссылками `{|}`
  (78 ссылок); **150 файлов** упоминают «Зоар/Сулам/Ашлаг» текстом → **~110 без ссылок**.

---

## 2. Что сделать в zohar-sulam (предусловие для §-ссылок)

### 2.0 Разделить `bereshit` на две главы (РЕШЕНО — делать ПЕРВЫМ)

**Почему:** слаг `bereshit` (102 статьи) склеивает два источника — `Bereshit` и `Bereshit-2`
(в пайплайне part 2 — отдельный docx `*-zohar-la-am-bereshit-2-*.docx`, парсер
`Scripts/parse_bereshit2_docx.py`); каталог парашот `Scripts/parasha.py` (`PARASHOT`) держит
«Bereshit» одной паршой, поэтому при сборке оба тома слиты под один слаг, и нумерация §
рестартится. Это единственная неоднозначная глава. Разделение убирает неоднозначность →
`{глава||§}` становится однозначным ВЕЗДЕ.

**Граница (проверено по факту):**
Обе части — **обычные главы** (симметрично, БЕЗ сохранения legacy-слага `bereshit`):
- **Часть 1** → слаг **`bereshit-1`**, имя **«Берешит 1»** (было «Берешит (В начале)»): статьи
  **001–049** (номера те же), §1–482 (последняя — «Не будет судить дух Мой человека», §477–482).
- **Часть 2** → слаг **`bereshit-2`**, имя **«Берешит 2»**: статьи **050–102** →
  **перенумеровать в 001–053** (новый N = старый N − 49); § свои, 1–448; первая — «Семь чертогов
  Ган Эдена» (§1–5).
- Книга обеих — `kniga-bereshit`. **Решение оператора (Q1, 2026-06-24): трактовать как обычные
  главы**, старую ссылочность `bereshit/*` не храним (ссылки yaniktoim правим сами, см. ниже).

**Правки в zohar-sulam (структура зашита в HTML — центрального structure.json в репо НЕТ):**
- `git mv bereshit/{001..049}.html → bereshit-1/{001..049}.html` (часть 1, номера те же) +
  `git mv bereshit/{050..102}.html → bereshit-2/{001..053}.html` (часть 2, −49). Старый каталог
  `bereshit/` исчезает целиком.
- В каждой перенесённой статье переписать: `<title>`/`<h1>` «Статья N», хлебные крошки
  (имя главы + «Статья N»), `<link rel=canonical>`, `og:url`, JSON-LD `BreadcrumbList`
  (position 3 = новая глава имя+url, position 4 = новый номер статьи), nav prev/next
  (нав — как у любой главы: каждая часть самостоятельна, межчастевой связки нет; у первой статьи
  «prev» нет либо ведёт на оглавление, у последней нет «next»).
- Сгенерировать `bereshit-1/index.html` (49 статей) и `bereshit-2/index.html` (53 статьи) —
  оглавления обеих глав в новом стиле (§2.4-C).
- `kniga-bereshit/index.html`: строку «Берешит (В начале)» заменить на ДВЕ — «Берешит 1»
  (`bereshit-1`) и «Берешит 2» (`bereshit-2`); счётчик глав 12 → **13** (статей 474 — без изменений).
- Корневой `index.html` (полное оглавление) — добавить `bereshit-2` в TOC книги Берешит.
- `pagefind` reindex + `sitemap` zohar-sulam.
- **Способ (РЕШЕНО, Q4): детерминированный скрипт-трансформ по репо HTML** (как §2.1) — `git mv`
  + правка затронутых файлов скриптом; исходный пайплайн НЕ воскрешаем.
- **NB URL:** весь старый путь `bereshit/*` исчезает → 404 (GitHub Pages без редиректов); legacy
  намеренно не храним (Q1). Ссылки корпуса чиним сами (ниже); внешние входящие на `bereshit/*` сломаются (принято).

**Правки в yaniktoim:**
- **`docs/art/0BH.zml`** — единственный затронутый файл (проверено grep), обе ссылки мигрируют:
  `{bereshit|1}` → **`{bereshit-1|1}`**; `{bereshit|95}` → **`{bereshit-2|46}`** (95 − 49 = 46).
- `zohar_index.json` пересобирается из НОВОГО HTML (§2.2) → появятся главы `bereshit-1` (49) и
  `bereshit-2` (53), старого `bereshit` не будет. Делать **после** split.
- SPEC/guide примеры на `achrei-mot`, bereshit не используют — правок не требуют.

**Приёмка split:** входит в §8.2 (идентичность остального корпуса + переходы по новым URL).

### 2.1 Якоря параграфов `#pNN`
- **Схема (РЕШЕНО, Q2):** `id="p53"` → URL `…/achrei-mot/007.html#p53` (ASCII, безопасно в URL).
- **Способ (РЕШЕНО, Q4 — без блокеров): детерминированный скрипт-трансформ по репо HTML.**
  Пройти `zohar-sulam/**/[0-9][0-9][0-9].html`, каждому `<p class="para-start">NN)` добавить
  `id="pNN"`. Идемпотентно (есть `id` → скип). Самодостаточно — пайплайн воскрешать НЕ нужно.
- **Источник-генератор НЕ трогаем (Q4):** `build_site.py` / `zohar-translator` не воскрешаем;
  при будущем пополнении zohar-sulam — повторный прогон скрипт-трансформа (идемпотентен).
- **CSS:** добавить `p.para-start{scroll-margin-top:…}` в `zohar-sulam/style.css`, иначе
  якорный § прячется под `header.breadcrumbs`.
- **Деплой:** zohar-sulam — отдельный репо, входит в общий `imyavel/push.bat`.

### 2.2 Индекс параграф→статья (для `{глава||параграф}` и валидации)
- Расширить `zohar_index.json`: на главу добавить карту `paras: {"53":"7", …}` (§ → номер статьи)
  **или** диапазоны `art_ranges: {"7":[53,56], …}`. Строить парсингом `para-start`-номеров
  из HTML zohar-sulam.
- **Сборщик:** база — `legacy/src/build_zohar_index.py` (строил chapters/articles по старому
  layout). Написать/дополнить так, чтобы он ЕЩЁ и парсил § из рендеренного HTML и писал карту
  `paras`. Выход → `cms-revival/editor/data/zohar_index.json`; `build_views.mjs:38` синкает в `docs/`.
- Пересобирать индекс при каждом пополнении корпуса Зоара (добавить в регламент сборки zohar-sulam).

### 2.3 Неоднозначность `bereshit` — снимается разделением (см. §2.0)
После split (§2.0) каждая глава имеет уникальные §, неоднозначных глав не остаётся — особого
случая в `render.js` не требуется.

### 2.4 Новый стиль отображения индексов zohar-sulam (убрать прогресс-семантику)

Сайт переводим из «трекера прогресса перевода» (✅ завершено / X/X) в **справочник**: слаги +
§-диапазоны + «Содержит». Правки **видимые и намеренные** (меняют смысл §8.2 — см. там).
Данные §-диапазонов берём из `<p class="para-start">`-номеров / меты статьи «Сулам §A–B» —
тем же проходом, что питает индекс `paras` (§2.2).

**A. Корневой `index.html` (Книга Зоар):**
- Шапка: «Прогресс: 52/52 главы · 1780/1780 статей» → **«Содержит: 53 главы · 1780 статей»**
  (после split 52→53).
- Правая колонка строк-книг: «12/12 главы» → **«N глав(а/ы)»** со склонением: Введение «1 глава»,
  Берешит «13 глав» (12→13 после split), Шмот «12 глав», Ваикра «10 глав», Бамидбар «10 глав»,
  Дварим «7 глав».

**B. Индекс книги `kniga-*/index.html` (напр. «Берешит (Бытие)»):**
- Правая колонка каждой строки-главы: вместо «✅ завершено» — **«<слаг> · N статей»**: латинский
  слаг (как в URL и в теге `{слаг||§}`) + число статей главы со склонением. Примеры:
  `noach · 44 статьи`, `bereshit-1 · 49 статей`, `bereshit-2 · 53 статьи`, `lech-lecha · 35 статей`.
- Шапка «Главы: 12/12 · Статей: 474/474» → **«Содержит: 13 глав · 474 статьи»** (склонение;
  подтверждено оператором 2026-06-24).

**C. Индекс главы `<слаг>/index.html` (список статей):**
- Шапка, где «Прогресс: 102/102 статей · ✅ Завершена» → **«§ 1 – NN»**, NN = последний параграф
  главы (`bereshit-1` → «§ 1 – 482», `bereshit-2` → «§ 1 – 448», `achrei-mot` → «§ 1 – 413»).
  Префикс «Книга: <…>» сохранить.
- Правая колонка каждой строки-статьи: вместо «✅» — **«§ A – B»** = диапазон параграфов статьи
  (из меты «Сулам §A–B», уже есть; напр. статья 7 → «§ 53 – 56»).

**Склонение после числительного** (правило, применять к «глава» и «статья» везде):
последняя цифра 1 и не 11 → «глава/статья»; 2–4 и не 12–14 → «главы/статьи»; иначе → «глав/статей».
Контроль: 1 глава · 12 глав · 13 глав · 53 главы · 474 статьи · 1780 статей. (Память о согласовании
числительных — обобщить и на «глава».)

**Реализация (РЕШЕНО, Q4):** скрипт-трансформ по репо HTML, одним проходом со split/якорями
(§2.0–2.2); исходный пайплайн НЕ воскрешаем.

---

## 3. Движок yaniktoim — `render.js`

### 3.1 Регэксп (третье необязательное поле)
```
старый: /\{([a-z][a-z-]*)?\|(\d*)\}/g
новый:  /\{([a-z][a-z-]*)?\|(\d*)(?:\|(\d*))?\}/g
```
Проверка совместимости (все кейсы держатся): `{|}`, `{ch|}`, `{ch|7}` — как раньше;
`{ch|7|53}` и `{ch||53}` — новые; `{term|текст}`/`{leit|текст}` — НЕ матчатся (после `|`
нецифры, нет закрывающей `}` на нужной позиции) → уходят в свой пасс. Пустой `para` (`{ch|7|}`)
трактовать как отсутствие параграфа (== `{ch|7}`). `{||53}` (нет главы) → broken.

### 3.2 Логика `resolveZohar`
- Распарсить `(ch, num, para)`.
- `{ch|N|para}`: href `${base}/${ch}/${pad3(N)}.html#p${para}`; (опц.) валидировать `para`
  в диапазоне статьи N по индексу — иначе broken. Якорь-текст: `Книга Зоар. <kniga>. <title>, §<para>`.
- `{ch||para}`: найти статью по `paras[ch][para]`; нашлась → как выше; не нашлась → broken.
  (После split §2.0 неоднозначных глав нет; страховка: если индекс маппит § на >1 статью → broken/WARN.)
- `{ch|N}`, `{ch|}`, `{|}` — без изменений.
- Хвост якоря-текста (РЕШЕНО, Q3): **«, §53»** — через запятую после заголовка.
- Обновить фикстуры: `cms-revival/zml3/teststand/`, демо-галерею `themes/gallery/*`.

---

## 4. Корпус — обогащение (survey + корзины)

**Инвентарь** (`docs/art/*.zml`): 41 файл со ссылками (78 ссылок) · ~110 файлов с упоминанием
Зоара без ссылок. Разбивка строк-кандидатов (упоминание Зоар/Сулам/Ашлаг ИЛИ глава-ссылка):

| где / тип | строк | со ссылкой | без ссылки |
|---|---|---|---|
| сноска + кавычки (цитата) | 108 | 21 | 87 |
| сноска без кавычек (отсылка) | 93 | 41 | 52 |
| тело + кавычки (цитата) | 219 | 1 | 218 |
| тело без кавычек (отсылка) | 96 | 9 | 87 |

> ⚠ **Числа — ВЕРХНЯЯ ГРАНИЦА, кандидаты под LLM-триаж**, не точный счёт: «Сулам» — ещё и
> титул Ашлага (Бааль Сулам), а кавычки «…» в теле часто обрамляют цитату Писания, а не Зоара.
> Точная классификация каждой ссылки — это и есть работа, которую план откладывает на LLM.

### Корзины действий
- **A. Расширить существующие ссылки → добавить параграф.** 41 файл / 78 ссылок.
  Каждую `{глава|N}` довести до `{глава|N|параграф}` (или `{глава||параграф}`). LLM читает
  цитируемую статью Зоара и определяет, к какому § реально относится окружающий текст.
- **B. Добавить недостающие ссылки (отсылки без цитаты).** Строки `*_ref` без ссылки (~139):
  «как сказано в Зоаре, Ахарей Мот…» → проставить `{глава||параграф}` или `{глава|}`.
- **C. Реальные цитаты Зоара без ссылки → ССЫЛКА + ЗАМЕНА на наш перевод.** Строки `*_quote`
  без ссылки (~305 кандидатов, тяжёлый LLM-триаж). Для подлинных цитат Зоара — см. §5.
- **D. «Обогатить».** Где стоит общая `{глава|}`/`{|}`, а контекст конкретен — поднять до
  конкретного §.

**Метод:** пофайловый прогон headless `claude.exe -p` (Opus 4.8, без явного effort → adaptive),
с доступом к корпусу `zohar-sulam` + `zohar_index.json` для резолва главы/§. zml в `docs/art/`
**не версионированы** (нет `_NNN` в имени) → правка штатная (Edit / редактор / Worker), без копий.

---

## 5. Замена цитаты на наш перевод (корзина C) — процедура LLM

Для каждой строки-кандидата с реальной цитатой Зоара (НЕ Писания):
1. **Опознать источник**: какая глава + § Зоара цитируется (по смыслу/ключевым словам).
2. **Найти наш текст**: открыть соответствующий `zohar-sulam/<глава>/<NNN>.html#p<§>` и взять
   НАШ перевод (Перуш ха-Сулам) этого параграфа.
3. **Сверить**: если автор привёл цитату по ДРУГОМУ переводу (Лайтман/иной) или пересказом —
   **заменить процитированный фрагмент в zml на наш перевод** (дословно из zohar-sulam),
   сохранив авторскую рамку (кавычки, `[quote]`/`[epi]`, атрибуцию).
4. **Проставить ссылку** `{глава||§}` (или `cite=` у блока `[epi]`/`[quote]`).
5. Если цитата — Писание (Тора/Псалмы/…), а не Зоар → НЕ трогать, ссылку Зоара не ставить.

> Соблюдать правило целостности внешних текстов (CLAUDE.md): работать только с реально
> полученным полным текстом нашего параграфа; если не удалось получить — остановиться и спросить.

---

## 6. Спека ZML3 + руководство (отразить формат)

- **`cms-revival/zml3/SPEC.md`**: порядок парсинга [SPEC.md:367](cms-revival/zml3/SPEC.md) и
  строку таблицы `{chapter|N}` [SPEC.md:374](cms-revival/zml3/SPEC.md) — дописать третье поле
  `{глава|N|параграф}` и `{глава||параграф}` (+ якорь `#p§`).
- **Публичная копия** `docs/zml/SPEC.md` — скопировать вручную (PROJECT.md: ручной синк спеки).
- **Руководство** `cms-revival/guide_build/guide.html`: строки **536** (вводный список),
  **544–558** (раздел «Ссылка на Зоар»), **902** (шпаргалка). Пересборка:
  `python cms-revival/guide_build/build_pdf.py` → `guide.pdf` → `cp guide.pdf docs/guide/rukovodstvo.pdf`.

---

## 7. Порядок выполнения (зависимости)

1. **zohar-sulam: разделить `bereshit`** (§2.0) — ПЕРВЫМ: меняет слаги/нумерацию, на этом строится индекс.
2. **zohar-sulam: якоря `#pNN` (§2.1) + новый стиль индексов (§2.4) + индекс `paras` (§2.2)** —
   одним проходом по HTML/генератору; без якорей/индекса §-ссылки некуда вести.
3. **render.js** (§3) + тест на 2–3 статьях.
4. **SPEC + guide** (§6).
5. **Корпус** (§4–5): сперва миграция `0BH` (`{bereshit|1}`→`{bereshit-1|1}`, `{bereshit|95}`→`{bereshit-2|46}`); затем корзина A,
   затем B, затем C (LLM-замена), затем D. Пакетно через headless, пилот 3–5 статей перед массой.
6. **Приёмка перед заливкой (§8, обязательно):** локальный preview + md-отчёт + самопроверка
   идентичности zohar-sulam через preview MCP.
7. Деплой: zohar-sulam и yaniktoim — через `imyavel/push.bat`; reindex перед push.

---

## 8. Приёмка перед итоговой заливкой (обязательно)

Выполняется ПОСЛЕ всех правок (§§2–6), ДО `push.bat`.

### 8.1 Локальный preview + md-отчёт об изменениях (для оператора)
- Пересобрать затронутые статьи (`node cms-revival/editor/build_views.mjs <id…>`) и поднять
  локальный preview-сервер корпуса.
- Сгенерировать отчёт **`enrich_zohar_changes.md`** — список ВСЕХ правок корпуса, строго
  **по приоритету**:
  1. **сначала** правки, **меняющие видимый ТЕКСТ ссылок/цитат** — корзина C (замена чужой
     цитаты на наш перевод) и любые места, где изменилась формулировка/анкор-текст;
  2. **потом** все прочие, где **только добавлена/расширена `{}`-ссылка** без смены текста
     (корзины A/B/D: `{ch|N}`→`{ch|N|§}`, новые `{ch||§}`).
- В каждой записи — **кликабельная (в preview) ссылка на ЛОКАЛЬНЫЙ файл статьи**: markdown-ссылка
  с относительным href на `docs/art/<id>.html` (и при необходимости на исходник `docs/art/<id>.zml`).
  Формат: `[<id> — <заголовок>](docs/art/<id>.html)`. Группировать по корзинам; внутри —
  по приоритету выше. Для каждой записи указывать суть изменения: для A/B/D — было `{ch|N}` →
  стало `{ch|N|§}` (или новая `{ch||§}`); для C — старый текст цитаты → наш перевод (diff).
- Цель: оператор открывает `enrich_zohar_changes.md` в preview, кликает по статьям и проверяет
  правки глазами, начиная с самых «текстовых».

### 8.2 Проверка zohar-sulam после правок (через preview MCP) — обязательна
Две зоны, проверяются раздельно (граница чёткая):

**(1) Невидимость — ТОЛЬКО в ТЕЛЕ статей** (ссылочная нумерация параграфов). Добавление `id="pNN"`
к `<p class="para-start">` + CSS `scroll-margin-top` НЕ должно менять видимую отрисовку тела:
- `git diff` по `<article>` любой статьи = только атрибуты `id`; текст/разметка перевода неизменны;
- спот-чек preview 2–3 статей из разных книг — тело перевода визуально идентично до/после.

**(2) Намеренно видимое — ТОЛЬКО списки разделов / оглавления** (новый стиль §2.4 + split §2.0):
через `preview_screenshot`/`preview_snapshot` проверить ВЕРНУЮ отрисовку (не идентичность):
- корневой `index.html`: «Содержит: 53 главы · 1780 статей» + склонения в строках книг;
- индекс книги `kniga-bereshit`: «Содержит: 13 глав · 474 статьи», строки вида «<слаг> · N статей»
  (`noach · 44 статьи`), две главы — Берешит 1 / Берешит 2;
- индекс главы `bereshit-2`: «§ 1 – 448», §-диапазоны в строках, нумерация 1–53; `bereshit-1`: 49 статей, «§ 1 – 482»;
- обычная глава `achrei-mot`: §-диапазоны статей (статья 7 → «§ 53 – 56»);
- **склонения глазами:** «13 глав», «53 главы», «44 статьи», «474 статьи», «1780 статей».

**(3) Навигация/якоря:** `…/achrei-mot/007.html#p53` скроллит к § (не спрятан под `header`);
`bereshit-1` и `bereshit-2` — самостоятельные главы (нав внутри каждой); весь старый `bereshit/*` → 404 (ожидаемо).
Перенесённые статьи законно меняют свой title/крошки/нумерацию (это split, не тело перевода).

Перед завершением остановить preview-серверы (PROJECT.md/CLAUDE.md). Зелёная приёмка → `push.bat`.

---

## 9. Решения (зафиксированы 2026-06-24) — открытых блокеров нет

- **Q1.** ✅ Split на **две обычные главы** — слаги `bereshit-1` / `bereshit-2`, имена «Берешит 1»
  / «Берешит 2»; legacy-ссылочность `bereshit/*` не храним (ссылки yaniktoim правим сами);
  нав — как у любой главы.
- **Q2.** ✅ Схема якоря — `id="p53"` (ASCII, безопасно в URL). См. §2.1.
- **Q3.** ✅ Хвост якоря-текста — **«, §53»** (через запятую). См. §3.2.
- **Q4.** ✅ Способ правок zohar-sulam — **детерминированный скрипт-трансформ по репо HTML**;
  исходный пайплайн НЕ воскрешаем. См. §2.0/§2.1/§2.4.
- **Q5.** ✅ Шапку индекса книги привести к «Содержит: 13 глав · 474 статьи» (как корневой). См. §2.4-B.

**План автономен:** все развилки закрыты, реализация zohar-sulam не зависит от внешнего пайплайна
(скрипт-трансформ по готовому HTML). Можно исполнять по §7 без блокеров.

---

## Приложение — ворклисты (id статей yaniktoim)

**A. Со ссылками `{|}` (41) — расширять до §:**
`05D 09G 0B6 0BC 0BCA 0BH 0CA 11U 11UA 12F 12L 12P 163 163A 165 16D 17U 18A 18AA 18T 19UA 1AA 1B5 1BF 1BH 1CF 217A 239 24I 255 28MA 2C7 341A 34O 462 46L 47H 47O 591 64P 666`

**B+C+D. Упоминают Зоар/Сулам/Ашлаг без ссылки (110) — добавлять/заменять/обогащать:**
`06I 071 075 07I 09S 0AC 0CP 11H 11HA 11T 127B 12FA 12M 12MA 12PA 138 138A 14I 14R 14U 151 152 158 158A 15FB 15J 15R 16E 16F 16J 16P 179 17BA 17D 17DA 17DB 17M 17Q 182 184 185A 186 189 18H 18I 18MA 191 19E 19N 19U 1AD 1AS 1B3 1BI 1BJA 1CB 1CIA 1CM 212 21K 227A 22F 22P 22R 237 23T 23TA 243 246 24H 253 255A 25G 266 26E 271 27GA 28M 2A9 2AE 2AFA 2AN 2AT 2B6 2BK 2BNA 2CG 2CQ 31C 31E 32F 32M 332 335 337 33D 33F 35J 35Q 363 365 3B7 42DA 44Q 463 464 571 574 5C4 654`

> Воспроизвести списки: `docs/art/` → Python-скан `{[a-z-]*\|\d*}` (ссылки) и
> регэксп `зоар|сулам|ашлаг` (IGNORECASE; в этом shell `grep -i` кириллицу НЕ фолдит — только Python).
