# Архив roadmap_cms — закрытое (летопись + Discovery + завершённые этапы)

Вынесено из `imyavel/roadmap_cms.md` 2026-06-17 для разгрузки живого плана.
Содержимое: Discovery-снимок (2026-06-09), хронологическая летопись «Статус» (append-only —
читать для «почему так», не «что делать») и завершённые этапы (аудит обобщаемости, дизайн-аудит
5 тем, дизайн-ревью топ-10, реализация fn-ревью). **Новые хронологические записи добавлять СЮДА.**
Живой план и «где мы / что дальше» — в `roadmap_cms.md`.

---

## Текущее состояние (Discovery — исторический снимок 2026-06-09; актуальная позиция — блок выше)
- **352 статьи** в `docs/art/` (имена — арт-id: `03H`, `roza_mira`…). Каждая —
  самодостаточный HTML с **собственным инлайн-`<style>`** (352 уникальных; общего
  CSS у статей нет — это и есть «разброс стилей»).
- 2 исходные схемы вёрстки (`data-scheme`: editorial 300 / manuscript 58). В каждой
  статье — скрытый `<script id="yanik-meta">` с `{scheme, form, rationale, features}`.
- Семантика общая (её задавал промпт `legacy/batch_runner/convert_prompt_005.md`:
  крошки, картинка, H1, byline, attribution, TOC с ↑, секции h2/h3, сноски с
  hover-popup, музыка lazy-YouTube `<details>`, prev/next, колофон). Имена классов —
  у каждой статьи свои (стих: stanza/verse/line/ln; сноски: fn/fnref/fn-pop/…; и т.д.).
- **Ручных правок в HTML фактически нет** — ценность живой вёрстки = решения LLM.
- `raw/` — 351 оригинал proza.ru. `.zml`-исходников корпуса **нет**. Базовый формат
  специфицирован: `cms-revival/zml1/SPEC.md` (**ZML1 v0.1**); формат эволюционировал в
  **ZML3** (ZML1 + `[spl]` + опции frontmatter) — **спеку ZML3 нужно написать**.
  Задание B = `cms-revival/plans/roadmap_2_content-mgmt_PLAN.md`.

## Статус (хронологическая летопись — append-only; НЕ источник плана)
*Решения формата продублированы в § «Зафиксированные решения / Б» и канонизированы в `zml3/SPEC.md`.
Здесь — летопись с обоснованиями и нюансами; читать для «почему так», не для «что делать».*
- **2026-06-09:** Discovery сделан, развилки закрыты, roadmap забейслайнен.
- **2026-06-09 (Ф0 ✔):** Стенд готов. cms-revival уже распакован; добавлен локальный
  драйвер ZML1→HTML: `cms-revival/editor/build.mjs` + `editor/package.json`
  (`type:module`, чтобы Node грузил `render.js` как ESM). Гейт пройден: `node build.mjs`
  рендерит `zml1/demo.zml`→HTML без ошибок, все блоки строятся. Расхождения с курируемым
  `demo.html`: (1) ожидаемое — КАПС→small-caps в заголовках/TOC (живая капс-адаптация
  ZML1); (2) плейсхолдеры демо — `[[195/078/170]]`, `{beresheet|17}` нет в манифесте/zohar
  → `class="broken"`; (3) на Ф3: `[epil]` не парсится (документ. epi/epil shadowing,
  эпилог = хвостовой `[epi]`), нет `class="attribution"` у лид-абзаца, нет backtick-`code`,
  дубль `cite_ref-2` при повторной `[^2]`. docs/ не тронут.
- **2026-06-09 (Ф1 ✔):** Разведка через рендер-арбитр (headless Chrome → 352 PNG →
  визуальная кластеризация зрением; `cms-revival/recon/`, отчёт `F1_recon_report.md`).
  Токен-фингерпринт был слишком груб (слил бордо/терракоту в «warm», не мерил
  композицию/декор) — снят. Глаз: единый дизайн-язык + ~2 «позы» (editorial/манускрипт);
  пестрота = обложки-иллюстрации (контент), 9 холодных выбросов. **Гейт принят: K=5**
  (A editorial + B manuscript — якоря анти-регресса; cyberpunk, swiss, ar-deco —
  творческие), **ширины=2** (~820/~1080). Состав свободно меняется позже.
- **2026-06-09 (Ф2 ✔):** 5 form-agnostic CSS-тем в `cms-revival/themes/` (+ контракт
  `THEME_CONTRACT.md`, стенд `gallery.mjs`, галерея `gallery/index.html`). Каждая
  покрывает весь контракт render.js; рендерятся через headless Chrome во всех темах.
  Оператор принял «базово». Шрифт-стеки дошлифовать кросс-платформенно к финалу.
- **2026-06-09 (Ф3 ✔ гейт):** Задание A — инлайн-тег `[spl]` (спойлер как в Телеграме:
  контент под обложкой-`::before`, клик раскрывает; `[spl="cover.gif"]` или дефолт-штрих;
  работает внутри `[sub]` и блоков) — парсер в `render.js` + базовый CSS/JS в
  `template.html`. Фикс `[epil]` (порядок PAIRED: epil раньше epi). demo с `[spl]`+все
  блоки рендерится во всех 5 темах, спойлер раскрывается (в т.ч. в `[sub]`), вложенная
  инлайн-разметка внутри спойлера обрабатывается. `theme:`/`width:` закреплены во
  frontmatter: `render.js` читает их → `body[data-theme]` + ширина классом
  `.wrap.w-narrow/.w-wide` (820/1080), **ширина ортогональна теме** (база в
  `template.html`, дефолт wide). Проверено: B/ar-deco на wide не ломаются.
  **Ф3 закрыт. Следующее: Ф4 — конвертер HTML→ZML + рендер через тему (оракул-сверка).**
- **2026-06-10 (Ф4, в работе — ручной оракул с оператором):** Конвертер
  `cms-revival/convert/html_to_zml.py` (role-based: разнородные классы → канон-блоки
  по роли + сбор «флагов» неуверенного маппинга). Тест-выборка 654 (проза) + 237
  (поэма) → `convert/out/*.zml`; просмотрщик `convert/viewer.html` (ZML×5 тем ↔ живой
  оригинал + флаги). Совместный разбор «баг/не-баг» начат.
  **Применено в разборе 2026-06-10:** стенд → ЖИВОЙ манифест (внутр. `[[art]]` чинятся);
  byline «Оригинал:»→«Источник:»; конвертер — чистка whitespace строф (была «лесенка» +
  двойные интервалы), обход секции в порядке документа (эпиграф ПЕРЕД стихом); notes-
  секция не плодит пустой `##`; **сноски — починен дубль номера** (`.num` оригинала тёк
  в тело → рендер давал «7. ↑ 7 …»; теперь номер берётся из `.num`/backref/id и чистится
  из тела; orphan-определения 4/6/10/28 нумеруются вместо `[^]`; хвостовая стрелка `↩`
  тоже течёт в тело — добавлена в набор backref-стрелок рядом с `↑`). Уровень — ТОЛЬКО
  конвертер (`.zml` = источник истины, обязан быть чистым); render не трогали. Сверено
  end-to-end: render даёт «N. ↑ тело» без дубля, явный `value="N"` держит gap (№22 нет,
  не съезжает), orphan'ы в списке. **Музыка vs «Музыка::»-сноски — НЕ дедупим** (оба
  остаются, решение оператора); **`[mus]`-трек валиден при ≥1 поле** (название ИЛИ url):
  конвертер роняет только пустой `[||]`, одиночные держит; render уже толерантен (url-only
  → плеер/ссылка, title-only → строка) — чтобы недостающее можно было дописать в он-сайт
  редакторе. **Секционные эпиграфы — починен ДУБЛЬ (2026-06-10, поймал оператор):**
  рекурсивный сбор шапочных `find(root, .epi)` хватал и эпиграфы внутри `sec-block`,
  которые потом ещё раз эмитил `walk_section` → один эпиграф выходил дважды (киноцитата
  «Ты почувствуешь его» в 237 — 2× при 1× в оригинале). Теперь шапочный цикл исключает
  секционные эпиграфы по `id`; корпусный баг (любая статья с эпиграфом внутри секции).
  Выключка эпиграфа — ЗАКРЫТО per-theme (см. ниже).
  Формат = **ZML3** (база ZML1 + `[spl]` + опции ниже).
  **TODO формата ZML3 (ЗАПИСАНО оператором, НЕ делать пока):**
  («к реализации» = подтверждено к реализации В ФОРМАТЕ, в отличие от «кандидатов
  на решение» ниже. Сами элементы/поля — ОПЦИОНАЛЬНЫ per-article: формат обязан их
  поддерживать, но конкретная статья может их не иметь.)
  - 📑 **СПЕКА ZML3 — создать** (`cms-revival/zml3/SPEC.md`; СВОЯ спека, НЕ на базе zml1 —
    zml1 + `zml2/ZML2_SPEC_010.md` = референсные входы, см. Статус 2026-06-10 «сверка с ZML2»).
    Формализовать: `[spl]`, фикс `[epil]`, `theme:`/`width:` (ширина ортогональна),
    + все пункты ниже (`audio`, `notes_title`, `caps`-флаг, section-subtitle, `shir` (формат TBD),
    `[mus]`-валидность ≥1 поле, `\n`-семантика по блокам, приоритет H1). Опорная спека
    для реализации и CMS-редактора.
  - 🆔 **ЗАГОЛОВОК H1 — приоритет ВНУТРИ-статейного H1 над manifest** (решение оператора).
    Если в статье есть свой H1 — ИМЕННО он идёт в `title:` frontmatter и в H1 вверху
    (напр. 237: «ДАНО ТВОРЦУ, СВЯТ БЛАГОСЛОВЕН ОН, ПРАВО ЛЮБИТЬ <\^/>», а НЕ короткий
    manifest-title «Дано Творцу право Любить»). manifest-title — фолбэк, если H1 нет.
    Сейчас конвертер берёт manifest ПЕРВЫМ (`title = rec.title or h1`) → перевернуть на
    H1-первым (очищая от audio-🎧). Закрывает прежний кандидат «источник title».
  - 🎧 **АУДИО-ОЗВУЧКА — к реализации.** Отдельный ОБЩЕКОРПУСНЫЙ элемент (введён ПОЗЖЕ
    LLM-вёрстки → единообразен; есть у большинства статей):
    `<a class="audio-link" href="https://t.me/art_017_tracks/NNN" title="Аудио: …">🎧</a>`
    рядом с H1, бывает НЕСКОЛЬКО (по частям статьи). → завести поле `audio:` во
    frontmatter ZML (список {url, подпись}); render/template рисует тем же значком 🎧
    у H1; конвертер ОБЯЗАН извлекать их в frontmatter (сейчас отбрасывает = баг).
  - 📝 **АВТОРСКИЙ ЗАГОЛОВОК СЕКЦИИ ПРИМЕЧАНИЙ — к реализации** (подтвердил оператор).
    Секция сносок/ссылок названа ПО-РАЗНОМУ от статьи к статье («Примечания», «Пост
    скриптум», «Полезные ссылки», …); render жёстко пишет «Примечания» → авторское имя
    теряется. → поле `notes_title:` во frontmatter (render рисует его вместо
    «Примечания», дефолт «Примечания»); конвертер извлекает заголовок notes-секции в
    это поле И НЕ эмитит его как `##` (иначе пустой заголовок + дубль «Примечания», как
    сейчас в 654 при flat-обходе).
  - 🔠 **CAPS-ОПЦИЯ (zml1-капс) — frontmatter-флаг, ДЕФОЛТ ВЫКЛ** (решение оператора).
    zml1 имел: КАПС-строка → `<span class="sb">` (+ список `proper-nouns.txt` для
    заглавных у имён + ручной `^Слово` + капитализация по предложениям). В zml2 от
    ВСЕГО отказались. В zml3 — как ОПЦИЯ: флаг во frontmatter (напр. `caps:`), по
    умолчанию **OFF**. OFF (дефолт) = текст как есть, без фолдинга/списка/`^`. ON =
    возрождает всё zml1-поведение. Сейчас `render.js` применяет `resolveCaps`
    БЕЗУСЛОВНО → надо загейтить флагом (вкл. proper-nouns и `^`-маркер). `.sb`-визуал
    (small-caps vs zml1-болд) — на усмотрение темы, когда опция включена. Список
    `proper-nouns.txt` тоже освежить из живого (как манифест).
  - 🏷 **ПОДЗАГОЛОВОК-ПОЯСНЕНИЕ ПОД ЗАГОЛОВКОМ (section-subtitle) — к реализации**
    (отметил оператор: отсутствие отдельной сущности = регресс). Пояснительная строка
    под заголовком РАЗДЕЛА (`.sec-sub`) — и концептуально под общим заголовком статьи.
    Сейчас конвертер выдаёт её обычным параграфом (нет ZML-конструкции) → регресс.
    Завести элемент (блок `[subsec]…[/subsec]` или расширить `[sub]` на пост-`##`
    позицию); render рисует стилизованным подзаголовком (приглушённый italic, мельче,
    привязан к заголовку), темы стилизуют; конвертер мапит `.sec-sub` в него. NB:
    article-level подзаголовок уже есть = `[sub]` → `h2.subtitle`; новый — секционный
    собрат той же сущности.
    **РЕАЛИЗОВАНО (база) 2026-06-10:** блок `[subsec]…[/subsec]` (в `PAIRED` перед `sub`,
    чтобы не съедался как `[sub]`); конвертер `emit_subsec` мапит `.sec-sub`, СОХРАНЯЯ
    `<br>`-строки (был регресс-склейка); `renderSubsec` → `<p class="sec-sub">` построчно;
    базовый стиль (приглушённый italic, мельче, прижат к `##`) в `template.html`. Сверено
    на 237: 4 sec-sub, многострочные идут через `<br>`, флаги sec-sub ушли. Выравнивание
    под тему сделано (база `text-indent:0`/left в `template.html`; B и ar-deco →
    `article .sec-sub{text-align:center}`, т.к. их заголовки центрированы); остальная
    per-theme эстетика `.sec-sub` — в фазе сборки.
  - 🎼 **БЛОК «ПЕСНЬ СТУПЕНЕЙ» (`[shir]`) — ФОРМАТ НЕ УТВЕРЖДЁН** (переименован из рабочего
    `[songgrid]` в `[shir]` 2026-06-10). Принят лишь ФАКТ «такой блок будет»; его дизайн на
    повестку оператора НЕ выносился (LLM зафиксировал черновик) — ниже открытая агенда.
    *Контекст (что переносим):* сейчас `docs/songs/index.html` — рукотворная страница: грид
    плиток (~220, 3 в ряд); плитка = lazy-YouTube (`.yt` data-id → клик впрыскивает iframe
    autoplay; превью `i.ytimg.com/vi/<ID>/hqdefault.jpg`; `.play`-оверлей) + `.t`
    «Композиция — Автор» + `.a` ряд ссылок на статьи через разделитель `♪` (уже на
    `../art/<арт-id>.html`). Цель — описать этот грид блоком `[shir …]…[/shir]`.
    **ОТКРЫТАЯ ПОВЕСТКА (решать по одному, ничего не утверждено):**
    1. **Границы сущности — РЕШЕНО (2026-06-10): общий переиспользуемый блок, два контекста.**
       (a) спец-страница «Песнь ступеней» — плитки + ПОД каждой список статей, где музыка
       упоминается (роль `.a` — только здесь); (b) внутри обычной статьи — «Музыка под
       настроение»: тот же плиточный грид с фреймами для нескольких музыкальных вставок подряд,
       БЕЗ списка статей. Контекст (b) в активном корпусе ОТСУТСТВУЕТ → сейчас НЕ внедряем/не
       мигрируем, но формат обязан его ДОПУСКАТЬ (список статей — НЕОБЯЗАТЕЛЬНЫЙ элемент: в (a)
       обычно есть, в (b) отсутствует; на валидность записи не влияет, см. п.3). NB: позже
       сверить с инлайн/блок `[mus]` (нынешняя музыка в статьях) — реконсиляция отложена.
    2. **Синтаксис записи — РЕШЕНО (2026-06-10): line-based** — одна песня = одна строка,
       колонки через `|` (компактно для ~220, удобнее по мнению оператора). Вложенный вариант
       отклонён. Открытые механики уходят в п.3: точный набор колонок (комп./автор РАЗДЕЛЬНО
       → НЕ пред-склеивать через `—`), разделитель списка статей, экранирование `|`,
       per-id override-подпись.
    3. **Поля записи и формат строки — РЕШЕНО (2026-06-10, оператор согласовал).** Колонки
       через `|`: `ytID | Композиция | Автор | id,id,id` (комп. и автор РАЗДЕЛЬНО, render
       склеивает «Композиция — Автор»; НЕ пред-склеивать `—`). YouTube-ID чистый, без `?si`
       (см. [[feedback_yaniktoim_youtube_no_si]]); статьи — внутр. арт-id через запятую, reuse
       `[[art]]` (title+href из манифеста, уходит хардкод); per-id override-подпись ссылки —
       `id:Текст` («П-е-с-н-ь C-т-у-п-е-н-е-й»); редкий `|` в поле экранируется `\|`.
       **Валидность — РЕШЕНО (2026-06-10):** запись валидна при ОБЯЗАТЕЛЬНЫХ трёх полях — ytID
       (видео) + Композиция (название) + Автор; нет любого из трёх → плитка битая. **Список
       статей — НЕОБЯЗАТЕЛЬНЫЙ** (0 допускается): в (a) «Песнь ступеней» он обычно есть, в (b)
       «Музыка под настроение» ВНУТРИ статьи ссылок под блоком НЕТ — поэтому на валидность не
       влияет. Правило едино для (a) и (b). 4-я колонка строки может быть пустой/отсутствовать.
       **Локализация ошибки — РЕШЕНО (2026-06-10, оператор согласовал):** битая строка роняет
       ТОЛЬКО свою плитку, НЕ весь блок; рендер/конвертер обязан дать точный диагноз (№ строки в
       блоке + её текст + какого поля нет), а в режиме отладки — рисовать на МЕСТЕ плитки видимый
       маркер-заглушку («⚠ строка N: нет автора»), чтобы ошибка была видна прямо на странице
       (важно в ~220-строчном блоке). **→ `[shir]` ЗАКРЫТ ПОЛНОСТЬЮ (8/8 пунктов).**
    4. **Опции раскладки — РЕШЕНО (2026-06-10): атрибуты на открывающем теге `[shir …]`** (не
       frontmatter — локально, допускает несколько гридов в одной статье, контекст (b)). `cols`
       = макс плиток в ряд, `min` = мин. ширина плитки (≈300px); адаптив через CSS
       `auto-fill + minmax(max(min,(100%−gaps)/cols),1fr)` без медиазапросов. Дефолты — `cols=3`,
       `min=300` (как нынешняя страница); уточнить при сборке. Структура: `[shir …]` = контейнер-
       грид, песня = строка внутри (своего per-song тега НЕТ — следствие line-based).
    5. **Проигрыватель — РЕШЕНО (2026-06-10): как на нынешней опубликованной странице, без
       нового.** Lazy-YouTube 1:1: превью `i.ytimg.com/vi/<ID>/hqdefault.jpg`, `.play`-оверлей,
       клик→впрыск iframe autoplay, a11y (`role=button`/`tabindex`/Enter/Space/`aria-label`);
       lazy ОБЯЗАТЕЛЕН (220 iframe сразу = смерть страницы). Механика фиксирована в
       render/template; вид плитки — тема (п.6).
    6. **Тематизация — РЕШЕНО (2026-06-10): в спеку/контракт СЕЙЧАС, не откладывать.** Компонент
       `[shir]`+плитка добавляется в `THEME_CONTRACT.md`; каждая из 5 тем (A/B/cyberpunk/swiss/
       ar-deco) оформляет плитку ПО АНАЛОГИИ со своим уже существующим дизайном (ничего нового
       не выдумываем — следуем идиоме темы: форма карточки, play-кнопка, разделитель-глиф,
       типографика подписи). Дизайн-намерение фиксируем сейчас; CSS-реализация — в отложенной
       фазе (п.8).
    7. **Заголовок блока — РЕШЕНО (2026-06-10): ОТДЕЛЬНО, `[shir]` его НЕ рисует.** Как на
       нынешней странице: сначала заголовок, потом описание, потом плитки → заголовок = обычный
       `##` сверху, описание = обычный абзац/`[subsec]` над блоком; `[shir]` рендерит ТОЛЬКО грид.
    8. **Носитель и фаза реализации — РЕШЕНО (2026-06-10): формат в ZML3 сейчас, реализация — в
       САМЫЙ КОНЕЦ.** Блок-формат `[shir]` фиксируем в `zml3/SPEC.md` сейчас; парсер/render/
       CSS-темизация + отладка + миграция спец-страницы songs (ZML-носитель
       `docs/songs/index.zml`?) — на ПОСЛЕДНЕМ этапе, ПЕРЕД редактируемыми разделами (Ф10).
       songs — спец-страница, не арт-id-статья.
  - 📐 **МОДЕЛЬ `[epi]` + эпилог — РЕШЕНО (2026-06-10, A4+B5).** `[epi]` = единый блок-РОЛЬ
    «обрамление» (предваряет / сопровождает / завершает; функция, не цитатность; содержимое —
    своя проза/стих/афоризм ИЛИ внешняя цитата → `cite=`). [[project_yaniktoim_epigraph_functional]].
    **Ось `kind=prose|verse|aphorism`, деф. `prose`:** `prose` — абзацы (мягкие переносы
    схлопываются, как сейчас); `verse` — стих, переносы строк СОХРАНЯЮТСЯ и рисуются построчно
    (это и чинит исходный баг склейки стих-эпиграфа), пустая строка = разрыв строфы; `aphorism` —
    короткая standalone-сентенция. Тема рисует 3 подвида по-своему (form-agnostic). **Эпилог —
    БЕЗ отдельного тега `[epil]` (A4, как ZML2 Q3):** эпилог = `[epi]` в ХВОСТОВОЙ позиции
    (последний блок перед аппаратом сносок); метку «Эпилог» (vs «Эпиграф») ставит render по
    позиции → откатить Ф3-активацию `[epil]` (убрать из `PAIRED`, добавить детект хвоста в
    `renderEpigraph`). `cite=` опц. — источник. **Конвертер:** `kind` эвристикой (стих-форма
    `<br>`-строк → `verse`, иначе `prose`, короткая сентенция → опц. `aphorism`), при `verse`
    хранит переносы; оператор переопределяет `kind`/`cite` в редакторе. **Реализация — в фазе
    сборки ZML3** (конвертер + render + 5 тем); приёмочный тест — стих-эпиграф 237 рисуется
    стихом, а не прозой. **БАЗА РЕАЛИЗОВАНА 2026-06-10:** `emit_epi` ставит `kind="verse"`
    по эвристике (≥2 строк, средняя длина < 50 симв — отсекает CAPS-прозу с косметическими
    `<br>`); хранит строки И разрыв строфы (косметический `\n`+отступ снимается, но двойной
    `<br>` выживает как пустая строка). `renderEpigraph` рисует строки через `<br>`, строфы —
    отдельными `<p>` + класс `.epigraph.verse`. Сверено на 237: эп1 = трёхстрочная цитата +
    пауза + хвост (как в оригинале), эп2/3 — проза, эп4 (киношный диалог) = verse. Осталась
    per-theme эстетика `.epigraph.verse` (5 тем) — в фазе сборки.
  - 📏 **ПЕРЕНОСЫ СТРОК — это КОНВЕРТЕРНАЯ корректность, НЕ клауза формата (2026-06-10).**
    Скан 352: `<br>` = 3303 (~99% в `stanza/verse/epigraph/сноски` = авторские); `\n` внутри
    `<p>` = 273, из них **267 — СТИХ** (напр. 17DA = пушкинское «Три девицы под окном» в
    `<p class="verse">`). Косметического «мягкого переноса» в корпусе практически нет —
    перенос почти всегда авторский. → **Converter-требование (Ф4) + приёмочный тест:** НЕ
    плющить авторские переносы — стих-в-`<p>` и многострочные сноски маршрутизировать в
    `[poem]`/continuation, эпиграф-стих → `[epi kind=verse]`; схлопывать ТОЛЬКО незначащий
    whitespace разметки (отступы вокруг тегов). **В спеку ZML3** идёт лишь таблица
    `\n`-семантики по блокам: significant (`[poem]`, `[epi kind=verse]`, continuation сносок
    отступом 4 пробела) vs flow (проза, `[epi kind=prose]`, `[quote]`, `[num]`; пустая строка
    = абзац/единица). Сам «принцип сохранения» — не правило формата (в `.zml` мусорных
    переносов нет, каждый `\n` намеренный), а корректность миграции HTML→ZML.
  - Прочие кандидаты разбора: **`lead-source` — ЗАКРЫТО (2026-06-10): оставляем как есть
    сейчас** (ссылка на источник остаётся на текущем месте, поведение не меняем).
    byline-формат («#AP237» vs «#237»; формат даты); путь обложки
    `pics` ↔ `img`; **сноски-`::`** — авторский разделитель (`См. также::Заголовок::
    Автор`, лежит в определениях сносок оригинала); живой сайт прихорашивает его в
    hover-popup (`: «» —`), а ZML рендерит сырой `::` → нормализовать в ZML или рендерить
    как есть (решить). **Выключка эпиграфа — ЗАКРЫТО (2026-06-10):** оставляем per-theme
    (темы намеренно расходятся выключкой — оператор постатейно выберет более красивый
    вариант позже, ровно как и сам выбор темы; единое правило не вводим). **Музыка-дубль
    (плеер + «Музыка::»-сноска) — ЗАКРЫТО:** не дедупим (см. «Применено»).
  - **Данные стенда:** стенд (gallery.mjs/build.mjs/html_to_zml.py) переключён на
    ЖИВОЙ `yaniktoim/manifest.json` (352) — копия `editor/data/manifest.json` (350)
    устарела (расхождение ~30 в каждую сторону → внутренние `[[art]]` на новые статьи
    бились). Копии `editor/data/*` (manifest, возможно zohar_index/proper-nouns)
    освежить из живых источников перед продакшеном.
- **2026-06-10 (формат — сверка с ZML2):** обнаружена ЗАВЕРШЁННАЯ спека **ZML2**
  (`cms-revival/zml2/ZML2_SPEC_010.md` + реестр `decisions_open_003.md`, 16/16 закрыто,
  ратифиц. оператором на воротах 2b 2026-05-29) — наш lineage её занижал («отказ от
  капс/списка/^»). **Решение оператора:** ZML3 — СВОЯ спека, НЕ базируется ни на zml1,
  ни на zml2; оба — референсные входы. Базу НЕ переустанавливаем; проходим точки
  расхождения по одной, вердикты кладём в `zml3/SPEC.md`; дальше пляшем только от ZML3.
  **Карта расхождений (агенда разбора, идём A→B→C по одному):**
  - **A. Противоречия (наш ZML3 ↔ ратифиц. ZML2):** A1 CAPS (наш флаг-ревайв zml1 ↔
    ZML2 «капс литерально; `^СЛОВО`/small-caps/proper-nouns ОТМЕНЕНЫ»; render сейчас
    делает `resolveCaps` безусловно = zml1-режим); A2 notes_title (хранить ↔ ZML2
    выбрасывает как служебный); A3 orphan-сноски (наша сег. нумерация `[^4]:`+`↩` ↔
    ZML2 Q11 анон-карточка без `↩`); A4 `[epil]` (Ф3 вернул отдельный тег ↔ ZML2 Q3:
    эпилог = `[epi]` в хвостовой позиции).
  - **B. ZML2 уже закрыл (а мы заводили заново):** B5 `[epi kind=verse|prose|aphorism]`
    (снимает развилку A/B из 📐-пункта выше); B6 `::`→канон `См. также: [[ID]] — Автор`
    (резолв из `[[art]]`); B7 byline → канонический art-id; B8 H1 = `#`-в-теле (§1.0);
    B9 cover → `frontmatter.image`.
  - **C. Фичи ZML2, которых в нашем ZML3 нет:** C10 inline `{term|…}`/`{leit|…}` (Q13);
    C11 блоки `[sig]`/`[cry]` (Q7/Q8); C12 `[quote kind=scripture]` (ось vs `[epi]`, Q2);
    C13 глифы `<\^/>`/`</v\>`/эмотиконы→`.glyph` + чистка мусора proza.ru `;;;;` (§5/§8;
    NB `;::)))` в 237 — глиф, не мусор); C14 `[num]` обобщён (lineage «отказ от списка»
    неверен — оставлен, в резерв убран лишь bulleted).
  **Уже решено выделенными TODO-пунктами выше (НЕ переоткрывать):** A1 = 🔠-пункт
  (caps-флаг, деф. OFF — осознанно ≠ zml2, причём про zml2-отказ пункт ЗНАЛ); A2 =
  📝-пункт (notes_title — хранить, ≠ zml2); B8 = 🆔-пункт (H1-приоритет, = zml2 §1.0).
  **C10–C14 — ЗАКРЫТО (2026-06-10, оператор подтвердил батч «взять всё»):** ZML3 берёт
  конструкции ZML2 — C10 inline `{term|…}`/`{leit|…}`; C11 блоки `[sig]`/`[cry]`; C12
  `[quote kind=scripture]` (уже есть в render `renderQuote`); C13 глифы
  `<\^/>`/`</v\>`/эмотиконы→`.glyph` + чистка мусора `;;;;`; C14 `[num]` обобщён (уже есть
  `renderNum`). Реализация новых (term/leit, sig, cry, глифы) — в фазе сборки.
  **Реально открытых развилок A→C больше нет.**
  **A3 — ЗАКРЫТО (2026-06-10):** сирота-сноска СОХРАНЯЕТ номер (`[^4]:`, верно источнику),
  но render НЕ печатает backref-`↑` для несвязанной сноски (возвращаться некуда — была
  битая ссылка на отсутствующий `cite_ref-N`). Середина: номер — от нас, «без `↩`» — от
  ZML2 Q11. Правка в `render.js`: реестр `inline.refNotes` (id всех упомянутых `[^N]` из
  тела и тел сносок), orphan = вне реестра → без стрелки. Сверено на 237: сироты 4/6/10/28
  без стрелки, упомянутые 1/7/12/25/29 — со стрелкой; номера сохранены.
  **A4+B5 — ЗАКРЫТО (2026-06-10):** единая модель `[epi kind=prose|verse|aphorism]` +
  эпилог-по-позиции (тег `[epil]` убираем) — детали в 📐-пункте выше; реализация — в фазе
  сборки ZML3, приёмочный тест = стих-эпиграф 237.
  **B6 — ЗАКРЫТО (2026-06-10), реализация отложена в конвертерную фазу:** принят канон ZML2
  для СМ.-ТАКЖЕ — `См. также: [[ID]] — Автор` (конвертер по заголовку ищет art-id → `[[ID]]`,
  render резолвит название; фолбэк при отсутствии совпадения — литерал с нормальной пунктуацией
  `: заголовок — автор`, без `::`). ВАЖНО (нюанс ZML2 §4): `::` НЕ трогаем огульно —
  sourceType-маркеры `Музыка::`/`Цитата::` остаются ЛИТЕРАЛЬНЫМИ (с `::`), и авторские
  `::`-перечни (`схема ::1::;::2::`) тоже. Отложено, потому что обратный поиск «заголовок→art-id»
  нечёткий (риск ложных совпадений) + нужна классификация типа сноски по префиксу — делаем
  аккуратно в конвертерной фазе, не на ходу.
  **B7 — ЗАКРЫТО (2026-06-10), уже соблюдено:** byline render рисует из FM каноническим
  art-id (`#237`, не авторский `#AP…`) + дата + «Источник:» — ровно канон ZML2 (авторский
  колофон конвертер отбрасывает, render регенерит). Формат даты сейчас ISO `2022-03-07` —
  презентационный, не развилка; гуманизацию («7 марта 2022») оставляем опц. полировкой темы.
  **B9 — ЗАКРЫТО + ИСПРАВЛЕНО (2026-06-10):** обложка во `frontmatter.image` (канон ZML2 Q10).
  Найден баг: render.js хардкодил `../pics/`, а картинки лежат в `docs/img/` (207 шт.,
  `docs/pics/` нет) → битый путь/404. Починено `../pics/`→`../img/` (как живой сайт); сверено
  (cover 237/654 = `../img/<art>.jpg`, файлы на месте).
  **ИТОГ РАМКИ ZML3 (2026-06-10):** ZML3 = набор конструкций **ZML2** + **3 осознанных
  расхождения** (A1 caps-флаг ≠ zml2-литерал; A2 notes_title-хранить ≠ zml2-выброс; A3
  orphan-с-номером уточняет zml2 Q11) + **5 новых поверх** (`[spl]`, ортогональная `width`,
  audio 🎧, `[shir]` (формат TBD), `[subsec]`). Развилки A→C пройдены.
  **СЛЕДУЮЩИЙ ШАГ: собрать `cms-revival/zml3/SPEC.md`** из вердиктов выше (своя спека, НЕ
  базируется на zml1/zml2 — оба референсы), затем реализовать отложенное в фазе сборки.
  Разобранные пункты фиксируем здесь и в `zml3/SPEC.md`.
- **2026-06-10 (`[shir]` ЗАКРЫТ 8/8 + СПЕКА ZML3 v1 СОБРАНА):** все 8 пунктов `[shir]`
  решены (сущность/контексты, line-based-синтаксис, поля+валидность [обязательны
  ytID+название+автор, список статей опц.], локализация ошибки, опции на теге, проигрыватель
  как сейчас, темизация по аналогии, заголовок отдельно, реализация — перед Ф10). **Создан
  `cms-revival/zml3/SPEC.md` (v1)** — полное определение формата из всех вердиктов (lineage =
  zml1/zml2 референсы; 3 расхождения A1/A2/A3 + 5 новых spl/width/audio/shir/subsec + батч
  ZML2-конструкций; §13 = карта реализации: что в render.js есть / что отложено в фазу сборки).
  **Развилки формата закрыты.**
- **2026-06-10 (ШАГ 1 пред-прохода ЗАКРЫТ — render-артефакты вычищены):** по go оператора («очевидное
  делаю сам, на развилках спрашиваю»). Правки в `render.js` + `convert/html_to_zml.py` + оба шаблона.
  - **CAPS-гейт (A1):** `resolveInline` и `renderPoem` теперь зовут small-caps-фолдинг только при
    `inline.capsEnabled` (FM `caps`, деф. **OFF**). Сверено: 0 `span.sb` на 10 (было >0) — CAPS-блоки
    рендерятся литерально, как задумано.
  - **Эпиграфы 266/34O:** `is_epi` обобщён (`div.epi` 237 / `p.epi`+`.src` 266 / `blockquote.epigraph`+
    `<cite>` 34O); `emit_epi` тянет cite из `.cite/.epi-cite/.src/<cite>` (прун `<sup>` из cite и cite из
    тела, чтобы не дублить); walk'и скипают эпиграф-узлы + label + heading «ЭПИГРАФ». 266 → 3 `[epi]`
    (вкл. verse Nightwish), 34O → `[epi]` с текстом Зоара.
  - **Эпилог-по-позиции (A4):** `[epil]` убран из `PAIRED`; `renderBlocks` метит ХВОСТОВОЙ `[epi]`
    (последний блок, не единственный) как «Эпилог» по индексу. На 10 хвостовых нет → мислейблов 0.
  - **notes_title (A2):** `find_notes_title` = последний `<h2>` перед собранными сносками (не только
    `id=notes` — 654 = `id=sec3` «Пост скриптум»); гейт `if fns` (не ломает несобранные 182/266);
    in-body заголовок подавляется по тексту. 237→«ПОЛЕЗНЫЕ ССЫЛКИ», 654→«Пост скриптум».
  - **H1-приоритет (B8):** `title = h1txt or rec.title` (чистка 🎧). 237 → длинный авторский H1
    «ДАНО ТВОРЦУ, СВЯТ БЛАГОСЛОВЕН ОН, ПРАВО ЛЮБИТЬ <\^/>» (глиф литерален — C13 в фазе сборки).
  - **Audio 🎧 (по go оператора «включим и закроем»):** конвертер собирает `a.audio-link`/`t.me`-ссылки
    → FM `audio: [{url,label}]` (несколько: 654 ×2 с подписями «Аудио: …», 29M ×2); `link()` больше не
    флагает их как «отброшено». Render: `renderArticleParts` парсит, рисует 🎧-ссылку(и) у H1 через
    `{{AUDIO}}` (добавлен в `template_view.html` И `template.html`) + структурный `.audio-link` CSS.
    Все 10 получили 🎧. Per-theme эстетика 🎧 — в фазе сборки.
  - **НЕ трогали:** см.-также (B6 — отложено, нечёткий обратный поиск) и расширение покрытия (шаг 4).
  - **Проверка:** конвертер на 10 без ошибок → `out/*.zml` → копия в `docs/art/` → `build_views.mjs` →
    10 `.view.html`. preview 8098 + DOM-eval 237: 0 small-caps, 4 эпиграфа, 🎧, notes «ПОЛЕЗНЫЕ ССЫЛКИ»,
    1 `.wrap`, консоль без ошибок.
  - *NB по среде:* `grep -i` в этом shell НЕ фолдит кириллицу (рег/строчные) → даёт ложные
    негативы при проверке; матчить регистр явно или питоном.

- **2026-06-10 (ВИЗУАЛЬНЫЙ REVIEW 10 вью своим зрением, Opus 4.8 → найдены и починены 4 системных бага переноса):**
  оператор: я сам глазами смотрю 10 ZML-вью на дефолт-теме постранично; косяки переноса чинить в МЕХАНИЗМЕ
  (конвертере), не в статьях; цель — гладкий алгоритм для корпуса. **Метод доставки картинок:** headless Chrome
  (`render_all.sh`-стиль) → полностраничный PNG → Pillow trim+нарезка по высоте → **JPEG** сегменты (PNG в контекст
  грузятся с потерей разрешения до нечитаемости — лечится перекодировкой в JPEG; высокие полные снимки харнесс
  ужимает → режу на сегменты ~1600px). Скрипт `cms-revival/convert/viewshots.py` → `_viewshots/<id>_NN.jpg`.
  **Найдено и ПОЧИНЕНО в конвертере (`html_to_zml.py`):**
  - **Музыка → сырой `[url]` без плеера (2BK, 368, 182):** title-класс не распознавался (`t-title`/`ttitle`) →
    `raw_text(track)` тащил переносы строк + ▶/↗ → строка `[mus]` рвалась на 3 физических → render не парсил.
    Фикс: `t-title`/`ttitle` в набор title-классов + `_clean_track_text` (схлопывание ws, срез ведущей ▶/хвостовой
    ↗) + `data-embed` на самом узле + **сбор «плоских» `.track`-сиблингов без обёртки `.music`** (182). Сверено:
    все 20 трек-строк на 10 = чистый одностроч. `[url|title|author]`; 654/34O/237 музыка без регресса.
  - **Сноски не собирались → определения терялись, маркеры `[N]` висли (266 `ul.fnlist/li.fn`; 182 `p.note`+`.marker`):**
    `collect_footnotes` знал только `ol.notes`/`div.fn`. Добавлены ветки `li.fn` (266) и `p.note` (182, звёзд. маркеры
    `*`/`**`, номер из back-href/id). Порядок веток взаимоисключающий → 237(div.fn)/654(ol.notes) без регресса (30/2).
  - **notes_title для новых схем:** `find_notes_title` научен `li.fn`/`p.note` → 266 = «ПРИМЕЧАНИЯ», 182 = «ПОСТ
    СКРИПТУМ» (заголовок из тела подавлен, без дубля).
  - **Сирота-лейбл «Аудиоверсия:» (29M):** после выноса 🎧 в FM оставалась пустая подпись → post-filter дропает
    голый аудио-лейбл, когда `audio` извлечён (ссылка сохранена 2-м 🎧).
  **Признано ВЕРНЫМ (не баг, не трогаем):** 185A список `1)…22)` = авторская ручная нумерация
  `<span class="num">N)</span>`; литеральный `^` (185A «Воина^Исраэль», 237 «ПО^НАСТОЯЩЕМУ») = текст источника;
  2BK «Парадок» = опечатка в исходном H1 (H1-приоритет перенёс верно).
  **Отложено (мелочь / известное):** B6 см.-также `::` (237); 266 сноска №1 тащит ▶/↗ из встроенных в тело
  плеер-ссылок; 266 `[*]`-сноска рисуется с префиксом «См. также:» (дефолт анонимной). **ВЕРДИКТ: перенос 10
  чист**, кроме перечисленной мелочи; затронутые схемы (2 музыкальные + 2 сносочные) теперь общие для корпуса.

- **2026-06-11 (подготовка дизайн-аудита — носитель и промпт переведены под запуск в Desktop App):**
  оператор решил гонять аудит НЕ headless, а отдельным промптом в Desktop App (полный набор инструментов
  модели). Следствия:
  - **`design_review/prompt.md` переписан и упрощён:** снята CLI-обвязка (headless `claude -p`,
    `--dangerously-skip-permissions`, нейтральный cwd), Chrome-MCP, «ТОЛЬКО JPEG»-доктрина и мета-блок Q1–Q3;
    оставлена суть (роль/задача/темы/ширины/элементы/критерии/отчёт), локальные пути показаны. Оператор сам
    уточнил формулировку фокуса (содержание блоков, напр. КАПС, — авторское).
  - **Отладочный каталог `yaniktoim/docs/zml-review/`** (внутри проекта, публикуемый — несекретно): 10 свежих
    `.view.html` (из `convert/out/*.zml` через `build_views.mjs`) + их `.zml`; темы скопированы в `docs/themes/`;
    внутр. ссылки вью (Оригинал/prev-next/«См. также») переписаны на `../art/` (вью переехали на уровень ниже).
  - **Лаунчер `docs/zml-review/index.html`** — список 10 в стиле раздела сайта (общий `style.css`, `.toc`).
    Проверено в браузере (preview :8098): тема применяется, переключатель тема×ширина рабочий, ссылки целы.
  - **JPEG-матрица / `viewshots.py`-тулинг — СНЯТ как блокер:** модель в Desktop App открывает интерактивные вью
    сама, переключает тему/ширину; заранее-нарезанные сегменты не нужны.
  - **Известная мелочь извлечения в 10** (справочно, помечено в промпте как «не дизайн-баг»): сырой `::` в
    сносках 237 (B6 отложен) + 22SA/182; ▶/↗ из встроенных плееров в 266; глифы `<\^/>` / `;::)))` / обрубки
    никуда `(аси;ти)` в 237 (C13-рендер отложен); литеральный `^` (185A/237 — верно источнику); опечатка H1
    «Парадок» 2BK (верно источнику). Эпиграфы все вышли блоками `[epi]`; задвоений нет.

## Этап: аудит обобщаемости правил конвертера — ШАГ 3 ✔ ЗАВЕРШЁН (2026-06-12, конвертер v2; исторический раздел)
**Поручение оператора (2026-06-11).** Контекст: каждый из 352 HTML корпуса — отдельная LLM-вёрстка БЕЗ
единого шаблона; конвертер `convert/html_to_zml.py` строился по схемам 10 статей «Избранного». Триггер —
баг эпиграфа 2 в 237 (prose-ветка `emit_epi` сплющила авторские `<br><br>`-абзацы вопреки уже записанному
📏-правилу «НЕ плющить авторские переносы», SPEC §9): правило БЫЛО, а реализация в одной из веток — нет.
**Что сделать.** Тщательный анализ ВСЕХ существующих правил конвертера (не только `<br>`/epi) именно на
предмет: «насколько вероятно, что каждое правило будет повторяться/ломаться по разнородному корпусу
снова и снова?» — т.е. систематически проверить каждую эвристику/селектор/ветку (классы-маппинги, сбор
сносок, музыка, эпиграфы, заголовки, whitespace-обработка в КАЖДОЙ ветке, byline/колофон, обложки,
audio, см.-также, глифы/мусор `;;;;`…) на обобщаемость за пределы 10 проверенных статей; где правило
заточено на частную схему — оценить, сколько схем реально в корпусе (грепы/скан по 352).
**Выход.** Отчёт оператору: выводы + ВАРИАНТЫ дальнейших действий (например: укреплять правила точечно /
менять архитектуру прохода / расширять выборку пилота / план валидации шага «покрытие корпуса»). Решение
принимает оператор; до его решения никакой другой работы по roadmap не продолжать.
**Замороженные правки под этот аудит:** (а) `emit_epi` prose: `<br><br>` → пустая строка `.zml`;
(б) `.epigraph.caps` без курсива (канон `.epi.caps{font-style:normal}`) — caps-детект в render + 5 тем.

## Этап: дизайн-аудит 5 тем (запуск в Desktop App) — Шаг 2 плана (после пред-прохода Шага 1)
**Что.** Внешний дизайн-анализ 5 тем × 2 ширины на 10 **локальных** ZML-вью статьях (на живом сайте ZML
больше нет — он отдаётся локально, preview 8098 / `editor/build_views.mjs`): найти проблемы читаемости/вида
и довести темы до «конфетки». По сути отложенная полировка Ф2 (+ per-theme эстетика ZML3-элементов) руками
Fable-5-ревьюера. Read-only (код не правит) → отчёт драйвит тема-полировку.
**Запуск (2026-06-11, ИЗМЕНЕНО — не headless).** Оператор гоняет аудит **отдельным промптом в Desktop App**
(полный набор инструментов модели). Прежний headless-рецепт (`claude -p --model fable-5
--dangerously-skip-permissions`, нейтральный cwd, MCP Chrome) — СНЯТ. **Промпт:**
`cms-revival/design_review/prompt.md` — переписан и упрощён под это (без CLI-обвязки / Chrome-MCP /
JPEG-доктрины / мета-блока Q1–Q3; локальные пути показаны).
**Носитель (2026-06-11, ИЗМЕНЕНО — интерактивные вью, не JPEG).** 10 `.view.html` + `.zml` — в отладочном
каталоге **`yaniktoim/docs/zml-review/`** (внутри проекта, публикуемый — несекретно); лаунчер
**`docs/zml-review/index.html`** (список 10 в стиле раздела сайта, на общем `style.css`) открыть для
просмотра; на каждой вью встроенный переключатель тема(5)×ширина(2). Темы — копия в `docs/themes/`; внутр.
ссылки вью переписаны на `../art/`. Прежняя «ТОЛЬКО JPEG»-доставка + `viewshots.py`-матрица — СНЯТА
(модель в Desktop App открывает интерактивные вью сама).
**МОМЕНТ — Шаг 1 ЗАКРЫТ (2026-06-10), аудит можно запускать (по go).** Аудит = **Шаг 2** опорного порядка,
ПОСЛЕ пред-прохода — он сделан (см. «Где мы» + Статус): CAPS-гейт (деф. OFF), эпиграфы 266/34O, эпилог-по-
позиции, H1-приоритет, notes_title, **audio 🎧** (вопреки прежней заметке «эти 10 аудио не используют» —
🎧 есть у ВСЕХ 10, поэтому включили) + по визуальному review добраны 4 схемы переноса (музыка ×2, сноски ×2).
Контент 10 рендерится КАК ЗАДУМАНО → ревьюер не спотыкается о render-артефакты.
**Регенерация вью (2026-06-11, СДЕЛАНО):** copy `convert/out/*.zml` → `docs/art/` + copy `themes/` →
`docs/themes/` + `build_views.mjs` (целит в `docs/art/`), затем перенос 10 `.view.html`+`.zml` в
`docs/zml-review/` с правкой относительных ссылок на `../art/` + лаунчер `docs/zml-review/index.html`.
Вью свежие, проверены в браузере (preview :8098): тема применяется, переключатель тема×ширина рабочий, ссылки целы.
**Развилки закрыты:** Q1 read-only (отчёт без CSS-патчей), Q2 локально, Q3 (Chrome-MCP-тест) — в Desktop App неактуален.

- **2026-06-11 (ДИЗАЙН-АУДИТ ПРОВЕДЁН + первая волна правок по нему):** аудит 5×2 выполнен в Desktop App
  (отчёт `cms-revival/design_review/audit_report_001.md`). Вердикты на родной ширине: B/ar_deco — «конфетка»,
  A/cyberpunk/swiss — «есть проблемы»; поломок ни в одной паре тема×ширина нет. **По go оператора применены
  CSS/шаблонные правки:** swiss H1 18ch→30ch (был 5-строчный висяк) · мера текста A/swiss `article p
  max-width:72ch` (A: 840px ≈ 85 зн., swiss: 721px ≈ 75 зн.; colophon исключён) · cyberpunk: стих
  `--ink`+cyan-кант вместо сплошного неона, обложка центрирована, `--muted` #6c7d92→#8295ac · A: стрелка ↑
  h2 `--rule`→`--muted` · ar_deco: byline .74→.82rem. **«Иван Иванович» в колофоне — НЕ баг:** И.И. = ИИ,
  намеренная дефолт-метка «собрано ИИ, без ручных правок» (разъяснил оператор; временную замену откатили,
  дефолт возвращён в render.js, отчёт исправлен → `audit_report_002.md`). Темы скопированы в `docs/themes/`, 10 вью пересобраны (build_views + префикс `../art/`,
  docs/art очищен), всё сверено зрением в preview. NB: `?v=20260529-05` у тем не бампнут — браузерный кеш
  отдаёт старый CSS (локально пробивали cache-бастером); бампнуть при выкате. **Осталось из отчёта:**
  буквица B (развилка: реализовать или снять из замысла), 🎧-перенос у H1 (render, не CSS), отступ
  безномерных `[*]`-сносок; hyphens:auto объявлен, но Chrome/Win переносы в ru-тексте не ставит — дыры
  выключки лечатся только мерой.

- **2026-06-11 (ФАЗА СБОРКИ, заход 1 — render-конструкции C10/C11/C13 + хвосты аудита):** по go оператора
  («делать по порядку, очерёдность сам»). **render.js:** inline `{term|…}`/`{leit|…}` → `span.term/.leit`
  (после zohar-пасса, цифровой `{ch|N}` не конфликтует) · `[sig]`/`[cry]` в `PAIRED` + `renderSigCry`
  (строки через `<br>`) · глифы C13: whitelist `resolveGlyphs` ПОСЛЕ htmlEscape (`<\^/>`, `</v\>`, `;::)))`;
  мусор `;;;;` НЕ трогает — он конвертерный) · H1 через новый токен `{{TITLE_H1}}` с глиф-пассом
  (`<title>`/крошки — литерал) · 🎧 приклеен к последнему слову H1 словом-джойнером U+2060 (эмодзи = точка
  переноса; чинит сирот-строку A×narrow) · CSS_VERSION → `20260611-01` (пробивает кеш тем). **Шаблоны
  (оба):** базовый CSS `.sig/.cry/.glyph/.term/.leit`. **5 тем:** per-theme акценты (B/deco золотят глиф,
  cyber — magenta+glow, cry капителью в идиоме темы) + `li.anon` выровнен с телом нумерованных сносок
  (был приклеен к левому краю, 266). **Проверено зрением:** тест-стенд `docs/zml-review/zml3test.(zml|
  view.html)` (все конструкции × A/cyber/B) + живые 237 (глиф в H1 золотом, `;::)))` в эпиграфе), 266
  (anon в строю), 654 A×narrow (🎧 не отрывается). **Конвертерная часть C10/C11/C13 (извлечение из живой
  HTML + чистка `;;;;`) — НЕ делалась**, пойдёт с расширением покрытия (шаг 4). NB: preview-скриншоты
  периодически виснут — лечится рестартом preview-сервера.
- **2026-06-11 (буквица B — ЗАКРЫТО: реализована):** оператор поручил выбрать дизайнерским глазом «с/без» —
  сравнение вживую (654/237, B×narrow) → **с буквицей** (церемониальный якорь секции, в идиоме рукописи).
  Чисто CSS (`::first-letter`, ZML не знает): первый АБЗАЦ ПРОЗЫ секции после `##` и его «свиты»
  (sec-sub/музыка/эпиграф между ## и абзацем учтены цепочками селекторов); `.sec-sub`/`.attribution`
  исключены (первая версия дала гигантские «[2»/«М» на sec-sub — поймано зрением, починено); поэмы без
  буквицы (не `<p>`). `h2.subtitle` (эхо H1) буквицу не даёт — она маркирует именно секции. CSS_VERSION
  → `20260611-02`, вью пересобраны.
- **2026-06-11 (frontmatter `dropcap:` — per-article выключатель буквиц):** по запросу оператора. Дефолт ON
  (без упоминания в FM); `dropcap: off` → render вешает `.no-dropcap` на `.wrap`, тема гейтит свои
  `::first-letter`-правила `.wrap:not(.no-dropcap)`. Буквица — фича ТЕМЫ (CSS), флаг — лишь выключатель;
  внесено в `zml3/SPEC.md` §1.1. В «свиту» заголовка добавлен `.cry` (секция может открываться возгласом).
  Проверено DOM-замерами на стенде zml3test (B): без флага — float:left 62.7px; `dropcap: off` →
  `wrap…no-dropcap`, first-letter обычный. CSS_VERSION → `20260611-03`, все вью пересобраны (NB: обрезка
  stdout node через `Select-Object -First` убивает build_views на EPIPE — собирать в переменную).

- **2026-06-11 (баг выравнивания колонок + СМЕНА МЕТОДОЛОГИИ ревью):** оператор в реальном браузере (1920,
  fullscreen) поймал разъезд колонок A_editorial×wide: моя мера `article p{max-width:72ch}` оставила TOC/
  музыку/линейки h2 на полной полосе → края сбиты (в превью 1280 разрыв был мал, я недооценил). **Фикс:**
  мера переведена на ВЕСЬ `article{max-width:72ch}` (A_editorial + swiss) — текст, TOC, карточки, линейки,
  сноски в одной колонке; шапка (крошки/H1/byline/обложка) — на полной полосе (асимметричная editorial-
  сетка). **Методология ИЗМЕНЕНА (решение оператора):** финальная визуальная приёмка — в РЕАЛЬНОМ Chrome
  через Claude_in_Chrome MCP (реальное окно ~1920), превью Desktop App (фикс. 1280) — только для быстрых
  итераций; [[feedback_design_review_real_browser]]. **Сразу пойман второй кросс-средовой баг:** у
  оператора Chrome Auto-Dark инвертирует светлые темы → всем 5 темам добавлен `color-scheme` в `:root`
  (light-темы `only light`, тёмные `dark`). NB-quirk: Auto-Dark решает при ЗАГРУЗКЕ страницы — «горячее»
  переключение тёмная→светлая в предпросмотре инвертируется до reload; в проде тема фиксирована FM →
  не воспроизводится (проверено: zml3test c default swiss грузится белым). CSS_VERSION → `20260611-05`,
  вью пересобраны.
  **Дополнение (тот же вечер): кап меры ОТКАЧЕН вовсе.** Оператор: ограничив весь `article` 72ch, «от
  широкой ширины осталась только широкая картинка» — кап убивал смысл рычага width. **Вердикт: тема меру
  НЕ ограничивает; wide = честная полная полоса (как исторический корпус), узкая мера = per-article
  `width: narrow`.** Пункты аудита «мера строки A/swiss» закрыты ЭТИМ решением (не капом). Выравнивание
  цело (все блоки снова одной ширины). CSS_VERSION → `20260611-06`, вью пересобраны, проверено в реальном
  Chrome (1920): текст/TOC/музыка одной полосой, wide широкая.

- **2026-06-11 (поздний вечер — эпиграф 2 в 237 ≠ канон → ПОРУЧЕНИЕ: аудит обобщаемости правил):**
  оператор сравнил эпиграф 2 (КАПС-проза) с каноном: (а) в каноне он разбит `<br><br>` на 3 абзаца —
  конвертер (prose-ветка `emit_epi`) их сплющил, нарушив записанное 📏-правило/SPEC §9 «НЕ плющить
  авторские переносы» (verse-ветка правило соблюдает — пробел реализации); (б) канон снимает курсив с
  КАПС-эпиграфов (`.epi.caps{font-style:normal}`), темы — нет. Оператор сформулировал ЕДИНОЕ правило:
  «СКЛЕИВАТЬ НИЧЕГО НЕ НУЖНО ВНУТРИ БЛОКОВ — авторская разметка intact» (подтверждено: уже записано в
  📏-пункте и SPEC §9) — и, главное, поставил вопрос системно: корпус = 352 разные вёрстки без шаблона,
  правила конвертера писались по 10 → **поручен АУДИТ ОБОБЩАЕМОСТИ ВСЕХ правил конвертера, ПЕРВЫМ ДЕЛОМ
  с чистого контекста** (см. § «Этап: аудит обобщаемости»); обе правки (а)/(б) заморожены под него;
  выводы и варианты действий — оператору, потом всё остальное.

- **2026-06-12 (ночь — ШАГ 3: АУДИТ ОБОБЩАЕМОСТИ ПРАВИЛ КОНВЕРТЕРА ВЫПОЛНЕН):** по поручению 2026-06-11.
  Инструмент `convert/audit_scan.py` (read-only скан 352 эвристиками конвертера + dry-run `convert()` в
  `convert/audit_out/`, боевой `out/` не тронут; данные `audit_scan.json`/`audit_cover2.json`). Отчёт —
  **`convert/audit_generalizability_001.md`**. Ключевые факты: ошибок 0/352, но **покрытие текста <0.8 у
  73 статей (из них <0.5 у 37; худшие ~0)**; `walk_section` срабатывает на 1 статье (сам пилот 237),
  `walk_flat` берёт только ПЕРВЫЙ контейнер и молча роняет незнакомые `div` (217A/18A/09S/18M), fallback
  включается лишь при нуле блоков; `walk_generic` теряет текст-ноды вне `<p>`, не знает `ol/li` (нет
  эмита `[num]` — 1AD), скипает `section.notes` при несобранных сносках, не извлекает `[quote]`
  (blockquote-роли `forum/msg/scripture/…` + 24 безклассовых). **Сноски собираются у 33, потеряны у 76**
  (инвариант — контейнер `section.notes`+родня, не классы айтемов). Музыка: ядро (`.track`/`data-yt`,
  231 статья) инвариантно, title-классы у 34 статей мимо набора. **Стих-в-`<p>` у 32 статей плющится**
  (`br="space"` в прозо-ветках) — самое массовое нарушение 📏/SPEC §9; замороженный (а) `<br><br>`-в-prose-epi
  — всего 2 статьи. Хром/роли-неведомки: `dateline/date/kicker/lead/seealso(52!)/closing/speaker/q-a/point`
  — теряются или утекают прозой. Обобщается отлично: audio 🎧 (337), хром-классы, `[[art]]`/zohar-линки,
  `;;;;` в живой HTML = 0 (чистка не нужна). **Вывод: класс-имена случайны per-article, инвариантны роли;
  перечни классов не сойдутся. Варианты:** A точечно · **B (рекоменд.) единый role-based walk + инвариант 📏
  + оракул-гейт покрытия** · C = B + LLM-добивка уникумов · D расширить пилот до ~30. Решение — оператора;
  до него конвертер заморожен.

- **2026-06-12 (ночь, автономная сессия по поручению 00:24 — КОНВЕРТЕР v2 + ВЕСЬ КОРПУС):** по решению
  оператора (вариант B+C) walk-слой переписан: `convert/html_to_zml.py` v2 (бэкап v1 рядом,
  `*_v1_pre_rewrite.py.bak`). Архитектура: ОДИН проход в документ-порядке, спуск в любые обёртки,
  эмит по роли; текст-ноды и `ol/ul` не теряются (`ol`→`[num]`, `ul`→абзацы-тире+флаг); `blockquote`
  вне эпиграф-роли → `[quote]` (+`kind=scripture` по классу); сноски — от контейнер-роли
  (`section.notes`+родня), потребляются ТОЛЬКО айтемы (не контейнер целиком — «П.С.»-абзацы рядом
  выживают, 2CQ), многострочные тела → continuation 4 пробела; музыка — title по структуре
  (b/strong + «—»-сплит) при незнакомых классах, дедуп плиток по yt-ID; 📏-инвариант: `<br>` через
  сентинел отделён от косметики разметки, `<br><br>` в прозе/epi → абзацы (фикс (а) поглощён),
  стих-в-`<p>` → `[poem]`, в стих-контексте значимы и текст-переносы (29M); аудио — только явные
  признаки (класс/лейбл/шапка; голая t.me в теле = контент, 42DA); орнамент-абзацы и лейбл «Эпиграф»
  гасятся; kicker→`[sub]`, h3→`##` с флагом; **оракул-гейт в convert()**: текст-масса оригинал↔ZML
  (<0.97 или >1.20 → флаг), метрика учитывает cite=/лейблы/резолв `[[ID]]`. Отладка на 10 канонных
  (дифф против `out_v1_canon/`): расхождения только улучшения (185A — сноски найдены + чистый title;
  182 — потерянный лид вернулся; 266 — музыка-сноска чистая; 2BK/34O — см.-также живо; 237 —
  epi-абзацы по канону + `{prayer}`-слаг; 22SA — intertitle спасён). **Корпус: 352/352 без ошибок,
  338 в коридоре 0.97–1.05** (v1: ~204), бывшие лосеры аудита 217A/18A/09S/1AD/18M/2CQ → ≥0.99.
  Оракул-флаги (12) разобраны постатейно: шум метрики (0BH/1BI — тексты zohar-ссылок; 53KA — дедуп
  дублей плиток; 42DA — URL-текст; 34O — см.также-резолв; 1C8A/1CB — задвоение попап-текста);
  реальный кейс 323 (CAPS-проза между строф внутри div.poem) починен (`emit_verse_blocks`), 1CB-схема
  inline-определений в `.fnref` подстрахована гейтом. **Текстовых потерь в корпусе не выявлено.** Флаг-сводка
  корпуса (169 статей с флагами): шапочная дата 86 · h3 69 (формат-вопрос: нужен ли `###`?) ·
  kicker 46 · ul 26 · диалог-разметка ~70 · dl 2. Данные: `convert/corpus_run_v2.json`,
  per-article флаги в `out/*.flags.txt`. Правка (б) `.epi.caps` разморожена, НЕ сделана (render/темы).
  Сайт не тронут (всё локально).

- **2026-06-12 (утро — отлов по скринам оператора: сноски):** оператор скринами вскрыл три слоя
  проблем сносок; все починены end-to-end (конвертер+render), корпус перегнан, 352 вью пересобраны.
  (1) **Дубль маркера в теле** («1. ↑ *1Это…») — 142 сноски/24 статьи: маркер «*N»/«N.» голым
  текстом; срез по совпадению с финальным номером, с защитой `(?!\d)` (ловила «22 буквы»→«2 буквы»
  на 186A) и без трогания звёздно-очищенных. (2) **Пер-секционная нумерация** (11 статей,
  дневниковый формат 217A: якоря `#s7-fnN`, парсер брал первую цифру id → 17 определений под
  `[^7]`) — `_fn_number` берёт `fn(\d+)`/последнюю группу; при коллизиях глобальная перенумерация
  в документ-порядке + карта якорей `FN_ID_MAP` (def-id → финальный номер), ссылки в теле резолвятся
  через неё. (3) **Звёздные сноски 182** («*», «**», «***») — РАСШИРЕНИЕ ФОРМАТА (внесено в
  `zml3/SPEC.md` §4): `[^*]:`-определения и `[^*]`-маркеры в теле, первоклассные (кликабельны в обе
  стороны, id `star<n>`; 9 статей корпуса); render: звёзды без скобок/двоеточия, дефолт анонимной
  «См. также:» остался для безмаркерных. Также 📏-добор: тело сноски-СТИХА (построчные `span.vsub`)
  → continuation-строки; render рисует continuation через `<br>` (было схлопывание в строку).
  Якорь-маркеры `<a href="#fnN">**</a>` без классов резолвятся через FN_ID_MAP в `link()`.
  Коридор покрытия стабилен (339/352). Канал отлова — скрины оператора в чат, работает отлично.

- **2026-06-12 (утро — НОСИТЕЛЬ ВЬЮ = ПРОДАКШЕН-ПУТИ; поручение оператора):** локальная рабочая
  копия теперь повторяет структуру паблика В ТОЧНОСТИ: 352 `.zml` + 352 `.view.html` лежат в
  `yaniktoim/docs/art/` рядом с оригиналами (как поедут при выкате), темы — `docs/themes/` (уже
  были). **Отладочный каталог `docs/zml-review/` УДАЛЁН** (его роль кончилась; тест-стенд
  `zml3test.zml` сохранён в `cms-revival/zml3/teststand/`). Модель работы: доводим сайт НА МЕСТЕ
  в живом дереве `docs/`, публикация — одним пушем по готовности (push.bat НЕ запускать до «go»).
  Локальный просмотр: preview :8098 = корень `docs/` (главная/разделы — как на паблике; вью —
  `/art/<id>.view.html`, внутри ссылка «Оригинал»). Пересборка вью: `cp convert/out/*.zml
  docs/art/` + `node editor/build_views.mjs` (пишет туда же; БЕЗ переноса и правки ссылок).
  Открыто: механизм навигации каталог→вью (вопрос оператору — Ф6/Ф9-вид индексов).

- **2026-06-12 (утро — Q1 РЕШЁН: навигация каталог→вью СДЕЛАНА):** вердикт оператора (06:19): по
  названию статьи — новый вид (ZML-вью с переключателем темы/ширины), внутри — ссылка на старый HTML,
  она остаётся и после перепубликации. Сделано: (1) 7 индексов разделов (`best/confront/cyberson/
  dabudet/dreamon/other/shoshana`) — все 352 title-ссылки `../art/<id>.html` → `<id>.view.html` (sed);
  (2) `render.js`: `resolveInternals` (`[[ID]]`) и prev/next → `<art>.view.html` (вью соседствуют с
  оригиналами в одной папке); зохар-ссылки (внешний сайт) и «Оригинал» (`{{NUMBER}}.html` в
  `template_view.html`) НЕ тронуты; (3) 352 вью пересобраны `build_views.mjs`. Сверено в preview :8098:
  индекс ведёт на вью, prev/next/`[[ID]]` внутри вью — на вью, «Оригинал» — на старый `.html`, обе
  версии 200. Хвосты: `songs/index.html` (220 ссылок) пока на старые `.html` (мигрирует с `[shir]`);
  Pagefind при reindex станет видеть `.view.html` — продумать дубли выдачи перед выкатом.

- **2026-06-12 (утро — ДЕВ-БАР ВЬЮ → ПОЛНОЦЕННЫЙ VIEWBAR):** оператор указал (скрин), что чёрная
  плашка «ZML-предпросмотр» — дев-обвязка без дизайна, не похожа на паблик. Перепроектировано:
  `template_view.html` — липкий тёмный бар снят, вместо него `nav.viewbar` ПЕРВЫМ элементом внутри
  `.wrap` (утилитарная строка над крошками, прижата вправо, мелкий кегль): селекты Тема/Ширина +
  «Оригинал» + **кнопка «✎ Править» (disabled — редактора ещё нет, Ф8; при логоне станет активной)**.
  Дев-тексты убраны: `<title>` без «— ZML-предпросмотр», колофон → «Редакция: {{LAST_EDIT}}» (И.И.-метка
  жива). Структурный CSS в шаблоне; типографика/цвета — per-theme в 5 темах (A/swiss muted+accent-hover,
  B в той же идиоме, cyberpunk mono-uppercase+cyan, ar-deco geo-uppercase+gold; тёмным добавлен
  `option{color/background}`); компонент внесён в `THEME_CONTRACT.md`. CSS_VERSION → `20260612-01`,
  темы скопированы в `docs/themes/`, 352 вью пересобраны, 4 темы осмотрены в preview (A/cyber/B/deco).
  NB: preview-скриншоты снова висли — лечится рестартом preview-сервера (известный квирк).
  **Доводка (06:43, по скрину оператора):** как в оригинале, крошки БЕЗ названия статьи («Главная ›
  Раздел»), viewbar посажен в ту же строку (`header.breadcrumbs` = flex: `.crumbs` слева, `nav.viewbar`
  справа, общая линейка) — вся шапка-обвязка занимает 1 строку. Вью пересобраны, A/B сверены в preview.

## Этап: дизайн-ревью топ-10 по примечаниям (поручение 2026-06-12, 09:00)
**Что.** 10 статей корпуса с НАИБОЛЬШИМ числом примечаний (отбор по `[^N]:`-определениям в `.zml`):
общее дизайн-ревью КАЖДОЙ + предметный регресс-чек ПОСТРАНИЧНО против старого HTML (оригинал =
оракул). Фокус — аппарат сносок (плотные кейсы), но смотреть всё (вкл. свежую общую рейку стихов).
**Список (фн/арт/раздел, утверждён 09:05):** 37·163A·shoshana / 33·12FA·shoshana / 33·12F·dabudet /
29·1CM·shoshana / 23·239·best / 23·163·shoshana / 20·341A·cyberson / 20·28MA·dreamon / 19·12L·shoshana /
18·16P·shoshana. **Исключены оператором:** 237 (пилот, уже облизана) · 217A (большая однотипная — НЕ
сканировать целиком, способ выборочной проверки СОГЛАСОВАТЬ ОТДЕЛЬНО позже).
**Метод.** ТОЛЬКО Chrome MCP (реальный Chrome ~1920, [[feedback_design_review_real_browser]]), НЕ
встроенный превью. Сервер: :8098 (preview.bat оператора уже работает; если упадёт — поднять снова).
На статью: открыть `art/<id>.view.html` и `art/<id>.html`, идти парой сверху вниз (скролл-экранами),
сличать сноски (маркеры в теле, тела, нумерация, backref, popup), эпиграфы, музыку, стихи, заголовки.
**Качество снимков:** следить; если Chrome-MCP-скрин мутный/обрезанный — fallback: headless
полностраничный → Pillow-нарезка → JPEG-сегменты (~1600px, `convert/viewshots.py` — формат отработан).
**Прогресс:** `cms-revival/design_review/fn_review_10_progress.md` — 10 пунктов, отмечать по ходу.
**Гейты оператора:** (1) список 10 подтверждён ДО старта; (2) НИКАКИХ правок без подтверждения —
в конце выдать раскрытый список предлагаемых изменений, применять только по «go».
**ВЫПОЛНЕНО (2026-06-12, 09:07–09:50):** все 10 просмотрены (вью целиком по JPEG-сегментам +
структурный JS-дифф против оригинала в Chrome). Находки и сводный список правок (A конвертер ×9 /
B render ×2 / C канон-вопросы) — в `fn_review_10_progress.md`; ждёт «go» оператора. Метод-находки:
Chrome-MCP скриншоты виснут (CDP timeout) → headless JPEG-сегменты ≤800px; сегмент >~550 КБ или
повторное чтение того же файла контекст-лоадер ужимает до нечитаемости → свежие имена + дорез
половинок. 217A — способ выборочной проверки согласовать отдельно.
**РАЗВИЛКИ РЕШЕНЫ (12:27, все 12):** группы сносок а-ля MediaWiki (`[^имя.N]` + `[refs=имя glyph=…]`
на авторском месте) · маркеры в `title:`/H1 (24 ст.) · `###` (h3+h4) · `[toc]`-маркер (только где
был, дефолт open) · `dates:` · ручная нумерация → `[num]` (+чек сносок до/после) ·
closing/banner→`[cry]`, coda→`[epi]`, dedication→`[epi kind=dedi]`, tags/dateline→`[line]` ·
лейбл «ЭПИГРАФ»→`[epi]` (35 ст., постатейный чек) · kicker→`[sub]` подтверждён · `[ul marker=…]` ·
диалоги прозой. Полный реестр — `design_review/fn_review_10_progress.md` §«РЕШЕНИЯ ОПЕРАТОРА».
**«GO» ПОЛУЧЕН (12:33). Чек-лист реализации — ниже, отдельным этапом.**
- **2026-06-12 (12:40–13:30, РЕАЛИЗАЦИЯ fn-ревью ✔ P1–P6):** Все 12 вердиктов воплощены за один
  проход. **Спека → v2** (группы §4.1, [^N]-в-title §1.2, ### §1.4, [toc] §1.5, [line]/[ul]/[num]/
  dedi §2.2–2.4, dates: §10, лейбл-правила §12). **Render:** парсер групп `[^имя.N]`+`[refs glyph]`
  (вывод на авторском месте, фолбэк-хвост с notes_title; orphan per group), сквозной `seenRefs`
  (дубль-маркеры теперь -extraK документно), inline-резолв notes_title и title→H1-sup (чистка в
  `<title>`/крошках), TOC только по маркеру (дефолт open; вложенный ### 2-м уровнем), [line] вне
  dropcap-цепочек (B-тема :not(.line)), [ul] `--marker`→`li::marker`, [num] li[value]+num-paren,
  [epi kind=dedi|aphorism]-классы, dates: в byline; CSS 20260612-02; THEME_CONTRACT + 5 тем +
  оба шаблона; тест-стенд `zml3/teststand/` (+build_test.mjs, 5 тем зрением). **Конвертер:**
  бэкап `*_v2_pre_fnreview.py.bak`; мульти-контейнерные сноски → группы s1..sN с пер-групповой
  АВТОРСКОЙ нумерацией и глифами серий (¤¶^‡/¤-1CM, */#-341A, юникод-∗ 239 → MARKER_CLS обобщён);
  тела сносок сериализуются ДВАЖДЫ (кросс-рефы [^s2.1] после полной FN_ID_MAP); [refs] эмитится
  на месте первого айтема (вкл. li внутри ol/ul); notes_title только при одиночном контейнере +
  guard «последний ##»; image: из реального src (196 ст., потерь 0); h3/h4→###; poemsub→[subsec];
  span.n→[num] (29 ст.); div.quote→[quote]; лейблы «Цитата»/«Музыка под настроение»/«ЭПИГРАФ»
  снимаются (ЭПИГРАФ-с-маркером — оставлен ##, спорное); муз-рефы отдельной строкой; роли
  closing/banner→[cry], coda→[epi], dedication→dedi, tags/dateline→[line] (шапочный диапазон → FM
  dates:, 163A); [toc]-эмит 79 (collapsed 1); ul→[ul] (14; прочие ul = TOC/сноски); 28MA: H2
  муз-секции из section.music-summary восстановлен (лейблы вставок «Песнь Ступеней» оказались
  АВТОРСКИМИ в живой HTML — находка A5б снята). **Прогон:** 352/0 ошибок, коридор 339 = база v2
  (4 сдвига к единице), сноски 962→962 (дельта 0), `corpus_run_v3.json`; вью пересобраны, спот-чек
  зрением 163A/12F/1CM/341A/217A. **Спорные — оператору:** kicker-даты→[sub] (дубль byline-даты);
  ЭПИГРАФ-с-маркером 16J/1CI/571; голый реф-маркер строкой после [mus] (11 ст., эстетика).

- **2026-06-12 (13:48–14:06, спорный кейс 1 РЕШЁН — дата-строки):** инвентаризация шапочных
  дата-строк: 86 статей, 4 способа обработки (4 киккер-даты→[sub]-дубль; 16 .date ВЫБРОШЕНЫ;
  65 [line]; 1 dates:). **Решение оператора (13:59): FM `dates:` ОТМЕНЁН (вердикт 6 снят)** —
  ВСЕ авторские дата-строки → `[line kind=dateline]` на своём месте в теле (как в оригинале);
  byline всегда рисует manifest-дату, дубль на странице — норма («где явно в тексте было — пусть
  будет дважды»). Спека v2 §1.1/§2.3/§10/§11 поправлена, render откатил dates:, конвертер:
  киккер-даты (детект _DATEISH_RX <40 симв.) и .date В ЛЮБОМ месте → [line kind=dateline].
  **Попутно вскрыт и починен СТАРЫЙ дефект (был и в v2):** сборники стихов с заголовками ВНУТРИ
  section.poem (184A) плющились в один [poem] — терялись h2-заголовки стихов и их даты; теперь
  спуск по ролям: 184A 0.962→1.002, вернулись «## Победителям над розами {p2}» + даты стихов
  dateline-строками. Прогон: 339 в коридоре (хвост прежний), сноски 962→962, dateline у 104
  статей, dates: 0. Вью пересобраны; 12P сверен зрением с оригиналом оператора.
- **2026-06-12 (14:25–14:33, ГЛОБАЛЬНЫЙ ИНВАРИАНТ «ничего не плющится»):** на вопросе «почему
  поэма в эпиграфе 571 сплющена» оператор закрыл класс проблем НАВСЕГДА: **конвертер не
  схлопывает НИКАКИЕ авторские переносы; деление verse/prose — только стилевое; склейка лишь
  косметического whitespace разметки (BR-сентинел) и по явному правилу оператора (таких нет)**.
  СПЕКА §9 переписана (significant/flow упразднена: \n значим ВЕЗДЕ; номинально однострочные
  зафиксированы: ##, [line], поля [mus]). RENDER: общий linesToHtml — проза/[quote]/[epi] любого
  kind/[num]/[ul]/[sub] рисуют \n→<br>. КОНВЕРТЕР: _groups_text-хелпер; para_blocks/emit_epi/
  emit_quote/ol→[num]/[ul]/span.n-захват сохраняют строки; + вложенный <p> в сериализуемом блоке
  = граница абзаца (двойной сентинел — вступление 571 слипалось с куплетом). Прогон: 339 в
  коридоре (хвост прежний), сноски 962→962; затронуто 62 статьи; зрением: 571 = оригинал
  (куплет построчно), 217 — проза без лесенок. Снят и флаг «переносы внутри пункта [num]
  сплющены». *(Идея [quote kind=verse] отпала — verse-класс остался бы чисто стилевым.)*

- **2026-06-12 (14:49–14:59, кейс 2 в работе — 16J):** оператор подтвердил «Эпиграф» в 16J
  отдельным заголовком; вскрыт вопрос «куда делись пункты 1.–6. из TOC». Скан корпуса:
  беззаголовочные TOC-пункты оригинала — всего 6 статей/11 пунктов (16J — единственная
  системная: 6 номерных секций-якорей; 158/17QA «музыка», 2BG «эпиграф», 0BH/2BSA — служебные
  ссылки на корень). **Решение по 16J (14:55): это нумерованные АБЗАЦЫ** → конвертер ловит
  висячий номер-метку СНАРУЖИ абзаца (`span.num-h` + сосед-`<p>`) → [num]-пункт с авторским
  номером; + слияние СОСЕДНИХ [num]-блоков (тезисы 16J живут в отдельных section; бонус — 06I
  слил пункты 4–6 из раздельных ol). Прогон: 339/962 без регрессов, затронуты только 16J/06I;
  16J сверен зрением. Судьба остальных 5 единичных TOC-пунктов — следующий вопрос оператору.
- **2026-06-12 (15:00–15:05, формат-добавка `[num kind=toc]`):** оператор: «нумерованный список
  абзацев минимальной правкой превращается в список оглавления». Сделано: `[num kind=toc]` —
  пункты получают якоря (`id="num-N"`) и входят в TOC своими номерами («1.», «2.»…) на
  документном месте (спека §2.2; render: ctx.toc.push из renderNum). Конвертер ставит kind=toc
  АВТОМАТИЧЕСКИ, когда оригинальный TOC ссылался на якоря этих пунктов (st.toc_hrefs);
  merge соседних [num] — только при одинаковой голове. Прогон: 339/962, изменился только 16J;
  его TOC теперь 1:1 с оригиналом (Предисловие · Эпиграф · 1.–6. · Музыка · Пояснения),
  сверен зрением. **15:04–15:06, добавка: `kind=toc|Пункт`** — опц. префикс TOC-метки
  («Пункт 1», «Пункт 2» — БЕЗ точки на конце, уточнение 15:06; с пробелами —
  `kind="toc|Мой пункт"`); без префикса голые «1.», «2.». Спека §2.2 + render; проверено
  стендом. Голые числа в TOC по корпусу: только 16J (решён) и 253 (там «1»/«2»/«3» — настоящие
  h2, уже переносятся). **15:08, откат автодетекта:** оператор уточнил — `kind=toc` РУЧНАЯ
  опция, деф. ВЫКЛ; автопроставление по toc_hrefs убрано из конвертера, 16J перегнан с голым
  [num]. **15:13: оператор оставил 16J на СВОЮ ручную правку после публикации** (включит
  kind=toc сам в эпоху CMS-edit; адресное исключение в конвертере не ставим).
- **2026-06-12 (15:14–15:25, маркеры сносок В ПОЛЯХ [mus] — кейс Gregorian[12], 16J):** оператор
  показал: в оригинале маркер — рабочая сноска с popup прямо на поле трека. Сделано: конвертер
  больше НЕ выносит маркеры отдельной строкой (кейс 3 закрыт: голых строк-маркеров 0) — sup/fnref
  замещаются текстовым `[^N]` НА МЕСТЕ в полях; render: парсер трек-строки сменён с regex на
  срез скобок+split (поля с `[^N]` не ломают), поля резолвят сноски (fld=resolveFootnotes∘escape),
  data-label остаётся плоским (маркер на label — флаг, 8 ст.). Заодно: «▾» в чистку артефактов;
  **поля треков больше не переворачиваются** — b-узел и первый кусок комбо-строки идут в
  author-слот (render «author — title» = авторский порядок: 16J «Gregorian[12] — Moment of
  Peace», 12L «Elton John — Circle of Life[5]»); дедуп уважает author-only треки. Спека: [mus]
  в §2-таблице. Прогон: 339/962, изменилось 70 статей (sweep-своп полей), 0 ошибок; 16J сверен
  зрением (popup работает).

- **2026-06-12 (15:31–15:41, регрессы 158 — три класса):** (1) **C10-извлечение терминов
  ЗАПУЩЕНО:** `span.term/leit` оригинала → `{term|…}`/`{leit|…}` (по корпусу только 158, 20 шт.;
  отложенный пункт «конвертерное извлечение C10» частично закрыт — leit в корпусе нет).
  (2) **Принцип «идём за оригиналом» для TOC:** муз-блок, бывший ПУНКТОМ оригинального TOC без
  заголовка → конвертер эмитит настоящий `## <текст пункта> {якорь}` (158/17BA/191/2CI/32F);
  render: [mus] сразу после ## → `.has-heading` (этикетка карточки скрыта — CSS был готов во
  всех 5 темах). (3) **«См. также» (p/div.seealso ± p.tag):** → анонимная сноска `[^]: …`
  (render рисует «См. также: …»; 30 статей; 20 пустых огрызков «См. также:» опущены с флагом;
  одиночный p.tag → [line kind=tags]). Сноски 962→995 (+33 анон-см.также), оракул учит
  дефолт-префикс рендера (42DA-шум устранён), коридор 339 = база. Скрин-сверка 158 ✓.

- **2026-06-12 (15:41, поручение + закрытие сессии):** оператор: «span.term только в 158 ≠
  термины выделялись только в 158 — копни глубже и выясни истину, но НЕ СЕЙЧАС» → TODO в
  «Открытые задачи». Снимок для холодного старта — в «ТЕКУЩЕЕ» (открытые вопросы 1–5 + TODO);
  целостность roadmap проверена (восстановлен потерянный заголовок Этап-секции, устаревший
  «ШАГ 3 — ПЕРВЫМ ДЕЛОМ» помечен ЗАВЕРШЁН). Рабочая копия docs/art синхронна out/ (352 вью
  пересобраны последним прогоном); push.bat по-прежнему НЕ запускался.
- **2026-06-12 (19:21–19:32, вопрос 1 РЕШЁН + ВНЕДРЁН — TOC-пункты на беззаголовочные
  блоки):** вердикт оператора 19:21: «если эпиграф — пункт оглавления, блок [epi] без
  собственной надписи + подраздел ## Эпиграф перед ним; по аналогии всё остальное».
  Конвертер: (1) хелпер `_toc_heading` — пункт оригинального TOC на беззаголовочный блок →
  `## <текст пункта> {якорь}` (обобщение муз-правила 15:31; точки вызова: is_epi-ветка +
  generic-спуск); (2) h2-эпиграф-ветка: заголовок-пункт-TOC больше НЕ подавляется — эмитится
  `##` перед [epi] (контент по-прежнему → [epi]); (3) bugfix по дороге: span `anchor-hint/
  anchor-note/anchor/up` «↑ наверх» тёк в заголовки из оригинала (18T «Музыка под настроение
  ↑ наверх» — тёк и ДО правки) → дроп в inline() по классу+тексту. Прогон: 352/0, коридор 339 =
  база, сноски 995 = база, TOC-правило сработало в 32 местах (24 статьи вернули «## Эпиграф»,
  0BH вернул и «## Текст», 239 — ДВА «## ЭПИГРАФ» — в оригинале реально два пункта). 2BSA
  «Точка Невозврата» ведёт на сам H1 → в наш TOC не переносится (заголовок есть — это title).
  Вью пересобраны; сверено: 2BG/0BH/18T TOC = полные копии оригинальных, все якоря на месте.
  Сводка прогона = `corpus_run_v4.json`.
- **2026-06-12 (20:09–20:25, вопросы 2 и 3 РЕШЕНЫ):** Q2: эпиграф-с-маркером — «в 1CI и 571
  проблем не вижу» → все три (16J/1CI/571) остаются `## Эпиграф[^N]` как есть, [epi]-обёртку
  не добавляем. Q3 (вскрытие по скринам оператора, первая же статья 654): «канон Автор —
  Композиция» был мифом для комбо-строк — конвертер кладёт первый кусок комбо в author-слот,
  render собирает «author — title» → визуально = оригинал («Пачка Сигарет — Кино»), но слоты
  семантически лгут; канон реально работал только при раздельных полях (158 → переворот).
  «Песнь Ступеней» (220 карточек) канонизирует «Название — Автор» единообразно → ВЕРДИКТ:
  привести ВСЕ трек-карточки статей к «Название — Автор» + сверить полноту songs-раздела
  («Пачки Сигарет» там нет) + найти/дозаполнить недостающие url треков (237…) + плашки тем
  приблизить к оригиналу (название **жирным**, автор обычным, значок ►). Пакет добавлен в
  § «Фаза сборки» (реализация по «go»).
- **2026-06-12 (20:27–20:47, вопрос 4 РЕШЁН + ВНЕДРЁН — маркер сноски на label [mus]):**
  вердикт оператора: «везде где есть сноска в оригинале — должна быть и в zml, надписи не
  дублируются». Сделано: (1) render — label с `[^N]` → реальная строка
  `.music-group.has-label-row > .music-label` (сноска резолвлена; ::before прячется);
  (2) темы ×5 — `.music-label` стилизован идентично `::before` (absolute в том же месте,
  ноль сдвигов), CSS_VERSION → 20260612-03, синк в docs/themes; (3) конвертер —
  `_label_marker_to_heading`: при `##` с той же надписью прямо над [mus] маркер переносится
  в `##` (11H «Песнь Ступеней[^7]»); дефолт-лейбл «…настроение» с маркером больше НЕ
  опускается (1C8C — маркер [^4] гиб). ПОПУТНО два скрытых бага: (а) блок-парсер render не
  матчил `]` внутри квотированного label → `[mus="…[^10]"]` у 7 статей выводился СЫРЫМ
  ТЕКСТОМ в абзаце (всегда); фикс ATTR_PART с непересекающимися альтернативами — первая
  версия фикса дала катастрофический бэктрекинг (build повис, убит), вторая (`[^\]"]`) линейна;
  (б) сам перенос маркеров. Прогон: 352/0, коридор 339, сноски 995; вью пересобраны; чек:
  12F=3 строки-этикетки с sup, 1C8C/2AE/2BI/2C3/2C7/2CG по 1, 11H — sup на ##, сырых `[mus=`
  по корпусу 0. Спека §2 [mus] + THEME_CONTRACT дополнены.
- **2026-06-12 (20:53–20:57, вопрос 5а РЕШЁН + ВНЕДРЁН — возгласы .shout/.call):** принцип
  оператора «смотрим на оригинал»: в 16J «ТОЛЬКО ВМЕСТЕ, МЫ - СИЛА!» = p.shout (центр, болд,
  акцент) — выделена → конвертер: классы shout/call/center-call на p/div добавлены в
  [cry]-правило (вердикт 8). Корпус-скан носителей: 13 статей блочных (0BC/11T/12FA×2/12P/
  163/16J/17DB×2/1BJ/21L/23TA/24I/2B6), все получили [cry]; span-носители ВНУТРИ бегущих
  абзацев (1C8B/2BSA/43O) не тронуты — inline-выделение, та же серая зона, что вопрос 6
  (термины). Прогон 352/0, коридор 339, сноски 995; вью пересобраны.
- **2026-06-12 (21:08–21:20, кейс 59C: киккеры-фантазии + см.-также НА МЕСТЕ):** оператор
  сверил 59C с proza.ru-первоисточником: киккер «Да будет Свет!» — фантазия верстальщика
  (имя раздела), в первоисточнике её нет → удалён из 59C.html (ручная правка источника по
  слову оператора). Вскрылись 3 статьи с kicker-спаном ВНУТРИ h1, ломавшим title (59C «Да
  будет Свет!И это только начало», 1B5A, 416B — там киккер дублировал сам заголовок) →
  конвертер: kicker-спан в inline() снимается с флагом, титулы чисты. Скан 40 standalone-
  киккеров: 20 = точный дубль раздела из крошек (59C-паттерн; ВОПРОС оператору открыт),
  20 = настоящие авторские надзаголовки («Стенограмма урока · Шамати 3», «Пьеса в 2 актах»…)
  → остаются [sub]. + **См.-также возвращены НА АВТОРСКОЕ МЕСТО** (вопрос оператора «почему
  уехали вниз»: правка 15:31 утаскивала p.seealso анон-сносками в хвост) → конвертер эмитит
  видимую строку «См. также: [[ID]]» на месте; 33 строки по корпусу, сноски 995→962 (ровно
  −33 анон), коридор 339, 352/0. Вью пересобраны; 59C сверен: 2 см.-также в начале на месте,
  настоящая сноска [^1] осталась сноской. **Вердикт по дублям (уточнён 21:22 после
  рассинхрона Q/A):** 1AQ — фантазия адаптатора → киккер удалён из 1AQ.html; затем
  СИСТЕМНО: киккер = точный дубль названия раздела из крошек → конвертер НЕ эмитит
  (st.sec_names из `../<slug>/index.html`-ссылок; Python lower() фолдит кириллицу,
  17O «ДА БУДЕТ СВЕТ!» ловится). Прогон: снято 19 дублей, 20 настоящих надзаголовков
  остались [sub]; 352/0, коридор 339, сноски 962; вью пересобраны.
- **2026-06-12 (21:25–21:33, вопрос 5г РЕШЁН + ВНЕДРЁН — [dlg] активирован):** оператор не
  признал «вердикт 12» про диалоги-прозой своим (строка была прицеплена к [ul]-вердикту
  оптом) → новый вердикт: «опознанный диалог как минимум помечать». Сделано: `[dlg]`
  активирован из резерва спеки (§2-таблица + определение; «прозой-тире» снят); render —
  PAIRED+dlg, renderDialog → `div.dialog` + `<p>`-реплики (вид наследует прозу, контракт
  дополнен: тема МОЖЕТ стилизовать); конвертер — DIALOG_CLS-ветка поднята ВЫШЕ p-ветки
  (p.speaker раньше перехватывался generic-абзацем), ран соседних диалог-узлов → один
  [dlg], p-узлы через para_blocks, не-абзацные внутренности → фолбэк-проза с флагом.
  Прогон: 352/0, коридор 339, сноски 962; [dlg] в 29/30 статей (3CM — фолбэк), вью
  пересобраны (165A: 2 div.dialog, 179: 1). NB 335: спикер-строки форумной перепалки
  («Зубастег написал(а):») дают однострочные [dlg] между [quote] — глянуть при спот-чеке.
- **2026-06-12 (21:35–21:42, 35Q: богатые li — эпиграф+цитата спасены):** оператор на
  спот-чеке 5д: в 35Q ol.celems два блока (эпиграф-стих Лукъяненко + длинная цитата
  Реутова) плющились в сплошной текст — ol-ветка гнала ЛЮБОЙ li через _mark_lines.
  Фиксы конвертера: (1) ol/ul с «богатыми» li (blockquote/div-с-p/заголовки/вложенные
  списки внутри) → НЕ [num]/[ul], развёртка поблочно walk'ом (номера/маркеры списка
  опущены с флагом); (2) голый inline-лейбл `<strong>Эпиграф</strong>` на блочном уровне →
  epi_pending (вердикт 9 обобщён с заголовков); (3) quote-классы по подстроке
  (`longquote`, кроме `*label*`) → [quote]; (4) label-строка «Цитата» отдельным узлом
  (div.qlabel) снимается. Итог 35Q: [epi kind=verse cite="С. Лукъяненко…"] со строфами +
  титул строкой + [quote] с абзацами. По корпусу развёртка зацепила ТОЛЬКО 35Q
  (коллатерали нет); 352/0, коридор 339, сноски 962; вью пересобрано.
  **Вторая волна 35Q (21:41–21:48, по скрину оператора):** (5) треки-details ВНУТРИ li
  («Коллекция Целемов» п.2/3 Кипелов/Ария плющились в текст с ▶↗) → `details`/`iframe`
  добавлены в rich-детект `_rich_li`; развёртка теперь сохраняет НУМЕРАЦИЮ плоских пунктов
  ([num] с явными номерами, честные дыры на местах плееров: 1. → [mus] → [mus] → 4.–8.);
  (6) шапочный `div.sub` → [sub] («Коллекция Целемов · АЯ ПЧФ»; бонусом 1CIA «(ивр.
  РАДОСТЬ ОТВЕТНОЙ ЛЮБВИ)»); (7) `p.resume` → [cry] («Резюме: ВО ВСЕЙ ВСЕЛЕННОЙ ПАХНЕТ
  НЕФТЬЮ»). Прогон 352/0/339/962, изменились только 35Q+1CIA, вью пересобраны, 35Q сверен
  зрением (viewshots). NB: встроенные в список плееры несут дефолт-этикетку «Музыка под
  настроение» (в оригинале её в списке нет) — мелочь, оператору глянуть.
- **2026-06-12 (21:49–21:55, карта флагов + поручение «визуальный разбор хвоста»):** на
  вопрос «272/2BS/32K/1AM — единственные ли „глянуть глазами“?» собрана сводка флагов
  корпуса (`convert/flags_summary.txt`): 199/352 статей с флагами, большинство
  информационные; реальный «глянуть»-хвост: оракул 12 (0BH 0.811 … 215B 0.968) + 336
  (1.081, выше коридора) + стих-цитаты 4 + диалог-фолбэк 3 + h4 2 = 22. **Поручение
  оператора 21:53:** разобрать их самостоятельно зрением (оригинал ↔ вью, Chrome MCP,
  JPEG <500КБ, хорошее качество), чинить механизм по «духу и букве» ZML3 без визуального
  регресса, прогресс в отдельном md. Создан живой трекер
  `convert/visual_tail_review_001.md` (метод, процедура, чек-лист 22, драйвер прогона,
  инварианты, контекст дня) — готов для холодного старта; шаг вписан в «ТЕКУЩЕЕ».
- **2026-06-13 (07:16–07:30, ВИЗУАЛЬНЫЙ РАЗБОР ХВОСТА начат — 35Q, БАГ СНОСОК починен):**
  первая статья трекера (35Q, оператор: «проверять первой»). Сверка оригинал↔вью по JPEG-
  сегментам (viewshots.py). **Найден и починен КОРПУСНЫЙ баг сборщика сносок:** 9 сносок 35Q
  (`section#notes.notes` > `div.notelist` > `div.note#fnN`+`span.nnum`+`a.upref`) схлопывались
  в ОДИН анонимный `[^]:` → маркеры [^1..9] в теле не резолвились, аппарат сносок терялся.
  Причина: обёртка `div.notelist` (класс ∈ `NOTES_CONT_CLS`) сама проходит `_is_note_item`
  через `_has_backref` (upref у потомков) → `gather` глотал её одним айтемом вместо спуска в 9.
  Триггер — вложенность `section.notes`>`div.notelist` (35Q; у 075/152/212 обёртка — верхний
  контейнер → работали). Фикс: `gather` спускается в вложенную обёртку класса `NOTES_CONT_CLS`.
  Прогон 352/0, коридор 339=база, **сноски 962→970** (+8: 35Q 1 анон→9 нумер., объяснимо),
  `cmp` out↔deployed = изменилась ТОЛЬКО 35Q. Деплой+вью+viewshots: «Пост скриптум» = 9
  нумерованных (1–9) с backref ↑ и ссылками 1:1 с оригиналом. **Сноски без регресса ✔.**
- **2026-06-13 (08:56–09:06, 35Q СПИСОК — формат-добавка «вложенный `[num]`»):** вердикт
  оператора: оригинал = единый сквозной список 1–10 (две `<ol class=celems>`: 1–2 и `start=3`→
  3–10), пп. 4,5 = встроенные ПЛЕЕРЫ со своими номерами → «[mus]/[epi] внутри [num]» (вариант A,
  бэкап отката `*_v3_pre_richnum`). **РЕАЛИЗОВАНО:** конвертер rich-`<ol>`-ветка → ОДИН `[num]`
  со сквозной нумерацией (honor `start=`), богатый пункт = ВЛОЖЕННЫЙ блок под номером
  ([epi]/[quote]/[mus]); render `renderNum` рисует rich-`[num]` (гейт `NUM_NESTED_RX`): бьёт по
  «N.»-маркеру, тело пункта = вложенный блок через `parseMain`+`renderBlocks`, у `[mus]` плашка
  «Музыка под настроение» гасится (`hasHeading`). Обычный `[num]` (29 ст.) — прежним путём,
  zero-regress (nested-[num] по корпусу только 35Q). Прогон 352/0, коридор 339, сноски 970,
  изменилась ТОЛЬКО 35Q. Зрением 1:1 с оригиналом: 1.[epi]/2.цитата под номерами, 3. строка,
  4./5. плееры (номера в жёлобе, без плашки), 6–10 строки. Спека ZML3 §2.2 ([num]) — дополнить
  «вложенные блоки в пункте» при сборке. **35Q ✔ полностью (трекер: status ✔).**
- **2026-06-13 (09:14–09:23, ВИЗ.ХВОСТ 2/22 · 0BH — ПОТЕРЬ НЕТ):** оракул-хвост cov 0.811
  (самый низкий) = ШУМ метрики: оригинал держит 16 popup-спанов сносок (`span.fn-popup`,
  скрыты CSS, но в DOM → `visible_text` считает) + полный список `<li>` = текст сносок дважды;
  ZML — раз → знаменатель раздут. Тело/эпиграф/17 определений/дата/TOC на месте (viewshots
  view↔orig). Перенумерация сносок ZML (сквозная, компактизация пропуска fn-2) ИСПРАВИЛА
  рассинхрон оригинала (маркер «8» вёл на пункт 7) → [^7]=пункт 7. **⚠ ОТЛОЖЕНО оператору** (по
  правилу «блокеры записывать, не спрашивать»): анон-сноска Wikipedia «Яблоко раздора»
  (`li.note-anon`, без маркера) → орфан `[^17]` (ZML3 A3: номер+, backref−); оригинал =
  анонимная без номера. Голого анон-вида (без ярлыка «См. также») в формате нет — оставить A3
  или новый рендер-путь. Затрагивает 0BH+184. Кода не трогал.
- **2026-06-13 (09:26, ПРАВИЛО):** вердикт оператора — вопросы-блокеры визуального хвоста НЕ
  задавать, а записывать ⚠ в трекер и идти дальше; оператор разберёт пачкой. Внесено в
  `convert/visual_tail_review_001.md` (§Задача).
- **2026-06-13 (09:27–09:39, ВИЗ.ХВОСТ 3/22 · 42DA — 2 РЕГРЕССА ПОЧИНЕНЫ):** оригинал 26000px =
  ПУСТОЙ Telegram-эмбед (не текст), потерь нет. (1) **audio:** `p.audiopost` ложно ловился audio-
  контейнером (класс ⊃ «audio» + текст «Аудио…») → t.me-ссылка уезжала в FM `audio:`, «Аудиопост —»
  висло с тире + лишний 🎧. Фикс: `audiopost` исключён из audio-детекта (класс+текст) — ссылка
  вернулась inline, 🎧 снят (yanik-meta: «не плеер, а плоская ссылка»). (2) **seealso:** `div.seealso`
  с неск. `<p>` склеивался в одну строку → ГРУППИРОВКА (label/marker-`<p>` начинает строку, контент-
  `<p>` прилипают, пустые отброшены); правка улучшила 12PB/1CI/215A/34O/42DA. Баг по пути: локальная
  `LABEL_CLS` затеняла глобальную (UnboundLocalError) → `SEEALSO_LBL`. Прогон 352/0, коридор 339,
  сноски 970, changed=5; cov 42DA 0.855→0.968; зрением 42DA+1CI 1:1. Деплой+вью 5.
- **2026-06-13 (09:42–09:46, ВИЗ.ХВОСТ 4–5/22 · 53KA + 1C8A):** **53KA** — потерь нет: только
  [mus] 5 треков (Моя Звезда…Небо Тебя Найдет) 1:1, cov 0.885 = шум (крошечная статья). **1C8A** —
  ПОЧИНЕН двойной дефис трека: оригинал `<span.track-artist>Lady Gaga</span><span.track-title>—
  Poker Face</span>` (тире вписано в название) → конвертер срезал лид-дефис у автора, но не у
  названия → render «Lady Gaga — — Poker Face». Фикс: симметричный strip `^[—–-]\s*` названию
  (стр.572). Теперь «Lady Gaga — Poker Face» 1:1; контент (картинка/🎧-озвучка/2 абз./сноска)
  на месте, cov 0.942 = шум. Прогон 352/0/339/970, changed=только 1C8A.
- **2026-06-13 (09:47–09:52, ВИЗ.ХВОСТ 6–7/22 · 2C3 + 1BI):** **2C3** — потерь нет (эпиграф+
  маркеры, трек-лейбл «Песнь Ступеней[^10]», 13 кросс-реф `[[ID]]`, notes_title «Пост Скриптум»);
  cov 0.957 = шум (13 popup-сносок). Двойной «См. также: См. также: Мечтай» в [^7] = артефакт
  ОРИГИНАЛА (`<li id="fn-7">`), не конвертер, faithful. **1BI** — 2 фикса: (1) тела 4 сносок несли
  протёкший самомаркер `[^N]` (оригинал `<li><sup>N</sup>текст` — голый sup номера → inline «[^N]»)
  → `_strip_lead_num` срезает ведущий `[^N]` при N=номер сноски; (2) `p.crow` «!!!! КУКАРЕКУ !!!!»
  (центр/болд/акцент) шёл прозой → `crow` добавлен в классы [cry]. Прогон 352/0/339/970, changed=
  только 1BI; зрением сноски чисты + КУКАРЕКУ центрирован. NB: .he-иврит-глоссы 1BI — вопрос 6.
- **2026-06-13 (09:53–09:57, ВИЗ.ХВОСТ 8/22 · 1CB · `pnum→[num]`):** нумерованные абзацы
  `<p class="para"><span class="pnum">N.</span>текст` шли ПРОЗОЙ со склеенным «1.Я» (пробел был
  CSS-margin .pnum). Класс `pnum` не входил в hangnum-сет `{n,num}` (вердикт 7) → добавил `pnum`
  в `_hangnum` + prune → корректный [num] с авторскими номерами (honor); разорванные треком/поэмой
  пункты → отд. [num], номер сохранён. Бонус: улучшены 7 статей (1AA/1BF/1CB/21K/221/255A/2BI),
  все стали списками 1-N; 176 уже был [num]. Прогон 352/0/339/970, changed=7; зрением 1CB+1AA 1:1.
- **2026-06-13 (09:59–10:05, ВИЗ.ХВОСТ 9/22 · 47H · КОРПУСНЫЙ фикс двойного автора в треках):**
  оригинал `<span class="track-title"><span class="artist">A</span> — Song</span>` (artist ВЛОЖЕН
  в title) → конвертер брал и title-текст «A — Song», и artist «A» → render «A — A — Song». Фикс:
  вырезаю вложенный ARTIST_CLS-спан из текста названия (`prune`) + срезаю разделитель-тире с ОБОИХ
  концов (лид 47H/Joan «— One of us», хвост 44Q/ManOwaR «Warlord —»). Затронуто **25 статей** — все
  double-author исправлены; зрением 47H («Joan Osborne — One of us») + 163A 1:1. **Коридор 339→338**:
  22S (0.962) вышел из коридора — двойной автор ложно раздувал его cov, теперь истинное значение
  (метрика, НЕ регресс; 22S→на быстрый просмотр). Сноски 970. NB: инфо-панели-рамки 47H → проза
  (нет box-конструкта в формате, ⚠ отложено).
- **2026-06-13 (10:06–10:15, ВИЗ.ХВОСТ 10/22 · 1AQA + корпусные муз-фиксы):** оригинал 26000px =
  пустой Telegram-эмбед (как 42DA). **(1) звёздные сноски:** маркер «*»/«**» в `<span class="note-
  mark">` (`<li id="fn1">…`) → конвертер нумеровал [^1]/[^2] по id И звезда текла в тело. Фикс:
  `note-mark`→MARKER_CLS + при ЧИСТОМ звёздном маркер-спане `num=None` (главенствует над id) →
  [^*]/[^**] (только 1AQA; 19 рабочих звёздных статей не тронуты). **(2) авторы треков:** `<span
  class="art">— Europe</span>` — `art` ∉ ARTIST_CLS → авторы (Europe/Enigma/…) терялись. Фикс:
  `art`→ARTIST_CLS (корпусно 5 ст.: 18MB/1AQA/2BI/2BSA/55D) + срез «N) — »-остатка после вырезания
  вложенного artist (2BSA; 53KA-номера в названии сохранены). Зрением 1AQA: [^*]/[^**] + авторы
  треков ✓. Коридор 338→**339** (1AQA поднялся, добрав текст авторов). **⚠ Отложено (музыка-
  канон):** track-номера 1)-4) (span.num), заметка «Слушать вместе и одновременно!» (текст-лосс в
  муз-контейнере), порядок «Автор — Песня» vs ориг «Песня — Автор». 22S остался ниже (0.962, трек
  чист — вероятно шум; добавлен в хвост на просмотр).
- **2026-06-13 (10:16–10:25, ВИЗ.ХВОСТ 11–12/22 · O4Q + 215B):** **O4Q** — потерь нет (эпиграф,
  поэма со сносками, трек, 5 богатых многострочных сносок с [[ID]]); пустой «(см. также: )» в
  муз-лейбле = в оригинале (faithful). **215B** — КОРПУСНЫЙ фикс обёртки-маркера сноски: маркер в
  `<span class="fnmark"><a href="#fn1"><sup>*</sup></a>…` (href на ВЛОЖЕННОМ `<a>`, не на спане) →
  `_ref_num` на пустом href спана не находил номер → маркеры в теле/поэме ДРОПались. Фикс (inline):
  при пустом href спана берём вложенный `<a href="#fnN">`, эмитим ТОЛЬКО при frag∈FN_ID_MAP (пустые
  сноски, 0BO `<li id=fn1>` лишь с ↩, не плодят висячий маркер). Корпусно **17 статей** (03H/0B6/
  0BP/0C2/186B/18K/1AM/1CF/215B/247/26Q/29N/2B3/2C5/39L/56N/61D). 215B зрением: [^1]/[^2]/[^4]
  восстановлены, [^3] орфан (в ориг. тоже без маркера). 352/0, коридор 339, сноски 970. **Оракул-
  хвост (13 ст.) почти закрыт — остался 336.**
- **2026-06-13 (10:26, ВИЗ.ХВОСТ 13/22 · 336 · ОРАКУЛ-ХВОСТ ЗАКРЫТ):** 336 cov 1.081 ВЫШЕ
  коридора = БОЛЬШЕ текста (не потеря): статья дважды перечисляет 7 треков (вперемешку с
  капс-куплетами + сводный список «Все треки в одном месте») + 8× лейбл «Песнь Ступеней» — дизайн
  оригинала, faithful (зрением 1:1). **Секция A (оракул-хвост, 13 ст.) ПОЛНОСТЬЮ разобрана:
  реальных потерь текста НЕТ нигде** — низкая/высокая cov везде = шум метрики (popup-дубли сносок,
  пустые Telegram-эмбеды, дубли треков, alt картинок). Попутно ПОЧИНЕНЫ корпусные баги: муз-двойной-
  автор (25 ст.), `art`→artist (5 ст.), обёртка-маркер сноски (17 ст.), pnum→[num] (7 ст.),
  seealso-группировка (5 ст.), звёздные сноски note-mark (1AQA). Осталось: группы B/C/D (9 ст.) + 22S.
- **2026-06-13 (10:29–10:36, ВИЗ.ХВОСТ 14/22 · 272 · стих-цитата + КОРПУСНЫЙ verse-фикс):**
  куплет «Circle of Life» (`<blockquote class="verse">` со сплошными `<br>`) разносился в 7
  отдельных строф — `emit_verse`-фолбэк давал `<br>`→\n + исходный pretty-print \n = «\n\n» =
  ложный разрыв строфы. Фикс: схлопываю ТОЛЬКО горизонт. ws, `<br>`(+окруж.ws)→ОДИН \n, пустая
  строка=строфа. Куплет стал тесным 1:1. Корпусно улучшены 6 ст. (193/272/2CQ/33G/42DA/457);
  источниково-переносные стихи (1CM/335 — каждая строка на своей строке исходника без `<br>`) НЕ
  сломаны, значимые \n сохранены (первая наивная версия `re.sub(\s+)` их слепляла — отловлено
  зрением по zml и переписано на единое правило). 352/0/339/970. NB: диалог М./i. прозой (нет
  dlg-класса в оригинале, faithful).
- **2026-06-13 (10:37–10:50, ВИЗ.ХВОСТ ЗАВЕРШЁН — 22/22 + 22S):** группы B/C/D разобраны: 2BS/32K
  (форум-диалоги [dlg]/[quote], построчные цитаты сохранены), 1AM (маркеры восстановлены 215B-фиксом;
  поэма+посвящение+эпиграф+[cry]), 158A/179/3CM (диалог-фолбэк прозой, контент цел; **179** — фикс:
  пустая опечатка-seealso «Смс. также:» давала «См. также: Смс. также:» → толерантность к «Смс.» в
  strip → дроп), 0BCA/1CM (h4→###; **1CM** — сложнейшая: [refs]-группы s1/s2/s3 с глифами ‡/¤/^
  работают с backref), 22S (трек чист после 47H-фикса). **ГЛАВНЫЙ ВЫВОД: реальных потерь текста НИГДЕ
  нет** — весь cov-разброс = шум метрики. Инварианты финал 352/0 · коридор 339 · сноски 970,
  out⇄docs/art синхронны, вью пересобраны. Полный список корпусных фиксов + ⚠-вопросов оператору
  (А анон-сноски, Б музыка-канон, В тема/render-полировка, Г источниковые артефакты) — в шапке
  `cms-revival/convert/visual_tail_review_001.md`.
- **2026-06-13 (12:39–13:48, ПАЧКА РЕШЕНИЙ А–Г + ТЕРМИНЫ — проведена и реализована):** оператор
  посмотрел `convert/operator_decisions_002.md` (5 кластеров, 13 строк) и дал «делай как написал всё,
  только roza_mira-серую-зону не ручной приёмкой, а сам приблизь». Внедрено за один проход (конвертер
  `html_to_zml.py` + `editor/render.js`, БЕЗ тем): **T-A** dl→`[ul]`+`{term|}` (roza_mira 40 / 071 8);
  **T-B** roza_mira strong→`{term|}` 471 терм. (правило B1/B2 + LLM-классификация серой зоны 303→206-терм
  через headless `claude -p --model claude-opus-4-8`, скрипт `convert/extract_roza_terms.py`, вердикт
  `grey_verdict_idx.json`, словарь `roza_mira_terms.json` — конвертер грузит его на импорте); **T-C**
  17BA `.sef`/1BI `.he`/46L b-аббр/462 strong→`{term|}`; **В5** `_drop_quote_label` во всех `emit_verse`/
  `emit_stanzas` (1CM 5 строф); **А1** `note-anon`→голый `[^|]:`→`li.anon-bare` (0BH), `fn-anon`→`[^*]:`
  (0BCA); **В1** renderQuote без `data-label` (ярлык ЦИТАТА снят, эпиграф-ярлыки целы); **В4** подтверждён
  (уже работал). Инфра: оракул `zml_visible_text` считает внутренность `{term|}`/`{leit|}`; SECTION_RX
  slug без `|` (термин в конце `##` не съедается). Прогон: **352/0 · коридор 339 · FN_DEFS 971** (+1 =
  0BCA новая сноска) · changed 9 (071/roza_mira/17BA/1BI/46L/462/1CM/0BH/0BCA — все объяснимы) ·
  out⇄docs/art синхронны · 352 вью пересобраны · сверено зрением 17BA(термин-заголовки)/2BS(цитата без
  ярлыка)/1CM(стих без «Цитата», TOC вложен). **184** `note-anon` несёт глиф «*1»+«См.также» (НЕ голый) —
  оставлен orphan-`[^1]` без регресса (нюанс оператору). push.bat НЕ запускался. **ОТЛОЖЕНО на фокус-
  проход (обоснование в шапке ТЕКУЩЕЕ):** Б1/Б2 музыка (сцеплены с каноном «Название—Автор» = большая
  per-article задача) · В2 форум-`[dlg]` (нужна спикер-структура конвертера).
- **2026-06-12 (15:44–15:54, расследование терминов — ВЫПОЛНЕНО):** скан 352 оригиналов
  (теги b/strong/em/i/u/mark/dfn/dt + все span-классы с термин-CSS, 224 стилизованных класса
  разобраны). Истина: оператор прав — термины выделялись не только в 158, но БЕЗ выделенного
  класса: (1) словари `dt/dd` roza_mira (40, «Глоссарий терминов») + 071 (8, «Словарь
  терминов») — сейчас плющатся в прозу, конвертер сам флагует «dl — вне формата»; (2) strong
  roza_mira 707 шт. = 216 глоссарий-строк «Термин — определение» (начало li/p, надёжно) + 92
  run-in «Подтема.» + ~400 inline (термин/эмфаза машинно неразличимы); (3) точечные: 17BA
  `.sef` сфирот-плашки (4), 1BI `.he` иврит-глоссы (3), 46L b-аббревиатуры СА/СС/ТС/КК/АР (5),
  462 strong-имена (3). Анти-находки: b корпуса (206/66) = треки/лейблы/номера (уже
  перехватываются [mus]/label-логикой), em = чистая эмфаза, i/u/mark/dfn/abbr в корпусе нет →
  вне roza_mira+словарей потерь терминов по ~345 статьям НЕТ. Отчёт с предложениями правил
  A–D (A: dl → `{term|Термин} — определение` без нового тега; Б1/Б2: roza_mira строки; C:
  адресные; D: чего НЕ делать) = `cms-revival/convert/term_investigation_001.md`. Конвертер
  НЕ трогал — внедрение по вердикту оператора (вопрос 6 в «ТЕКУЩЕЕ»).
- **2026-06-13 (16:00–18:00, №1 МУЗЫКА-КАНОН «Название — Автор» ЗАКРЫТА + №2 ФОРУМ-СТИЛЬ начат):** фокус-проход
  доборов (оператор: «делай всё сам»). **№1:** оракул-ориентации = `docs/songs/index.html` (220 карточек канон,
  подтв. оператором) → резолвер `convert/music_canon.py` (фолдинг латин-диакритики · junk/url-детект · split
  склеек · faithful-1CM) канонизирует КАЖДЫЙ трек в `emit_music` (slot2=название всегда). NODB-хвост 35→16 добит
  YouTube-**oEmbed**+знанием во frozen `music_canon_overrides.json` (ре-ран детерминирован). 489 треков: KEEP 323 ·
  SWAP 65 · FILL 28 · FILLBOTH 44 · SPLIT 13 · NODB 16. Render флипнут `Автор—Название`→`title — author`. Политика
  (прецедент 158 «Обман|Ария·» уже-канон): СОХРАНЯЕМ текст статьи, чиним только ориентацию/мусор/склейки; маркеры
  сносок целы (16J `Gregorian[^12]`, 12F/1CM refs `|N`). **Б1** нумерация — явный «N)» в начало названия (НЕ
  авто-счёт: 2BN номера 4/3/2/5/1 = идентичность трека; залипший «4)» из слота автора снят). **Б2** inter-track
  заметки — строки `>` в `[mus]` (1AQA «Слушать вместе…»), CSS `.music-note`. Конвертер: `collect_music`
  (порядок+заметки, дедуп дублирующих трек-строк); `parseAttrs` render парсит голые флаги. **Добор (в)** плашка-канон
  `▶ N) `**назв**` — `*автор* (split-спаны `.trk-*` + ▶ вместо `›`; CSS `.music-group .title .trk-*` в
  `template_view.html`) — сверена зрением во ВСЕХ 5 темах. **Добор (б)** url: url-less было всего 3 (237 ×2, 31I),
  `music_canon.fill_url` дозаполнил из базы songs + Whitney `3JWTaaS7LdU`. **Добор (а)** полнота songs ОТЛОЖЕНА на
  [shir]-миграцию (10 новых песен + 17 альт-загрузок измерены; хенд-добавлять в обречённую рукотворную страницу =
  throwaway). Инварианты: 352/0 · коридор 338 (215B/34O чуть<0.97 BENIGN: дедуп убрал padding-текст; множество
  УНИКАЛЬНЫХ видео old=new, потерь нет) · FN_DEFS 971. Сверено зрением: 42D/127A/337/1CM/1AQA. flag-oddball 1CIA
  «Поехали!|Хава Нагила» (`claKl5gUL6w`, oEmbed=Хор Турецкого) оставлен faithful. Артефакты: `music_canon.py` ·
  `music_canon_overrides.json` · `music_canon_recon.{py,json}`. **№2 ФОРУМ-СТИЛЬ (частично, render-уровень):**
  `renderDialog`/`splitSpeaker` парсят ведущую спикер-метку реплики («Ник написал(а):» · «Вопрос/Ответ:» · «Имя:»)
  → пост-в-рамке + метка small-caps (CSS `.dialog .post/.post-by/.post-text` в `template_view.html`, цвет `--accent2`
  темы). Покрывает **19 inline-label** статей (Q&A 17BA/09S + именованные спикеры 262 «Нипна/Анибе»). Сверено (262).
  ОСТАЛОСЬ: **11 standalone-header** статей (221A 27G 27H 27J 2BA 2BS 332B 335 39L 3AT 55K) — у них `[dlg]` = ТОЛЬКО
  заголовок «Ник написал(а):», ТЕЛО реплики лежит СНАРУЖИ блока (+ вложенные форум-цитаты) → нужна КОНВЕРТЕРНАЯ
  спикер-структура (втягивать тело-сиблинг под заголовок, расширить DIALOG_CLS-ран). Эталон форума = ccastaneda.ru
  (2BS/27G ссылаются). Деплой ЛОКАЛЬНЫЙ, push.bat НЕ запускался.
- **2026-06-17 (07:10–07:47, №2 ФОРУМ-СТИЛЬ standalone-header ЗАКРЫТ — 10/11):** go «Продолжаем roadmap». Разведка:
  probe-парсер показал — все 11 = строго чередование «короткая метка-спикер (`.speaker`/`.answer-label`, у 3CM
  `.reply-label`) → тело-сиблинги (blockquote/голые `<p>`/`.answer`/`.body-prose`/`p.quote`/`blockquote.verse`)»,
  вложенных цитат НЕТ. **Дизайн-развилка решена в пользу ЯВНОГО маркера `>> `** (а не широкой `:`-эвристики
  splitSpeaker): скан корпуса показал, что широкое правило `^Метка:` ложно ловит строки тела (стих 335 «Свойство
  зеркальце имело:»), а конвертер класс метки знает точно → маркер `>> ` снимает угадывание. **Конвертер**
  (`html_to_zml.py`): хелперы `_is_speaker_label` (DIALOG-узел + текст ≤80 + хвостовое `:` — отсекает реплики-строки
  571 «"- Что означает…?"», 18M «Вопрос(…) Например?», поэму-речь 33H), `_is_dlg_body_stop` (лид/подпись/заголовки
  h2-h6/поэма-блок/списки/музыка/хром), `_dlg_body_paras` (стих `.v`/`blockquote.verse` → строки через `_verse_lines`,
  НЕ [poem]-обёртка), `_emit_forum_thread` (тред «метка→тело» → `[dlg]` с `>> метка`). Хук в DIALOG_CLS-ветке, `55K`
  исключён по art-id. **Render** (`render.js`): `renderDialog` при наличии `>>` идёт явным путём (метка→пост-в-рамке,
  след. неразмеченные абзацы впитываются как `<p>` в тот же пост); эвристический путь улучшен (метка открывает пост
  даже с пустым телом — чинит попутно 416A/2BG). CSS `.post-text+.post-text` (многоабзацная реплика) в
  `template_view.html`. **Итерации отладки:** первый прогон дал CHANGED=14 (ложные 18M/33H/571 — слишком широкий
  `_is_speaker_label`; 571 утянул заголовки `##`) → добавлено требование хвостового `:` + h-теги/поэмы в body-stop
  → CHANGED=10 = РОВНО цели. Инварианты: 352/0, коридор 338, FN_DEFS 971, дифф out↔снимок = ровно 10. Сверено зрением
  (viewshots 10/10): 2BS (3 поста + капшен-метка «Диалог на форуме…»), 27G/27H/2BA/3AT (многоабзацные реплики в одной
  рамке), 335 (стих-пост с переносами), 39L (сноска [^1] в самой метке «Реплика (Сталкеr¹)»), 221A (авторская
  `.lead`-реплика без метки → проза вне рамки). **Отложено:** 55K (пьеса, нужен play-cue над `[poem]`); 3CM (класс
  `p.who`, можно влить тем же `_emit_forum_thread`, если оператор захочет). Деплой ЛОКАЛЬНЫЙ (out→docs/art синкнут,
  вью пересобраны; push.bat НЕ запускался).

- **2026-06-17 (14:01–14:16, sig/cry/глифы-извлечение ЗАКРЫТО — конвертер):** go «Продолжаем roadmap_cms».
  Следующий по «Остатку п.6». **Разведка** живой HTML (`docs/art`): класс-сканы дали 18 ст. с подписью
  (`.signature/.sign/.signoff/.signed`), `;;;;`-мусора в живой HTML НЕТ (она = причёсанная LLM-вёрстка, не raw →
  пункт «чистка `;;;;`» в конвертере моот; реальные глифы `<\^/>` переносятся текстом и render их `.glyph`-обернёт,
  237). `cry` оказался УЖЕ сделан (.closing/.banner/.shout/.call/.center-call/.resume/.crow → `[cry]`). **Остаток =
  только sig.** Подписи раньше падали в дефолт-прозу (CHROME_CLS их не содержит). **Конвертер** (`html_to_zml.py`):
  `SIGN_CLS={sign,signature,signoff,signed,podpis}`, хелперы `_strip_sig_label` (снимает форум-лейбл «Подпись
  пользователя» — `.sig-label`/`.cap`/декор-текст, 2CQ/324) + `_is_dateline_only` (однострочная дата-подпись
  «Написано … г.»); хук в walk сразу после `[cry]`-ветки: `(c & SIGN_CLS) and t in (p,div)` → `[sig]` (переносы
  через `_mark_lines`), чистая дата → `[line kind=dateline]` (§2.3; byline всё равно рисует manifest-дату). `SIGN_CLS`
  добавлен в `DLG_BODY_STOP_CLS` (подпись ≠ тело форум-поста; future-proof, текущий вывод не изменил). **Не трогаем:**
  инлайн `<span class=sign>` в прозе (24F — хвост абзаца), `.sign` внутри эпи-контейнера (34Q — cite эпиграфа, идёт
  через emit_epi). **Прогон:** CHANGED=16 (07I/15D/163/18AA/18T/221A/247/262/2BI/2CQ/31E/324/341/3A7/3C7/433),
  352/0 ошибок, FN_DEFS 971, below0.97=14 (→ коридор-нижняя 338 цел; строгий [0.97,1.05]=335 — стабилен, сверено
  прогоном с выключенным хуком → мой код коридор-нейтрален). **Сверено зрением** (viewshots, тема A): 31E (имя+город+
  дата построчно, правовыключка-курсив), 2CQ (стих-подпись, лейбл снят), 324 (кредо-подпись, лейбл снят), 07I (два
  `@`-ника). **Краевой кейс 2AA:** сайн-офф-дата внутри `<article>`-тела реплики впитана в `[dlg]` (текст цел,
  отдельным dateline не стал — `_dlg_body_paras` рекурсит в контейнер; чинить = вынимать хвост-подпись из контейнера,
  тяжело ради 1 ст.) → допустимая мелочь. Деплой ЛОКАЛЬНЫЙ (out→docs/art синкнут, вью пересобраны; push.bat НЕ запускался).

- **2026-06-17 (15:17–15:28, `[subsec]`-добор краевых — чистая часть ЗАКРЫТА; найден header-trap):** go «Продолжай».
  **Разведка** (`convert/subsec_recon.py`→`subsec_recon.json`): 120 subtitle-ish узлов, 20 классов. Разбор: `.subtitle`
  (55) — 54 шапочных (→`[sub]`, оставлено) + 1 в-секции (158); `.deck`/`.standfirst` (по 2) — дескриптивные деки
  (СВОЯ CSS: deck italic 1.18rem, standfirst muted 20px ≠ subtitle sans 15px); `.subhead` — h3 (→`###`, не subsec);
  `.vsub` — стих-спаны (182, не subsec); `.lede` (6) — СМЕСЬ (метадата-блок 462 «телеграм-канал/ссылка», контент-
  реплика 35J, дескриптив 215/666) → НЕ трогаем; `.lede-rule`/`.subkicker`/`.sub-paren`/… — хром/киккер/прочее.
  **Конвертер** (`html_to_zml.py`): (1) ветка `c & {sec-sub,deck,standfirst}` → `emit_subsec`; (2) `.subtitle` СРАЗУ
  под заголовком (`blocks[-1]` матчит `^#{2,3} `) → `[subsec]` — НЕ `st["started"]` (первый прогон с `started`
  дал CHANGED=17: киккер/дата-строки взводят started в шапке → ложно утянули 14 шапочных subtitle в subsec; заменил
  на «пред. блок = заголовок» → CHANGED=3). **Итог:** CHANGED=3 (12F дек, 158 в-секции, 46L стэндфёрст), 352/0,
  FN_DEFS 971, below0.97=14 (коридор-338 цел), 335 строгий — стабилен. Сверено зрением (158: subsec muted-italic под
  `## ВМЕСТО ВСТУПЛЕНИЯ`; 12F: дек под H1/byline). out→docs синк, вью пересобраны, push.bat НЕ запускался.
  **НАХОДКА (эскалирована, ждёт go):** `convert/subsec_recon2.py` — 18 описательных подзаголовков заперты внутри
  `<header class=lead>`/`.lead` (CHROME_TAGS содержит `header` → весь subtree выбрасывается; оракул `visible_text`
  тоже исключает header → потеря авторского текста БЕЗ сигнала покрытия). Список: 12FA/14U/152/19UA/1AH/1CF/1CI/22P/
  24H/273A/27E/2A9/43O/53K/54H/54Q/Z46. Спасать (→`[sub]`/`[subsec]`/`[line dateline]` по классу + научить оракул
  считать их, иначе ложный >1.05) ИЛИ держать мастхед-декором (как hero→frontmatter.image) — решение оператора.

- **2026-06-17 (16:16–16:23, спасение 18 header-подзаголовков — ВЕРДИКТ «спасай»):** оператор выбрал спасать.
  **Точная диагностика** (`convert/subsec_recon3.py`): из 23 «dropped» реально потеряны 18 узлов внутри `<header>`-тега
  (17 ст.; 1AH — 2); «div DROPPED» оказались ложными (19U `_italic_`, 21LA `*1/*2`-сноски ломали probe — текст на
  деле `[sub]`) или осознанными sec-name-дублями (1CM/29N/2BNA, гард `[sub]`=раздел). **Конвертер:** `find_lead_subtitles`
  (subtitle/deck/standfirst/article-subtitle ВНУТРИ `<header>`; div.lead walk видит сам — не дублируем), эмит ПЕРЕД
  телом walk (под H1, §1.3 «концептуально под общим заголовком»): deck/standfirst→`[subsec]`, subtitle/article-subtitle
  →`[sub]`, дата-строка (Z46 «Написано 06.04.19…»)→`[line dateline]`, дубль раздела→снять. **Оракул:** `visible_text`
  при header теперь добавляет `lead_subtitle_text(k)` (спасённые подзаголовки видимы и в ov; иначе zv их содержит, ov
  нет → ложный сдвиг >1.05). **Итог:** CHANGED=17 = ровно цель, 352/0, FN_DEFS 971, below0.97=14 (коридор-338 цел),
  строгий [0.97,1.05]=335 — стабилен (оракул-фикс сработал). Сверено зрением (тема A): 24H (стэндфёрст «Об Учителе-
  Духе…»→muted-italic [subsec] под byline), 1AH (2×[sub] «Школьная история» / «Рассказ 2, часть 2» под title с
  `[2]`-сноской). out→docs синк, вью пересобраны, push.bat НЕ запускался. Разведка-скрипты `subsec_recon*.py` оставлены.

- **2026-06-17 (16:16–17:12, CAPS:ON возрождён — список ПО ДАННЫМ):** сперва ошибочно счёл CAPS:ON «фичей без
  потребителя» (0 статей с `caps: on`) и предложил пропустить. **Оператор поправил:** caps — режим отображения капс-
  строк (в корпусе их куча, включая целые статьи/стихи — скан: 30 ст. ≥3 капс-строк; 323/337/1AN/55K почти целиком);
  при `caps: on` они должны переключаться в болд/семиболд с выборочной капитализацией первых букв — это и была задумка.
  Render-машинерия уже была (`resolveCaps`/`capsToSmallcaps`/`capitaliseSentences`/`applyHonorific`, гейт `capsEnabled`
  деф. OFF) — дело в `proper-nouns.txt`. **Директива:** старый ручной список (60) — в архив; новый строить ПО ДАННЫМ —
  слово держит заглавную, если в НЕ-капс АВТОРСКОМ тексте корпуса так пишется в БОЛЬШИНСТВЕ; исключить статью
  `roza_mira` (другой автор) и реплики читателей/оппонентов в `[dlg]`/форуме. **Реализация**
  (`convert/caps_proper_build.py`): подсчёт регистра по середине предложений (исключены начала, `. ! ? … : « —`),
  капс-строки, блоки `[dlg]`/`[quote]`, `roza_mira`; группировка pymorphy3-леммой ТОЛЬКО для счёта (lemma/POS garble
  на ивр./арам.: Тиферет→ТИФРАТЬ, Кли→КЕЛИТЬ); БАЗА = LCP titlecase-форм (render pnStem+CASE_ENDINGS покрывает
  склонения), либо `!exact` если слово всегда ALLCAPS (акроним/имя-в-капсе: АВАЯ/ЧСВ/ГАР/ЗАТ). Порог: строгое
  большинство ratio>0.5 & total≥4. Чистка: стоп-слова, прилагательные исключены (матчер падежный → прил.
  рассинхронит; «Свят/Благословен» — applyHonorific), дедуп склонений, фиксы беглых/косвенных основ (ОМЕГОЙ→ОМЕГА,
  ЗМЕЕМ→ЗМЕЙ, ЛЮБ→ЛЮБОВЬ, ЗАМЫС-дроп). **Итог: 171 запись** (config + editor/data синк; старый →
  `config/proper-nouns_legacy.txt`). **Добивание канон-наборов** (вердикт оператора «логично писать все с заглавной,
  если часть уже»): +Хесед/Гвура/Нецах (сфирот) +Брия/Ецира (миры) — все always-cap в авторском тексте, но недобрали
  порог частоты (`MANUAL_ADD` в скрипте). **Ход НЕ добавлен:** омоним с нарицательным «ход» (15 строчн./4 загл.),
  матчер не различает (оба→ХОД) → ложно капитализировал бы обычное «ход» в фолдинге. Матчер `render.js` дополнен
  окончанием `О` (ср. род Кольцо/Царство/Древо).
  **Сверка зрением** (caps:on tmp-рендер `editor/caps_test.mjs`): 323 «Обитаемый остров» (капс-стих целиком →
  читаемая semibold-капитель, первые буквы предложений заглавные), 337 (стих, ложных нет), 322 (теолог. проза:
  Творца/Света/Вселенной/Ангел/Рассвет из списка держат заглавную, «Благословен Он» — гонорифик, ложных нет). Корпус
  остаётся `caps: off` (включается пооператорно), converter не тронут (CHANGED=0, 352/0, FN 971). Разведка-скрипты
  `caps_proper_*.py` оставлены. push.bat НЕ запускался.

## Этап: РЕАЛИЗАЦИЯ решений fn-ревью (go 12:33, 2026-06-12) — ЗАВЕРШЁН 13:30 ✔
*Статусы обновлять ПОСЛЕ КАЖДОГО пункта: ⬜ → 🔧 в работе → ✔. Реестр решений (12 вердиктов
оператора, «что именно делать») = `cms-revival/design_review/fn_review_10_progress.md`
§«РЕШЕНИЯ ОПЕРАТОРА» + §«Сводный список предлагаемых правок» (A1–A9/B10–B11 с примерами статей).
Всё ЛОКАЛЬНО, push.bat НЕ запускать. Просмотр: preview.bat / :8098. Пересборка вью:
`node cms-revival/editor/build_views.mjs` (пишет в docs/art). Скрин-осмотр: `convert/viewshots.py
<id> [<id>.html]` → `_viewshots/*.jpg` (сегменты ≤800px; >550КБ или повторное чтение того же
файла — ужимается, лечится свежим именем/дорезом половинок). Chrome-MCP скриншоты могут виснуть
(CDP timeout) — структурные JS-проверки работают.*

- ✔ **P1. Спека** (12:44) `cms-revival/zml3/SPEC.md` → v2: группы сносок `[^имя.N]` + `[^имя.N]:` + `[refs]`/
  `[refs=имя glyph=¤]` (фолбэк-хвост с notes_title; id `cite_note-имя-N`; orphan-правило в группе;
  сирота-[refs] = флаг) · маркеры `[^N]` в `title:` (рендер H1-sup, чистка в `<title>`/крошках/
  prev-next) · уровень `###` (h3+h4; TOC вложенно) · `[toc]`/`[toc collapsed]` (рисовать ТОЛЬКО по
  маркеру; дефолт open) · `[line kind=tags|dateline]` (закрытый список kind) · `[ul]`/`[ul marker=◆]`
  (пункты «- », дефолт-значок темы) · FM `dates:` (строка показа даты в byline) · `[epi kind=dedi]` ·
  `[num]` с авторскими номерами и скобко-стилем («1.»/«1)») · правило «лейбл ЭПИГРАФ → [epi]» ·
  label-спаны («Цитата» и пр.) снимаются конвертером · poemsub → `[subsec]`/poem-title. §13 обновить.
- ✔ **P2. Render** (13:00; группы+glyph, notes_title/title-sup, [toc], ###, [line], [ul],
  dedi, dates:, num-скобка; темы+контракт+база шаблонов; тест-стенд 5 тем осмотрен;
  регрессия 352 in-memory: 350 идентичны, 2 ожидаемых улучшения 163A/1CM; темы синканы в docs/themes) (`editor/render.js` + оба шаблона + 5 тем + THEME_CONTRACT): inline-резолв
  notes_title и title→H1 (sup ОТЛИЧИМЫЙ от текста заголовка во всех 5 темах — в контракт!) ·
  парсер/рендер групп сносок и `[refs glyph]` (popup/backref/orphan per group) · `[toc]`-позиция
  (убрать фикс-слот; без маркера TOC не рисуется) · `###` (+TOC 2-й уровень, стили тем) ·
  `[line]`-рендер (`p.line.line-<kind>`, исключить из dropcap-цепочек) · `[ul]` (`--marker` →
  `li::marker`) · `[epi kind=dedi]` (темы) · `dates:` в byline · `[num]` скобко-стиль маркера ·
  CSS_VERSION бамп. Тест-стенд: дополнить `cms-revival/zml3/teststand/zml3test.zml` новыми
  конструкциями, осмотреть в 5 темах.
- ✔ **P3. Конвертер** (13:17; бэкап `*_v2_pre_fnreview.py.bak`; группы+[refs glyph] на авторских
  местах (двухпроходная сериализация тел — кросс-рефы [^s2.1] верны) · image из реального src ·
  [^N] в title · h3/h4→### · poemsub→[subsec] · span.n→[num] · лейблы Цитата/ЭПИГРАФ/музыка ·
  div.quote · юникод-∗ · муз-рефы отдельной строкой · роли→[cry]/[epi dedi]/[line] · [toc]-эмит ·
  dates: · ul→[ul]; 28MA-заголовок муз-секции восстановлен (лейблы вставок оказались АВТОРСКИМИ
  в живой HTML — A5б снят); 14 ревью-статей в коридоре; 654/237 diff = только +[toc]) (`convert/html_to_zml.py`; перед правкой бэкап `*_v2_pre_fnreview.py.bak`):
  `image:` из РЕАЛЬНОГО src (нет картинки — не эмитить) · пер-секционные сноски → группы + `[refs]`
  на авторском месте (notes_title больше НЕ красть заголовки секций; секции не опустошать) ·
  сохранять `[^N]`-маркеры в `title:` и notes_title · h3/h4 → `###` (флаг при уродстве структуры);
  poemsub → `[subsec]` · `span.n`/`span.num` В ТЕЛЕ → `[num]` (сносочные num НЕ трогать) ·
  label-спаны снимать («Цитата», «ЭПИГРАФ»-заголовок → [epi], музыка-лейблы) · `div.quote` →
  `[quote]` · юникод-звёзды (∗, U+2217) как звёздные маркеры · музыка: реф-цифры НЕ клеить к
  названиям (маркер сохранять рядом с блоком), label чужой секции не наследовать, label-строку
  перед карточкой съедать, финальный трек-лист 12F (формат «Автор — Композиция», вычистить ↗↑›) ·
  closing/banner → `[cry]`, coda → `[epi]`, dedication-класс → `[epi kind=dedi]`, tags/dateline →
  `[line]` · `[toc]`-эмит по оригиналу (только у 74; collapsed у 1) · `dates:` (163A) · `ul` → `[ul]`.
- ✔ **P4. Прогон корпуса** (13:20): 352 OK / 0 ошибок → `corpus_run_v3.json`; коридор 339/352 =
  база v2 (хвост идентичен — прежний разобранный шум; 4 сдвига, все К единице); zml → docs/art,
  352 вью пересобраны. Конструкции: [toc] 79 (collapsed 1 ✓), [refs]-группы 4 ст., ### 11 ст.,
  [line] 77, [ul] 14, [num]-захват 29 ст., title-маркеры 37 ст., dates: 1 (163A), image 196
  (только реальные src; потерь обложек 0). А-чек (а): определения сносок 962→962, дельта 0.
- ✔ **P5. Чеки** (13:27): (а) сноски 962→962, дельта 0, span.num не утёк ✓ · (б) ЭПИГРАФ:
  29 авто→[epi] (все 29 имеют [epi] в zml ✓) + 3 спорных (16J/1CI/571 — маркер сноски НА лейбле,
  оставлены заголовком, иначе сноска-источник сиротеет) · (в) 217A: 42/42 определения по 11
  группам, рефы↔определения замкнуты 0/0, выдуманного заголовка нет; зрением шапка/секция/хвост ✓ ·
  (г) kicker 46: ЧАСТЬ киккеров = даты («25 февраля 2021 г.») → [sub] дублирует byline — СПОРНОЕ
  оператору; диалоги 30 ст. прозой; [ul] 14 ст., остальные ul = TOC/сноски (разъяснено) ·
  (д) топ-10 перегнан: 12L «Два корабля» чист, 239 ∗∗∗→[^***] двусторонний (MARKER_CLS обобщён),
  28MA H2 восстановлен, обложки/группы/title-маркеры сверены · (е) зрением: стенд 5 тем (P2) +
  живые 163A/12F/1CM/341A/217A — группы/глифы/[num]/dates/порядок секций на месте.
- ✔ **P6. Финализация** (13:30): roadmap/летопись обновлены, прогресс-файл закрыт, отчёт
  оператору со спорными (kicker-даты · ЭПИГРАФ-с-маркером ×3 · муз-реф-строка после [mus] ·
  28MA-лейблы оказались авторскими).

- **2026-06-17 (17:42–18:08, `[shir]` ЯДРО — последний формат-пункт фазы сборки):** по «go» оператора.
  **render.js:** `shir`→PAIRED; `renderShir(b,ctx)` — line-based `ytID|Композиция|Автор|arts`, helper `splitEscaped`
  (`\|`/`\,`-escape), `id:Текст`-override подписи ссылки, art-резолв из manifest (`a.internal`/`a.broken`) с базой
  `ctx.artHrefBase` (добавлена в renderCtx; "" в статье / "../art/" на songs); адаптив-грид через CSS-vars
  `--shir-cols/--shir-min`. **Диагностика §6.3:** нет ytID(11-симв.)/названия → роняет ТОЛЬКО плитку +
  видимый `.song-broken` (№строки+текст+чего нет) + console.warn + `<!--ZML:flag-->`. **АВТОР смягчён до
  опционального** (§6.3 был «обязателен»; 17 одобренных карточек без автора — саундтреки/детские → comp-only,
  консистентно с `[mus]`; ноль-регресс) — **ратификация формат-правки ждёт go.** **Шаблон спец-страницы**
  `editor/data/template_songs.html`: база-грид (`auto-fill minmax`, без медиазапросов) + `.song`/`.yt[16/9]`/`.play`/
  `.song-broken` + lazy-YouTube (`.yt` click/Enter/Space→iframe) + theme/width-свитчер (без byline/prev-next). **5 тем:**
  `.songgrid .song…` per-theme (префикс `.songgrid ` перебивает базу) — A левая терракот-полоса · B/ar_deco золотая
  двойная линейка · cyberpunk magenta-полоса+cyan-glow+mono · swiss чёрная линейка плоско · ar_deco гео-капитель;
  play в палитру на hover; ♪-разделитель. +THEME_CONTRACT. **Миграция:** `convert/gen_songs_zml.py` (songs_data.json
  →`docs/songs/index.zml`, rsplit по последнему « — » [кейс Цоя цел], 220 плиток/368 ссылок, порядок как на странице;
  замер: 0 расхождений art-подпись↔manifest → override не нужен, 17 без автора, 1 мульти-сплит) + `editor/build_songs.mjs`
  (синтет.rec, синк тем, `index.view.html`; принимает [in][out] для probe). **Живой `index.html` НЕ тронут** (cutover Ф9).
  `convert/viewshots.py` расширен: `songs`/`songs:<name>` → `docs/songs/`, окно W_SONGS=1400. **Проверка зрением:**
  probe (7 валидных + 1 битая) ×5 тем — все различимы, битый маркер/`.broken`(ZZZ999)/override(«Ода…»)/author-less
  («Jerusalem of Gold») корректны; инварианты целы (352/0, FN 971, CHANGED=0); 216 art-ссылок все резолвятся.
  **ОСТАЁТСЯ (а) полнота songs:** +10 новых/+17 альт (NODB-35) — контент-курация, отдельный проход.

- **2026-06-18 (08:09–08:25, дозаполнение авторов 17 безавторных карточек songs):** по «go» оператора («меняй все
  как обнаружил»). Резолв реальных видео **через браузерный MCP** (вкладки YouTube — oembed/фрейм с локалхоста
  YouTube часто режет; навигация `browser_batch` пачками по 8, заголовок+meta-описание JS-эвалом). Нашёлся автор у
  ВСЕХ 17; всплыли и кривые названия страницы (клички/имя-артиста/заглушки) → исправлены: Звёздный дебют→**Позорная
  звезда**·Агата Кристи · Саша Поровский→**Стражники зари**·А.Поровский · Песенка в тему→**Господи, помоги**·Сектор
  Газа · «Берегись, Мастер»→**Берегись**·Мастер · Brides of Lucifer (имя группы)→**Warriors of the World**·Brides of
  Lucifer · NeverEnding Story→Limahl · Jerusalem of Gold→Shuli Nathan · Голубой вагон→БДХ · Choose Life→Trainspotting
  (киномонолог) · Бобр→**Бобр(Зимний сон)**·Slava Skripka · Театр→Баста · Оно выделяло тепло→АИГЕЛ · Чебурашка→
  Шаинский · Песня о настоящем индейце→Ноль · Ни там ни тут→Агата Кристи · Noubliez Jamais→Joe Cocker · Улетай на
  крыльях ветра→Е.Сотникова. Канон → `convert/songs_overrides.json` (ytID→{comp,author}); `gen_songs_zml.py`
  применяет ПОСЛЕ сортировки (порядок плиток цел). Регенер+пересборка: **220 плиток, 0 без автора**, рендер
  comp—author сверен в `index.view.html`. §6.3 строгим остаётся (author-less нет; рендер терпит пустого как safety-net).
  **NB про корпус:** прямой поиск ytID в `[mus]` корпуса дал автора лишь для 1 («Ноль»); YouTube-резолв — авторитетнее
  (поле `[mus]` часто junk/пусто/«Theme Song»/перепутано). ОСТАЁТСЯ только +10/+17 NODB (новые плитки).

- **2026-06-17 (B6 см.-также — РЕАЛИЗАЦИЯ, КОНСЕРВАТИВНО):** вердикт оператора — минимум вмешательства. Функция
  `b6_seealso` в `_reserialize` (`html_to_zml.py`), покрыты и групповые `[^s2.x]`. Классификация по префиксу
  (См.также/А также/Музыка/Цитата); `::`→пунктуация (префикс→`: `, поля→` — `); снос ПСЕВДОНИМА-автора СВОИХ статей
  (Зеир Арихович Анпин/Зеир А"А/ЭБМ/Элиягу Бар Малей — через разделители `— , . ; (`), внешних авторов (Кастанеда/
  Высоцкий/…) ОСТАВЛЯЕМ. `[[ID]]`, внешние ссылки, текст внешних источников (Зоар/Бааль Сулам/Псалмы) — НЕ трогаем;
  авто-резолв названий→art-id НЕ расширяли (что резолвилось HTML-ссылкой — уже `[[ID]]`; 121/244). Пустые «См.также:»
  обрубки (~6) — ФЛАГ, не удаляем (сломало бы in-text маркер). Инварианты целы (352/0, FN 971, коридор 338). Разведка
  — `convert/b6_recon.py`→`b6_recon.json`.

- **2026-06-18 (11:00–13:22, ДИНАМИК-MUTE [mus]+songs + равнение трек-строки + равная высота плиток + фикс глифа):**
  запросы оператора (YouTube в рекламе убрал кнопку «без звука» → даём свою). **(1) `[mus]` в статьях (render.js +
  `template_view.html`):** монохромный SVG-динамик (`SPK_SVG` — общий конст; `MUTE_BTN` = span-обёртка) справа на
  каждой YouTube-трек-строке; клик/Enter/Space → IFrame API `postMessage(mute/unMute)` (embed несёт `?enablejsapi=1`,
  при lazy-впрыске `&origin=`); `preventDefault`/`stopPropagation` чтобы клик не дёргал `details.open`; mute-состояние
  применяется и при открытии плеера (ретраи 700/1800ms на «только что открыли»). Трек-строка `summary`→`display:flex`
  (текст СЛЕВА во всех 5 темах — перебивает `text-align:center` у B_manuscript/ar_deco; динамик прижат вправо
  `margin-left:auto`). **(2) Песнь Ступеней `[shir]` (`template_songs.html` + `renderShir`):** тот же глиф; ИТОГОВО —
  кнопка в правом-нижнем углу ПОДПИСИ (не на кадре: на пёстром превью терялась). Эволюция по вердиктам оператора:
  оверлей на `.yt` (тёмная пилюля) → перенёс в `figcaption` → **«не увеличивать высоту НИКОГДА»** → убрал резерв
  padding, заякорил к `.song` (figure `position:relative`, figcaption `static`) → угол ПЛИТКИ (220/220, ровно), у
  коротких карточек уходит в пустой stretch-добор. `go()` (lazy-YT) переписан: `insertBefore` iframe (вместо
  `innerHTML`-затирания `.yt`), `.play` удаляется, динамик СОХРАНЯЕТСЯ; JS — обход `.songgrid .song` (динамик —
  сиблинг `.yt`). Эмпир. проверка наложения на ссылки по ВСЕМ 220 (Range.getClientRects): осталась 1 плитка (Earth
  Song) → закрыта `drop-shadow(--paper)`-гало (невидимо на 219). **(3) Равная высота плиток в ряду:** `.songgrid`
  `align-items:start`→`stretch` (карточка тянется на высоту ряда, контент кверху). **(4) ФИКС глифа:** у конуса не
  было `fill` → дефолт-чёрный терялся на тёмных кадрах/темах → `fill="currentColor"` в `SPK_SVG` (улучшил и `[mus]`
  на cyberpunk/ar_deco). **Проверка:** preview (1400px, before/after `getBoundingClientRect`) — рост высоты 0 (ряды
  416.95/391.56/367.23 ×3), динамик у угла 220/220, наложений 1/220 (гало); функц. — клик не стартует/не рестартит
  видео, src с `enablejsapi+origin`, без JS-ошибок; viewshots [mus] 5 тем. Реальное заглушение рекламы headless не
  воспроизвести (реклама случайна) — проверена вся машинерия до `postMessage`. Пересобраны ВСЕ 352 вью + songs,
  инварианты целы (352/0, FN 971, CHANGED=0). **Живой `index.html` НЕ тронут** (только `*.view.html`, cutover Ф9;
  вердикт оператора — динамик ТОЛЬКО в ZML-вью). Доб. preview-конфиг `zml-preview` (8099) в `~/.claude/launch.json`
  (8098 занят ручным сервером оператора).

## Этап: Ф8 доборы редактора — иллюстрация (b) + «Песнь Ступеней» (c) (2026-06-18, ВЖИВУЮ)

**Ф8(b) Иллюстрация (`image:`) в ZML-редакторе — ВЖИВУЮ `fb6f1ed` + Worker `ae9607a5`.**
Блок «Иллюстрация» в `docs/ze-core.js` (`opts.image={artId}`): «Загрузить…/Заменить…»/«Удалить» правят строку
`image:` во frontmatter textarea; «Просмотр» показывает выбранный файл как `data:`-URL ДО сохранения (подмена
`src="../img/<name>"` на data-URL выбранных байтов); «Сохранить» шлёт бинарь в `/api/save`. «Удалить» = отвязка
строки `image:` (файл в `docs/img/` остаётся — «рукописи не горят»). Worker: `commitFiles` чтит флаг `f.binary`
(content = ЧИСТЫЙ base64 байтов, без utf8-обёртки) — общий для save/commit; `/api/save` принимает опц.
`image:{name,content}` → коммит `docs/img/<name>` (валидация имени `[A-Za-z0-9_-].(jpg|jpeg|png|gif|webp)`, art-id
`[A-Za-z0-9_-]{1,32}`; роль editor+admin — admin-only `/api/commit` НЕ ослаблялся). Решение «какой эндпоинт для
editor-загрузки» = `/api/save`. **Проверки:** `worker/test_commit.mjs` 28/28 (бинарь восстановлен байт-в-байт, НЕ
двойное кодирование, негативы имя/ext/пусто/битый-art) + frontmatter-хелперы `get/set/removeFmImage` 15/15 (скретч,
удалён) + браузер end-to-end в превью (03H: текущая `03H.jpg` → «Заменить»; выбор PNG→`image: 03H.png` + data:-URL
превью [1×1 декодировался]; «Удалить»→отвязка; перехват fetch → payload `/api/save` верный: art/branch/zml-строка/
html-ссылка/`image` с PNG-сигнатурой 137-80-78-71). **Файлы (live):** `docs/ze-core.js` (+блок image, чистые
`get/set/removeFmImage`, `swapPreviewImage`, `isDirty` учитывает pendingImage), `docs/ya-edit.js` (`image:{artId:ART}`
+ image в payload). Живой авторизованный тест загрузки — ПРОПУЩЕН по решению оператора (НЕ блокер). NB: `ya-struct`
(создание новой статьи) image-блок ПОКА не подключён (scope=правка; при желании — тот же `opts.image`, save через
admin `/api/commit`, бинарь там уже проходит).

**Ф8(c) Редактирование «Песни Ступеней» — ВЖИВУЮ `974d682` + Worker `efe2a94e`.**
Кнопка ✎ на `songs/index.view.html` (admin/editor) → общий `ze-core` (БЕЗ image-бара). Рендер вынесен в
`render.js::renderSongsHtml` (новая экспорт-функция) — ОДИН код для `build_songs.mjs` (node) и `docs/ya-songs.js`
(браузер), как `renderArticleHtml` ↔ `ya-edit`. **Паритет доказан байт-в-байт в превью:** browser `renderSongsHtml`
с теми же данными === node-сборка `index.view.html` (260429 B, firstDiff −1). Save → `/api/save` `page:"songs"` (фикс.
пути `docs/songs/index.zml`+`index.view.html`, editor+admin, без art-id; admin-only `/api/commit` НЕ трогали).
`template_songs.html`: +кнопка ✎ / `data-worker="{{WORKER_URL}}"` / `<script ../ya-songs.js>` / `.vb-edit` CSS;
`build_views.mjs` теперь синкает его в `docs/editor/data/`; `build_songs.mjs` зовёт `renderSongsHtml` + читает
`site.json` (→ WORKER_URL). Живой `songs/index.html` НЕ тронут (cutover Ф9). **Проверки:** `worker/test_commit.mjs`
36/36 (+songs: 2 blob'а index.zml/view, art-id не нужен, негативы html/токен) + браузер end-to-end (✎→редактор без
image-бара, Просмотр=220 плиток, payload `page:songs`/без art верный, консоль чиста). **NB-порядок:** правка меняет
уже собранный `index.zml`; повторный `convert/gen_songs_zml.py` перезатрёт → ручные правки делать ПОСЛЕ генерации (или
переносить в `convert/songs_overrides.json`). **Файлы (live):** `docs/ya-songs.js`(новый), `docs/editor/render.js`
(+`renderSongsHtml`), `docs/editor/data/template_songs.html`(новый синк), `docs/songs/index.view.html`(пересобран).
Worker-исходник: `cms-revival/worker/src/index.js` (деплой `wrangler deploy`, нужен свежий `CLOUDFLARE_API_TOKEN`).
